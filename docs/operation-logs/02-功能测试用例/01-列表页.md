[← 返回索引](./00-索引与映射.md)

## 1. 列表页

### OL-L-01：列表加载与服务端分页

**功能模块**: 操作日志列表  
**优先级**: P0  
**用例编号**: OL-L-01  
**OpenAPI**: `GET /operation-logs`

#### 前置条件

- 系统中存在 ≥3 条操作日志记录
- 管理员已登录，有操作日志读权限

#### 测试步骤

1. 管理员登录，从侧边栏进入「操作日志」列表页
2. 观察列表加载过程中的网络请求
3. 检查筛选栏与表格列

#### 预期结果

1. 页面路由为 `/operation-logs`；面包屑 / 页面标题展示「操作日志」
2. 列表请求为 `GET /operation-logs`，携带 `page=1`、`page_size=20`（服务端分页，对齐原型与 UI 源码 `server-pagination: true`）
3. 筛选栏包含：**起始时间**、**结束时间**（DatePicker datetime 类型）、**查询**按钮
4. 表格列包含：**操作人**、**操作动作**（Tag）、**资源类型**、**资源名称**、**结果**（Tag）、**操作时间**、**操作**（详情按钮）
5. 加载成功后列表正常渲染，无报错；操作时间格式为 `yyyy-MM-dd HH:mm:ss`

---

### OL-L-02：时间范围筛选

**功能模块**: 操作日志列表  
**优先级**: P0  
**用例编号**: OL-L-02  
**OpenAPI**: `GET /operation-logs`（`start_time` / `end_time`）

#### 前置条件

- 存在时间跨度 ≥1 天的多条操作日志

#### 测试步骤

1. 在筛选栏设置「起始时间」为昨天 00:00:00
2. 设置「结束时间」为今天 23:59:59
3. 点击「查询」按钮
4. 观察网络请求与列表结果

#### 预期结果

1. 点击「查询」后触发 `GET /operation-logs`，Query 携带 `start_time` 与 `end_time`（Unix 秒级时间戳）
2. 列表仅展示 `created_at` 落在该时间闭区间内的日志
3. 查询后页码重置为第 1 页
4. 清空时间后再次查询，恢复全量（不传 `start_time` / `end_time`）

---

### OL-L-03：操作人模糊搜索

**功能模块**: 操作日志列表  
**优先级**: P1  
**用例编号**: OL-L-03  
**OpenAPI**: `GET /operation-logs`（`operator_name`）

#### 前置条件

- 存在不同操作人的日志（如 admin、testuser）

#### 测试步骤

1. 在「操作人」列搜索框输入关键词（如 `admin`）
2. 观察网络请求与筛选结果

#### 预期结果

1. 触发 `GET /operation-logs`，Query 携带 `operator_name=admin`
2. 列表仅展示操作人名称模糊匹配的记录
3. 清空搜索后恢复全量

---

### OL-L-04：操作动作筛选

**功能模块**: 操作日志列表  
**优先级**: P1  
**用例编号**: OL-L-04  
**OpenAPI**: `GET /operation-logs`（`action`）

#### 前置条件

- 存在不同 action 的日志（如 create、update、delete）

#### 测试步骤

1. 点击「操作动作」列筛选下拉
2. 选择 `update`
3. 观察筛选结果

#### 预期结果

1. 下拉选项包含：`create`、`update`、`delete`、`reset`、`import`、`bind`、`unbind`
2. 选中后触发 `GET /operation-logs`，Query 携带 `action=update`
3. 列表仅展示 action 为 `update` 的记录；操作动作列以 Tag 展示

---

### OL-L-05：资源类型筛选

**功能模块**: 操作日志列表  
**优先级**: P1  
**用例编号**: OL-L-05  
**OpenAPI**: `GET /operation-logs`（`resource_type`）

#### 前置条件

- 存在不同 resource_type 的日志

#### 测试步骤

1. 点击「资源类型」列筛选下拉
2. 选择 `entity`
3. 观察筛选结果

#### 预期结果

1. 下拉选项包含接口定义的全部资源类型：`entity`、`entity_type`、`api_key`、`provider`、`cluster`、`route`、`domain`、`certificate`、`quota_plan`、`rate_limit_policy`、`model_price`、`user`、`token`
2. 选中后触发 `GET /operation-logs`，Query 携带 `resource_type=entity`
3. 列表仅展示对应资源类型的记录

---

### OL-L-06：资源名称模糊搜索

**功能模块**: 操作日志列表  
**优先级**: P1  
**用例编号**: OL-L-06  
**OpenAPI**: `GET /operation-logs`（`resource_name`）

#### 前置条件

- 存在不同资源名称的日志

#### 测试步骤

1. 在「资源名称」列搜索框输入关键词
2. 观察筛选结果

#### 预期结果

1. 触发 `GET /operation-logs`，Query 携带 `resource_name` 参数
2. 列表仅展示资源名称模糊匹配的记录

---

### OL-L-07：结果筛选

**功能模块**: 操作日志列表  
**优先级**: P1  
**用例编号**: OL-L-07  
**OpenAPI**: `GET /operation-logs`（`status`）

#### 前置条件

- 存在成功（status=1）与失败（status=2）的日志各至少 1 条

#### 测试步骤

1. 点击「结果」列筛选下拉
2. 依次选择「成功」「失败」

#### 预期结果

1. 下拉选项为「成功」（value=1）、「失败」（value=2）
2. 选择「成功」后，列表仅展示绿色 Tag「成功」的记录；Query 携带 `status=1`
3. 选择「失败」后，列表仅展示红色 Tag「失败」的记录；Query 携带 `status=2`

---

### OL-L-08：组合筛选

**功能模块**: 操作日志列表  
**优先级**: P1  
**用例编号**: OL-L-08

#### 前置条件

- 存在满足多条件的日志记录

#### 测试步骤

1. 设置时间范围
2. 在「操作动作」选择 `create`
3. 在「资源类型」选择 `entity`
4. 点击「查询」

#### 预期结果

1. 请求同时携带 `start_time`、`end_time`、`action=create`、`resource_type=entity`
2. 列表结果同时满足所有筛选条件
3. 各列搜索与时间筛选可叠加使用

---

### OL-L-09：服务端分页

**功能模块**: 操作日志列表  
**优先级**: P1  
**用例编号**: OL-L-09  
**OpenAPI**: `GET /operation-logs`（`page` / `page_size`）

#### 前置条件

- 存在 ≥21 条操作日志（默认每页 20 条）

#### 测试步骤

1. 进入列表页，确认第 1 页展示 20 条
2. 点击「下一页」或页码 2
3. 修改每页条数为 50（如支持）
4. 观察网络请求

#### 预期结果

1. 翻页触发 `GET /operation-logs`，`page` 参数递增
2. 第 2 页展示剩余记录；分页总数与接口 `pagination.total` 一致（与首页 `total` 相同，且 `total > 20`）
3. 修改每页条数后 `page_size` 参数更新，页码重置为 1
4. 点击页码 / 下一页后，分页页码（1、2）仍可见，当前页高亮正确（回归 [#95](https://github.com/rainway-ai-gateway/ai-gateway-web/issues/95)：翻页后页码消失）

---

### OL-L-10：空列表展示

**功能模块**: 操作日志列表  
**优先级**: P2  
**用例编号**: OL-L-10

#### 前置条件

- 筛选条件导致无匹配记录（如时间范围设为未来）

#### 测试步骤

1. 设置起始 / 结束时间为未来区间
2. 点击「查询」

#### 预期结果

1. 表格展示空状态（无数据行）
2. 页面无报错；分页 total 为 0

---

### OL-L-11：操作动作 Tag 颜色

**功能模块**: 操作日志列表  
**优先级**: P2  
**用例编号**: OL-L-11

#### 前置条件

- 存在不同 action 的日志

#### 测试步骤

1. 进入列表页，观察各 action 的 Tag 颜色

#### 预期结果

1. 各 action Tag 颜色与原型一致：
   - `create` → green
   - `update` → blue
   - `delete` → red
   - `reset` → orange
   - `import` → purple
   - `bind` → cyan
   - `unbind` → volcano
2. 未知 action 使用 default 色

---
