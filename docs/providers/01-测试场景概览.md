# 模型服务商（Providers）模块 - 测试场景概览

## 模块概述

模型服务商模块负责维护 AI 网关的第三方模型服务商（Provider），是集群 LLM 配置、模型定价的**上游基础数据**。对应 API 端点：`/providers`。

对应接口文档：`design-docs/api-define/OpenAPI接口定义/providers.md`；对应原型：`design-docs/prototype-design/pages/providers.html`。

| 功能区域 | 说明 |
| --- | --- |
| 服务商列表 | **全量拉取**（`GET /providers` 不传 `page` / `page_size`），前端筛选（名称 / 描述 / 协议 / 模型）+ 前端分页 |
| 创建 / 编辑 | 基本信息（名称、描述）、实例池（IP / 服务商域名两种形态）、模型服务配置（协议、模型列表接口、模型发现回填 + 手动输入）、服务鉴权 Keys |
| 详情页 | 基本信息（含创建 / 更新时间）、实例池、模型服务配置、服务鉴权 Keys（Key 值脱敏）、分段计价配置 Card |
| 分段计价配置 | 独立 Drawer：时区（IANA 文本输入 + `Intl` 校验）、计价时段（忙时 peak）、时间段（适用时段 Checkbox + 快捷链接 + 时间输入），提交 `PUT /providers/{name}/pricing-tiers` |
| 删除 | 被集群引用时返回 `409` 不可删除；存在 `/model-prices` 同名记录不阻塞删除 |
| 模型价格跳转 | 列表操作列「查询模型价格」→ 跳转模型定价列表并自动打开详情 |

**核心变更依据**（2026-08-24 / 2026-08-25 / 2026-08-27 UI 变更）：

- Provider 为新增独立对象，实例池 / 模型协议 / 模型发现端点 / Key 明文从 Cluster 迁入；Cluster 仅通过 `llm_config.provider` 引用。
- 列表改为**全量拉取 + 前端筛选/分页**；模型列 2 Tag + `+N`。
- 新增「分段计价配置」Drawer 与详情 Card；`tier_prices` 支持分段计价。
- 模型列表支持「获取」探测回填 + **手动输入**（`filterable + allow-create`）+ **批量添加**（多行/分隔符粘贴，合并去重）。

---

## 配置顺序 / 依赖关系

模块间**真实配置链路**（用户操作顺序，主链路为强依赖）：

```text
① 配置模型服务商（Providers）          基础数据：实例池 / 模型协议 / 模型发现端点 / Keys / 分段计价
        │ 服务商名称、探测到的模型、Keys
        ▼
② 集群大模型配置引用服务商（Clusters）  llm_config.provider 指向服务商 → 转发模型 / Keys 均引用服务商数据
        ▼
③ 路由规则 / 全链路                     将请求转发至集群实例池

（并行 / 独立，不进主链路）
模型定价（Model Prices）                独立数据模块：provider 字段通常填服务商名（软关联，非必填，不阻塞集群配置）
```

**依赖关系说明**：

| 关系 | 说明 |
| --- | --- |
| Providers → Clusters（**强依赖**） | **必须先创建服务商，才能配置集群**：集群「大模型配置」的所属服务商下拉、转发模型、Keys 均来自服务商数据；服务商不存在时集群无法完成大模型配置 |
| Providers → Model Prices（软关联） | 定价记录 `provider` 字段通常填服务商名（非必填、不强校验）；从服务商列表「查询模型价格」直达定价列表；服务商删除不级联删除定价 |
| Clusters → Providers 联动 | 修改服务商 `instance_pool` 会同步更新引用该 provider 的集群实例池；Key `name` 变更影响引用集群 |

- **无前置依赖**：服务商可独立创建、编辑、删除；模型定价可独立维护，与集群配置互不阻塞
- **被引用约束**：已被集群引用的服务商不可删除（409）

---

## 场景测试设计

### 场景一：管理员查看服务商列表

**场景描述**：管理员进入服务商列表页，查看全量数据展示，使用名称 / 描述 / 协议 / 模型本地筛选、前端分页浏览，并通过操作列进入详情 / 编辑 / 删除 / 查询模型价格 / 分段计价配置。

| 步骤 | 操作 | 对应编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | 进入服务商列表页，验证全量拉取与表格加载 | PR-L-01 | P0 |
| 2 | 按名称筛选 | PR-L-02 | P1 |
| 3 | 按描述筛选 | PR-L-03 | P1 |
| 4 | 按协议筛选（openai / anthropic） | PR-L-04 | P1 |
| 5 | 按模型筛选 | PR-L-05 | P1 |
| 6 | 组合筛选 | PR-L-06 | P1 |
| 7 | 前端分页（翻页 / 页码跳转，不触发请求） | PR-L-07 | P1 |
| 8 | 模型列 2 Tag + `+N`，悬停查看全部 | PR-L-08 | P1 |
| 9 | 空列表展示 | PR-L-09 | P2 |
| 10 | 操作列按钮完整性与可用性 | PR-L-10 | P1 |
| 11 | 「查询模型价格」跳转与自动打开详情 | PR-L-11 | P0 |

**测试目标**：验证列表全量拉取、前端筛选 / 分页、模型列折叠展示、操作列跳转行为正确。

---

### 场景二：管理员创建 / 编辑服务商

**场景描述**：管理员创建（IP 模式 / 服务商域名模式）与编辑服务商，配置实例池、模型服务、Keys，通过「获取」探测并回填模型列表，或手动 / 批量输入模型名。

| 步骤 | 操作 | 对应编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | 创建服务商-成功（IP 模式） | PR-C-01 | P0 |
| 2 | 创建服务商-成功（服务商域名模式，单实例 443/100） | PR-C-02 | P0 |
| 3 | 编辑服务商-成功（名称不可改） | PR-C-03 | P0 |
| 4 | 实例池-添加 / 删除行（至少保留一行） | PR-C-04 | P1 |
| 5 | 实例 IP + 端口重复校验（底栏提示） | PR-C-05 | P0 |
| 6 | 模型协议多选必填校验 | PR-C-06 | P1 |
| 7 | 模型列表接口（schema / 只读 addr:port / uri）展示 | PR-C-07 | P1 |
| 8 | 「获取」模型按钮-可用条件与回填 | PR-C-08 | P0 |
| 9 | 「获取」置灰条件（未选协议 / 未填实例地址） | PR-C-09 | P0 |
| 10 | 模型列表手动添加 / 删除 | PR-C-10 | P0 |
| 11 | Keys 表-添加 / 删除 Key 行 | PR-C-11 | P1 |
| 12 | 获取成功后须「提交」才保存 | PR-C-12 | P0 |
| 13 | 提交体结构校验（不传 `instance_pool[].name` / `create_time` / `update_time`，无 `Authorization` 头） | PR-C-13 | P0 |
| 14 | 名称全局唯一校验（重复被拦截） | PR-C-14 | P0 |
| 15 | 取消 / 关闭抽屉 | PR-C-15 | P1 |
| 16 | 模型列表批量添加（弹窗合并去重 + 下拉粘贴拆分） | PR-C-16 | P1 |

**测试目标**：验证创建 / 编辑完整流程、两种实例形态、模型发现回填与提交体、字段联动校验。

---

### 场景三：管理员查看服务商详情

**场景描述**：管理员进入详情页，核对全部字段与接口一致，重点验证 Key 脱敏、创建 / 更新时间并入基本信息、分段计价配置 Card。

| 步骤 | 操作 | 对应编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | 详情页全字段与接口数据一致性 | PR-D-01 | P0 |
| 2 | 详情页分段计价配置 Card（未配置时提示） | PR-D-02 | P1 |

**测试目标**：验证详情页所有字段展示值与 `GET /providers/{name}` 返回完全一致。

---

### 场景四：管理员配置分段计价

**场景描述**：管理员通过操作列「分段计价配置」打开独立 Drawer，填写时区、忙时（peak）时间段，提交 `PUT /providers/{name}/pricing-tiers`。

| 步骤 | 操作 | 对应编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | 打开分段计价 Drawer（标题固定、顶部只读服务商名） | PR-T-01 | P0 |
| 2 | 时区文本输入 + IANA 校验 | PR-T-02 | P0 |
| 3 | 适用时段 Checkbox（周一~周日）与全选语义 | PR-T-03 | P1 |
| 4 | 快捷链接（全选 / 工作日 / 周末） | PR-T-04 | P1 |
| 5 | 时间段添加 / 删除（至少保留 1 段） | PR-T-05 | P1 |
| 6 | 时间格式校验（HH:MM、end > start） | PR-T-06 | P0 |
| 7 | 同 tier 时间段重叠校验 | PR-T-07 | P0 |
| 8 | 提交 PUT pricing-tiers（payload 结构） | PR-T-08 | P0 |
| 9 | 编辑回填与提交后回显 | PR-T-09 | P1 |
| 10 | 取消 / 关闭 | PR-T-10 | P2 |

**测试目标**：验证分段计价配置的完整流程、时区 / 时段校验与提交体正确。

---

### 场景五：管理员删除服务商

**场景描述**：管理员删除服务商，验证未被集群引用时可删除、被引用时 409 拦截、存在模型定价记录时不阻塞。

| 步骤 | 操作 | 对应编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | 删除服务商-成功（无集群引用） | PR-DEL-01 | P0 |
| 2 | 删除服务商-被集群引用 409 | PR-DEL-02 | P0 |
| 3 | 删除服务商-取消操作 | PR-DEL-03 | P1 |
| 4 | 删除服务商-有 model-prices 记录不阻塞 | PR-DEL-04 | P1 |

**测试目标**：验证删除的二次确认、引用保护与删除后列表刷新。

---

### 场景六：字段校验

**场景描述**：管理员在创建 / 编辑表单中填写各字段，验证前端与后端校验规则，详见字段校验矩阵。

| 步骤 | 操作 | 对应编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | name 必填与格式校验 | PR-V-01 / PR-V-02 | P0 |
| 2 | description 长度与控制字符校验 | PR-V-03 | P1 |
| 3 | instance_pool 数量 / addr / port / weight 校验 | PR-V-04 ~ PR-V-07 | P0 |
| 4 | model_protocols 数量与重复校验 | PR-V-08 | P0 |
| 5 | model_endpoint schema / uri 校验 | PR-V-09 / PR-V-10 | P1 |
| 6 | keys name / key 校验 | PR-V-11 / PR-V-12 | P0 |
| 7 | models 元素校验 | PR-V-13 | P1 |
| 8 | time_zone IANA 校验 | PR-V-14 | P0 |
| 9 | tiers / time_ranges 校验 | PR-V-15 | P1 |

**测试目标**：验证全部字段的前端校验行为与接口定义一致（参考 `providers.md` §3 校验规则）。

---

### 场景七：搭建可用的 AI 业务集群（服务商 → 集群联动，定价软关联）

**场景描述**：管理员按真实操作链路搭建可用的 AI 业务集群：**先配置模型服务商**（实例池 / 模型 / Keys）→ **集群「大模型配置」中关联该服务商**并完成配置（主链路强依赖）；**为服务商模型配置模型定价**为可选步骤（软关联，不阻塞集群配置）。验证服务商作为集群数据源的数据联动一致，以及服务商后续变更（实例池 / Keys）对引用集群的同步影响与删除保护。

| 步骤 | 操作 | 对应编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | 创建模型服务商（含实例池、模型、Keys） | PR-C-01 / PR-C-08 | P0 |
| 2 | （可选）为服务商模型配置模型定价（provider 关联服务商名） | PR-LINK-01 / MP-LINK-01 | P0 |
| 3 | 集群「大模型配置」选择该服务商（所属服务商下拉出现） | PR-LINK-02 / RM-BC-71 | P0 |
| 4 | 集群转发模型选择该服务商探测到的模型（含全选） | PR-LINK-03 / RM-BC-73 | P0 |
| 5 | 集群 Keys 下拉引用服务商 keys（名称，无明文） | PR-LINK-04 / RM-BC-76 | P0 |
| 6 | 修改服务商实例池 → 验证引用集群实例池同步更新 | PR-LINK-05 | P0 |
| 7 | 从服务商列表「查询模型价格」直达定价列表并自动打开详情 | PR-LINK-06 / PR-L-11 | P1 |
| 8 | 删除已被集群引用的服务商 → 409 拦截（主用例 PR-DEL-02，此处为端到端回归视角） | PR-LINK-07 / PR-DEL-02 | P0 |
| 9 | 删除服务商后，其模型定价记录保留 | PR-LINK-08 / MP-LINK-04 | P1 |

**测试目标**：验证服务商作为集群大模型配置的**数据源**（主链路），与模型定价的**软关联**，在端到端配置链路中数据一致、联动正确、删除保护生效。

---

## 测试场景统计

| 功能区域 | 测试场景数量 | 优先级分布 |
| --- | --- | --- |
| 列表页 | 11 个 | P0: 2, P1: 8, P2: 1 |
| 创建 / 编辑 | 16 个 | P0: 8, P1: 8 |
| 详情页 | 2 个 | P0: 1, P1: 1 |
| 分段计价 | 10 个 | P0: 5, P1: 4, P2: 1 |
| 删除 | 4 个 | P0: 2, P1: 2 |
| 字段校验 | 15 个 | P0: 7, P1: 8 |
| 跨模块联动（服务商→集群，定价软关联） | 8 个 | P0: 5, P1: 3 |
| **总计** | **66 个** | **P0: 30, P1: 34, P2: 2** |

---

## 测试用例编号规则

| 前缀 | 含义 | 示例 |
| --- | --- | --- |
| PR-L-xx | 列表页（List） | PR-L-01 ~ PR-L-11 |
| PR-C-xx | 创建 / 编辑（Create/Edit） | PR-C-01 ~ PR-C-16 |
| PR-D-xx | 详情页（Detail） | PR-D-01 ~ PR-D-02 |
| PR-T-xx | 分段计价（Tier/Pricing） | PR-T-01 ~ PR-T-10 |
| PR-DEL-xx | 删除（Delete） | PR-DEL-01 ~ PR-DEL-04 |
| PR-V-xx | 字段校验（Validation） | PR-V-01 ~ PR-V-15 |
| PR-LINK-xx | 跨模块联动（Linkage） | PR-LINK-01 ~ PR-LINK-08 |

---

## 优先级定义

| 优先级 | 级别 | 说明 |
| --- | --- | --- |
| P0 | Critical | 列表全量拉取、创建 / 编辑主流程、模型获取回填、删除引用保护、分段计价提交与核心校验、必填校验、跨模块联动（服务商→集群） |
| P1 | High | 前端筛选 / 分页、模型列折叠、时区与时段交互、取消操作、长度边界与格式校验 |
| P2 | Medium | 空列表、取消 / 关闭等次要场景 |

---

## 测试前置条件

| 测试场景 | 前置条件 |
| --- | --- |
| PR-L-01 ~ PR-L-08 | 存在多个服务商（≥3 个，覆盖不同协议、多个模型、不同实例形态） |
| PR-L-09 空列表 | 无任何服务商记录（需先清空） |
| PR-C-01 / 02 | 不存在与待创建名称重复的服务商 |
| PR-C-08 / 12 模型获取 | 预置可访问的模型发现端点（如 mock 的 `https://api.deepseek.com/v1/models`），或依赖 mock 数据 |
| PR-C-14 名称重复 | 已存在同名服务商 |
| PR-D-01 详情一致性 | 存在一条字段完整、已配置分段计价的服务商记录 |
| PR-T-01 ~ PR-T-10 分段计价 | 至少存在一个服务商（可配置 / 已配置 peak 时段） |
| PR-DEL-02 删除 409 | 存在一个被集群引用的服务商 |
| PR-DEL-04 不阻塞 | 存在一个在 `/model-prices` 有同名记录的未被引用的服务商 |
| PR-LINK-01~08 跨模块联动 | 已创建 deepseek 服务商（含探测模型 deepseek-chat、keys）；存在引用该服务商的集群；存在 provider=deepseek 的定价记录 |

---

## 测试数据准备

| 数据类别 | 准备内容 | 用途 |
| --- | --- | --- |
| 基础服务商 | name=deepseek，description 有值，instance_pool 多实例，model_protocols=[openai]，keys 2 个 | 列表 / 详情 / 编辑 / 删除 |
| 域名模式服务商 | 仅服务商域名形态，提交后 instance_pool 单实例 port=443 weight=100 | 创建用例 |
| 被引用服务商 | 已被某集群 `llm_config.provider` 引用 | 删除 409 |
| 模型定价同名记录 | `/model-prices` 中存在 provider=该服务商名的记录 | 删除不阻塞 |
| 分段计价配置 | 时区 Asia/Shanghai，peak 含 2 个时间段 | 分段计价回显 / 详情 Card |
| 联动集群 | 引用 deepseek 服务商的集群（转发模型 / Keys 已配置） | 跨模块联动（同步更新 / 删除 409） |
| 联动定价 | provider=deepseek 的模型定价记录（含 tier_prices） | 跨模块联动（查询模型价格直达 / 删除保留） |

| 数据项 | 说明 | 示例值 |
| --- | --- | --- |
| name | ProviderName，1–64，字母/数字/`_`/`-`/`.`，首尾不能为 `.`/`-`/`_` | deepseek |
| description | 可选，0–256，不能含控制字符 | DeepSeek 官方 API |
| instance_pool | 必填 ≥1；addr 必填（Hostname）；port 1–65535；weight 0–100，至少一个 >0 | {addr: api.deepseek.com, port: 443, weight: 100} |
| model_protocols | 必填 ≥1 且不重复；枚举 openai / anthropic | ["openai"] |
| model_endpoint | schema http/https（默认 https）；uri 以 `/` 开头（默认 /v1/models） | {schema: https, uri: /v1/models} |
| models | 可选，元素非空不重复；可通过发现接口回填、手动输入或批量添加 | ["deepseek-chat"] |
| keys | 可选；name 1–128 组内唯一；key 1–512 | {name: key-primary, key: sk-xxx} |
| time_zone | 合法 IANA 时区名 | Asia/Shanghai |
| tiers | 初期仅 peak；time_ranges 至少 1 个，end > start，组内不重叠 | [{name: peak, time_ranges: [...]}] |

---

## 自动化实施建议

| 阶段 | 范围 | 说明 |
| --- | --- | --- |
| 第一期 | P0 主路径 | 列表全量拉取、创建 / 编辑主流程、模型获取回填、分段计价提交、删除引用保护 |
| 第二期 | P1 用例 | 前端筛选 / 分页、模型列折叠、时区与时段交互、取消操作、格式校验 |
| 第三期 | P2 用例 | 空列表、取消 / 关闭 |
| 跨模块 | 服务商→集群联动链路 | 创建服务商 → 集群大模型配置引用（主链路）→ 实例池同步 / 删除保护；定价软关联验证 |

---

## 相关文档

- [功能测试用例](./02-功能测试用例/00-索引与映射.md) — 详细步骤与预期
- [providers.md](../../../ai-gateway-web/design-docs/api-define/OpenAPI接口定义/providers.md) — 服务商接口定义（含校验规则）
- `ai-gateway-web/design-docs/prototype-design/pages/providers.html` — 服务商原型
- `ai-gateway-web/src/modules/Providers/index.vue` — 列表页源码
- `ai-gateway-web/src/modules/Providers/components/ProviderUpsert.vue` — 创建 / 编辑抽屉
- `ai-gateway-web/src/modules/Providers/components/ProviderView.vue` — 详情页
- `ai-gateway-web/src/modules/Providers/components/ProviderPricingTiers.vue` — 分段计价 Drawer

---

*文档版本: v0.2*  
*最后更新: 2026-09-02（PR-C-16 模型列表批量添加）*
