[← 返回索引](./00-索引与映射.md)

## 1. 服务商列表

### PR-L-01：列表加载与全量拉取

**功能模块**: 服务商列表  
**优先级**: P0  
**用例编号**: PR-L-01

#### 前置条件

- 系统中存在 ≥3 个服务商（覆盖不同协议、多个模型、不同实例形态）

#### 测试步骤

1. 管理员登录，进入「资源管理 → 模型服务商」列表页
2. 观察列表加载过程中的网络请求
3. 检查表格列与数据展示

#### 预期结果

1. 列表请求为 `GET /providers`，**不携带** `page` / `page_size` / `model_protocol` 等分页或筛选 Query（对应变更：全量拉取 + 前端筛选）
2. 表格展示全部服务商记录；列包含：**名称、描述、协议、模型、操作**（对齐原型 `providers.html`）
3. 协议列为 `model_protocols` join 展示；模型列为 Tag 展示（2 个 + `+N`，见 PR-L-08）
4. 加载成功后列表正常渲染，无报错；再次点击「筛选 / 翻页」不触发列表请求（见 PR-L-02 / PR-L-07）

---

### PR-L-02：名称筛选

**功能模块**: 服务商列表  
**优先级**: P1  
**用例编号**: PR-L-02

#### 前置条件

- 存在多个名称各异的服务商

#### 测试步骤

1. 在列表页「名称」搜索框输入关键词（如 `deep`）
2. 观察筛选结果与网络请求

#### 预期结果

1. 对**全量数据**本地筛选，仅展示名称包含关键词的服务商（大小写不敏感）
2. **不触发**任何列表接口请求
3. 清空关键词后恢复全部记录

---

### PR-L-03：描述筛选

**功能模块**: 服务商列表  
**优先级**: P1  
**用例编号**: PR-L-03

#### 前置条件

- 存在描述各异的服务商

#### 测试步骤

1. 在列表页「描述」搜索框输入关键词
2. 观察筛选结果

#### 预期结果

1. 仅展示描述包含关键词的服务商（本地筛选，不触发请求）
2. 空描述记录不参与匹配

---

### PR-L-04：协议筛选

**功能模块**: 服务商列表  
**优先级**: P1  
**用例编号**: PR-L-04

#### 前置条件

- 存在协议为 openai / anthropic 的服务商

#### 测试步骤

1. 点击「协议」列筛选下拉
2. 依次选择 `openai`、`anthropic`

#### 预期结果

1. 下拉选项**仅** `openai`、`anthropic`（不含 gemini 等）
2. 选中后本地筛选出包含该协议的服务商；不触发列表请求
3. 支持多协议服务商（任选其一即可命中）

---

### PR-L-05：模型筛选

**功能模块**: 服务商列表  
**优先级**: P1  
**用例编号**: PR-L-05

#### 前置条件

- 存在 models 不同的服务商

#### 测试步骤

1. 在「模型」搜索框输入模型名关键词（如 `deepseek-chat`）
2. 观察筛选结果

#### 预期结果

1. 本地筛选出 models 中包含该关键词的服务商（基于 models join 匹配）
2. 不触发列表请求

---

### PR-L-06：组合筛选

**功能模块**: 服务商列表  
**优先级**: P1  
**用例编号**: PR-L-06

#### 前置条件

- 存在多个服务商

#### 测试步骤

1. 同时设置名称 + 协议 + 模型筛选条件
2. 观察结果

#### 预期结果

1. 各筛选条件为**与**关系，仅展示同时满足全部条件的记录
2. 任一条件清空后对应过滤失效
3. 全程不触发列表请求

---

### PR-L-07：前端分页

**功能模块**: 服务商列表  
**优先级**: P1  
**用例编号**: PR-L-07

#### 前置条件

- 服务商记录数量超过一页（pageSize=20）

#### 测试步骤

1. 点击下一页 / 上一页 / 页码跳转 / 每页条数切换
2. 观察网络请求与数据

#### 预期结果

1. 分页为**前端分页**（`server-pagination=false`），翻页**不请求**接口
2. 页码、总条数、每页条数显示正确；筛选与分页叠加生效（筛选后总条数按筛选结果计算）
3. 删除 / 编辑等操作后全量刷新列表（见 PR-DEL-01）

---

### PR-L-08：模型列 2 Tag + `+N`

**功能模块**: 服务商列表  
**优先级**: P1  
**用例编号**: PR-L-08

#### 前置条件

- 存在 models ≥3 个的服务商，及 models 为空的记录

#### 测试步骤

1. 查看 models >2 个的服务商行
2. 悬停 `+N` 标签
3. 查看 models 为空的服务商行

#### 预期结果

1. 模型列最多展示 **2 个 Tag**，其余以 `+N` 标签折叠（N = 隐藏数量）
2. 悬停 `+N` 显示全部模型列表（title / Tooltip，逐行展示）
3. models 为空时展示占位符 `-`
4. 空行 / 无模型记录不报错

---

### PR-L-09：空列表展示

**功能模块**: 服务商列表  
**优先级**: P2  
**用例编号**: PR-L-09

#### 前置条件

- 无任何服务商记录

#### 测试步骤

1. 进入服务商列表页

#### 预期结果

1. 展示空态（表格无数据占位，如「暂无数据」）
2. 筛选 / 分页控件正常显示但不报错
3. 「添加服务商」按钮可用

---

### PR-L-10：操作列按钮

**功能模块**: 服务商列表  
**优先级**: P1  
**用例编号**: PR-L-10

#### 前置条件

- 存在至少 1 个服务商

#### 测试步骤

1. 查看操作列按钮集
2. 分别点击各按钮

#### 预期结果

1. 操作列包含：**详情、编辑、删除、查询模型价格、分段计价配置**（5 个入口）
2. 「分段计价配置」为 warning 风格按钮
3. 点击「详情」打开详情 Drawer（见 PR-D-01）
4. 点击「编辑」打开编辑 Drawer 并回显（见 PR-C-03）
5. 点击「删除」弹出删除确认（见 PR-DEL-01）
6. 点击「查询模型价格」跳转（见 PR-L-11）
7. 点击「分段计价配置」打开分段计价 Drawer（见 PR-T-01）

---

### PR-L-11：「查询模型价格」跳转并按服务商筛选

**功能模块**: 服务商列表  
**优先级**: P0  
**用例编号**: PR-L-11

#### 前置条件

- 存在服务商；其中部分服务商在 `/model-prices` 有定价记录，部分没有

#### 测试步骤

1. 点击某服务商行的「查询模型价格」
2. 在模型定价列表页观察 URL、提供商筛选、列表数据与是否打开详情

#### 预期结果

**有定价记录时：**

1. 跳转到模型定价列表，URL 携带 `provider={name}`（不携带 `autoView`）
2. 提供商筛选条件选中该服务商，列表仅展示该 provider 的定价记录
3. **不打开**定价详情抽屉

**无定价记录时：**

1. 跳转到模型定价列表，URL 携带 `provider={name}`
2. 列表为空，展示提示「未找到提供商 {provider} 的模型定价」
3. 不打开定价详情、不报错

---

*文档版本: v0.3*  
*最后更新: 2026-09-03（PR-L-11 改为按服务商筛选，无记录时保留提示）*
