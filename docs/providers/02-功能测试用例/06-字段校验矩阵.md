[← 返回索引](./00-索引与映射.md)

## 6. 字段校验矩阵

> 校验规则依据 `design-docs/api-define/OpenAPI接口定义/providers.md` §3 校验规则；以下为 UI 侧应拦截 / 提示的校验用例，后端拦截以接口 422 为准。

### PR-V-01：name 必填校验

**功能模块**: 字段校验  
**优先级**: P0  
**用例编号**: PR-V-01

#### 前置条件

- 打开创建抽屉

#### 测试步骤

1. 名称留空，提交
2. 仅输入空白字符，提交

#### 预期结果

- 提交被拦截，提示名称必填；创建后名称不可修改

---

### PR-V-02：name 格式校验

**功能模块**: 字段校验  
**优先级**: P0  
**用例编号**: PR-V-02

#### 前置条件

- 打开创建抽屉

#### 测试步骤

依次输入以下名称并触发校验：

1. **长度 >64**：65 个字符 → 预期拦截，提示长度超限
2. **非法字符**：`deep seek`、`deep*seek`、`deep.` → 预期拦截，仅允许字母 / 数字 / `_` / `-` / `.`
3. **首尾非法**：`.deepseek`、`-deepseek`、`_deepseek`、`deepseek-` → 预期拦截，首尾不能为 `.` / `-` / `_`
4. **含空白**：`deep seek` → 预期拦截，不能包含空白字符
5. **合法值**：`deepseek`、`deep-seek.v2` → 预期通过

---

### PR-V-03：description 长度与控制字符校验

**功能模块**: 字段校验  
**优先级**: P1  
**用例编号**: PR-V-03

#### 前置条件

- 打开创建抽屉

#### 测试步骤

1. 描述输入 257 字符
2. 描述输入 256 字符
3. 描述输入含控制字符（如 `\n`、`\t`）的文本
4. 描述留空

#### 预期结果

- 257 字符拦截；256 字符通过
- 含控制字符拦截（0-256 字符且不能包含控制字符）
- 留空为合法（可选字段）

---

### PR-V-04：instance_pool 至少 1 个

**功能模块**: 字段校验  
**优先级**: P0  
**用例编号**: PR-V-04

#### 前置条件

- 打开创建抽屉

#### 测试步骤

1. 尝试删除全部实例行（或未填任何实例）直接提交

#### 预期结果

- 至少保留 1 行实例；空实例池提交被拦截，提示实例必填

---

### PR-V-05：instance.addr 必填与 Hostname 校验

**功能模块**: 字段校验  
**优先级**: P0  
**用例编号**: PR-V-05

#### 前置条件

- 打开创建抽屉（IP 模式）

#### 测试步骤

1. 实例地址留空提交
2. 地址输入非法 Hostname（如 `http://x`、含空格的域名、`-abc.com`、单个字符 `a`）
3. 地址输入合法值（`127.0.0.1`、`api.deepseek.com`）

#### 预期结果

- 地址必填，留空拦截
- 非法格式拦截（Hostname 须符合 RFC 1123 / IP，本系统要求长度 ≥2，标签不以 `-` 开头结尾）
- 合法 IP / 域名通过

---

### PR-V-06：instance.port 范围校验

**功能模块**: 字段校验  
**优先级**: P1  
**用例编号**: PR-V-06

#### 前置条件

- 打开创建抽屉（IP 模式）

#### 测试步骤

1. 端口输入 `0`、`65536`、非数字
2. 端口输入 `1`、`65535`、`443`

#### 预期结果

- `0` / `65536` / 非数字拦截（端口范围 1–65535）
- 边界值 1、65535 通过

---

### PR-V-07：instance.weight 范围与至少一个 >0

**功能模块**: 字段校验  
**优先级**: P0  
**用例编号**: PR-V-07

#### 前置条件

- 打开创建抽屉（IP 模式）

#### 测试步骤

1. 权重输入 `-1`、`101`、非整数
2. 全部实例权重填 `0` 提交
3. 至少一个权重填 `100`，其余 `0`

#### 预期结果

- `-1` / `101` / 非整数拦截（范围 0–100）
- 全部为 0 拦截（至少一个 `weight > 0`）
- 存在 `>0` 实例时通过

---

### PR-V-08：model_protocols 至少 1 个且不重复

**功能模块**: 字段校验  
**优先级**: P0  
**用例编号**: PR-V-08

#### 前置条件

- 打开创建抽屉

#### 测试步骤

1. 不选协议提交
2. 尝试重复选择 `openai`（多选重复项）

#### 预期结果

- 未选协议拦截（必填，至少 1 个）
- 多选控件不允许重复项（元素不可重复）
- 选项仅 `openai`、`anthropic`

---

### PR-V-09：model_endpoint.schema 校验

**功能模块**: 字段校验  
**优先级**: P1  
**用例编号**: PR-V-09

#### 前置条件

- 打开创建抽屉

#### 测试步骤

1. schema 下拉选择 `http` / `https`
2. 不选择（默认值）

#### 预期结果

- 下拉仅 `http` / `https`；未设置时默认 `https`
- 提交体与所选一致

---

### PR-V-10：model_endpoint.uri 校验

**功能模块**: 字段校验  
**优先级**: P1  
**用例编号**: PR-V-10

#### 前置条件

- 打开创建抽屉

#### 测试步骤

1. uri 输入 `v1/models`（不以 `/` 开头）、空字符串
2. uri 输入 `/v1/models`

#### 预期结果

- 不以 `/` 开头拦截；空值使用默认 `/v1/models`
- `/v1/models` 通过

---

### PR-V-11：keys.name 必填与唯一校验

**功能模块**: 字段校验  
**优先级**: P0  
**用例编号**: PR-V-11

#### 前置条件

- 打开创建抽屉，Keys 表添加 ≥2 行

#### 测试步骤

1. Key 名称留空提交
2. 两行 Key 名称相同提交
3. Key 名称 129 字符

#### 预期结果

- name 必填（1–128 字符），留空拦截
- 同一 provider 内 name 唯一，重复拦截
- 129 字符拦截

---

### PR-V-12：keys.key 必填与长度校验

**功能模块**: 字段校验  
**优先级**: P0  
**用例编号**: PR-V-12

#### 前置条件

- 打开创建抽屉，Keys 表已添加行

#### 测试步骤

1. Key 值留空提交
2. Key 值 513 字符
3. Key 值 512 字符

#### 预期结果

- key 必填且非空（1–512），留空拦截
- 513 字符拦截；512 字符通过

---

### PR-V-13：models 元素非空不重复

**功能模块**: 字段校验  
**优先级**: P1  
**用例编号**: PR-V-13

#### 前置条件

- 打开创建抽屉（模型列表可「获取」回填，也可手动输入或批量添加）

#### 测试步骤

1. 通过「获取」回填模型，观察模型列表
2. 手动输入模型名，回车 / 失焦添加，观察是否加入列表
3. 「批量添加」粘贴含重复项的多行文本，观察是否去重合并

#### 预期结果

- 回填 / 手动输入 / 批量添加结果元素非空且不重复（后端校验）
- 手动输入可回车 / 失焦添加
- 批量添加合并进现有列表，跳过已有名称
- 空列表不传或传空数组合法

---

### PR-V-14：time_zone IANA 校验

**功能模块**: 字段校验  
**优先级**: P0  
**用例编号**: PR-V-14

#### 前置条件

- 打开分段计价 Drawer

#### 测试步骤

1. 输入合法 IANA 时区（`Asia/Shanghai`、`UTC`、`America/Los_Angeles`）
2. 输入非法值（`GMT+8`、`ABC`、空）

#### 预期结果

- 合法时区通过；默认 `Asia/Shanghai`
- 非法值拦截（`Intl` 校验）；留空拦截（分段计价提交场景）
- 极少数边界时区前后端库差异时以 API 422 为准

---

### PR-V-15：tiers / time_ranges 校验

**功能模块**: 字段校验  
**优先级**: P1  
**用例编号**: PR-V-15

#### 前置条件

- 打开分段计价 Drawer

#### 测试步骤

分别验证以下场景并提交：

1. **time_ranges 为空** → 预期拦截，「忙时至少包含 1 个时间段」
2. **weekdays 元素越界（如 7）** → 预期拦截（元素须 0-6，UI 为固定 7 项 Checkbox 不会产生越界）
3. **start/end 非 HH:MM** → 预期拦截
4. **end <= start** → 预期拦截
5. **同 tier 时间段重叠（适用时段有交集且时间相交）** → 预期拦截
6. **合法配置（含 `weekdays: []` 表示每天）** → 预期通过

> UI 通过固定 Checkbox 约束 `weekdays` 取值，越界校验主要由后端兜底；前端重点覆盖格式 / 大小 / 重叠校验。

---

*文档版本: v0.3*  
*最后更新: 2026-09-02（PR-V-13 补充批量添加去重合并）*
