[← 返回索引](./00-索引与映射.md)

## 7. 跨模块联动（软关联）

> 本节验证服务商作为**数据源**，在真实配置链路（服务商 → 模型定价 → 集群大模型配置引用）中的联动行为。关联模块用例：`model-prices`（MP-C/MP-T/MP-LINK）、`business-cluster`（RM-BC-70~86）。
> 联动关系：集群 `llm_config.provider` 引用服务商名；集群转发模型 / Keys 均来自服务商数据；服务商删除受集群引用保护（409）；模型定价 `provider` 字段关联服务商名（**软关联**，删除服务商不级联删除定价）。

### PR-LINK-01：服务商模型定价（provider 关联服务商名）

**功能模块**: 跨模块联动  
**优先级**: P0  
**用例编号**: PR-LINK-01

#### 前置条件

- 已创建服务商 `deepseek`（含探测模型 deepseek-chat / deepseek-v3）

#### 测试步骤

1. 在模型定价列表「新增」，provider 填写服务商名 `deepseek`，model 填写 `deepseek-v3`，配置默认价格（及可选分时段价格）
2. 提交成功
3. 返回服务商列表，点击该服务商「查询模型价格」

#### 预期结果

1. 定价创建成功（`POST /model-prices`），provider 与服务商名一致
2. 服务商列表「查询模型价格」跳转定价列表 `provider=deepseek` 并按该服务商筛选，不打开详情（联动 PR-L-11 / MP-LINK-02）
3. 服务商与定价为**软关联**：服务商不存在时定价仍可独立存在（见 PR-LINK-08）

---

### PR-LINK-02：集群大模型配置-所属服务商下拉出现该服务商

**功能模块**: 跨模块联动  
**优先级**: P0  
**用例编号**: PR-LINK-02

#### 前置条件

- 已创建服务商 `deepseek`

#### 测试步骤

1. 进入集群创建向导第 4 步「大模型配置」
2. 展开「所属服务商」下拉

#### 预期结果

1. 下拉选项来自 `GET /providers/actions/get-provider-names`，包含 `deepseek`（联动 RM-BC-71）
2. 选择 `deepseek` 后调用 `GET /providers/deepseek` 拉取详情
3. **服务商不存在时**该下拉无法出现其名称，集群无法引用（强依赖）

---

### PR-LINK-03：集群转发模型选择服务商探测模型

**功能模块**: 跨模块联动  
**优先级**: P0  
**用例编号**: PR-LINK-03

#### 前置条件

- 服务商 `deepseek` 已通过「获取」回填模型 deepseek-chat / deepseek-v3

#### 测试步骤

1. 集群大模型配置选择 `deepseek`
2. 展开「转发模型」多选，选择模型并提交集群

#### 预期结果

1. 转发模型选项为该服务商探测到的模型（联动 RM-BC-73）
2. 支持「全选」；所选模型为服务商模型的**子集**
3. 集群提交成功后，详情 / 复查展示转发模型与该服务商模型一致

---

### PR-LINK-04：集群 Keys 引用服务商 keys

**功能模块**: 跨模块联动  
**优先级**: P0  
**用例编号**: PR-LINK-04

#### 前置条件

- 服务商 `deepseek` 已配置 keys（key-primary / key-secondary）

#### 测试步骤

1. 集群大模型配置选择 `deepseek`
2. 添加 Keys 行，展开「服务商 Key」下拉

#### 预期结果

1. 下拉选项为该服务商的 key **名称**（key-primary / key-secondary），不含 key 值明文（联动 RM-BC-76）
2. 集群提交体 `llm_config.keys` 仅含 name + weight，无 key 明文
3. 服务商无 keys 时集群隐藏 Keys 表格（联动 RM-BC-78）

---

### PR-LINK-05：修改服务商实例池 → 引用集群实例池同步

**功能模块**: 跨模块联动  
**优先级**: P0  
**用例编号**: PR-LINK-05

#### 前置条件

- 存在引用 `deepseek` 的集群；服务商实例池当前为 1 个实例

#### 测试步骤

1. 编辑服务商 `deepseek`，新增 / 修改实例池（如新增一个实例、修改权重）
2. 提交成功后打开引用集群详情

#### 预期结果

1. 服务商更新成功（`PATCH /providers/deepseek`）
2. **后端同步**：引用该 provider 的集群实例池随之更新（集群本身无需重新编辑）
3. 集群详情展示的实例池与服务商最新实例池一致
4. 修改服务商 Keys（name 变更）后，引用集群 Keys 一并更新（联动校验）

---

### PR-LINK-06：查询模型价格直达（联动）

**功能模块**: 跨模块联动  
**优先级**: P1  
**用例编号**: PR-LINK-06

#### 前置条件

- 服务商 `deepseek` 已有模型定价记录

#### 测试步骤

1. 服务商列表点击「查询模型价格」
2. 在定价列表页验证筛选与详情未打开

#### 预期结果

1. 跳转定价列表 `provider=deepseek`（不携带 `autoView`），列表按该服务商筛选（联动 PR-L-11）
2. **不打开**定价详情
3. 无定价记录时提示「未找到提供商 deepseek 的模型定价」，不打开详情、不报错

---

### PR-LINK-07：删除被集群引用的服务商（联动保护）

**功能模块**: 跨模块联动  
**优先级**: P0  
**用例编号**: PR-LINK-07

#### 前置条件

- 服务商 `deepseek` 已被某集群 `llm_config.provider` 引用

#### 测试步骤

1. 删除服务商 `deepseek` 并确认

#### 预期结果

1. 后端返回 `409`，前端提示「该服务商已被集群引用，无法删除」（联动 PR-DEL-02）
2. 服务商保留；集群配置不受影响

---

### PR-LINK-08：删除服务商后模型定价记录保留

**功能模块**: 跨模块联动  
**优先级**: P1  
**用例编号**: PR-LINK-08

#### 前置条件

- 服务商 `deepseek` 未被集群引用，但存在 provider=deepseek 的定价记录

#### 测试步骤

1. 删除服务商 `deepseek`（成功，见 PR-DEL-04）
2. 在模型定价列表按 provider=deepseek 查询

#### 预期结果

1. 服务商删除成功（`/model-prices` 同名记录不阻塞）
2. 定价记录**保留**，计费 / 报表数据不丢失（软关联，联动 MP-LINK-04）

---

*文档版本: v0.2*  
*最后更新: 2026-09-03（PR-LINK-06 改为按服务商筛选，无记录时保留提示）*
