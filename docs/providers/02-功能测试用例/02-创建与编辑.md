[← 返回索引](./00-索引与映射.md)

## 2. 创建与编辑

### PR-C-01：创建服务商-成功（IP 模式）

**功能模块**: 创建与编辑  
**优先级**: P0  
**用例编号**: PR-C-01

#### 前置条件

- 不存在与待创建名称重复的服务商

#### 测试步骤

1. 列表页点击「添加服务商」，打开创建抽屉
2. **基本信息**：填写名称 `deepseek_{ts}`、描述「DeepSeek 官方 API」
3. **实例池**：实例形态选择「IP」；填写一行实例：地址 `127.0.0.1`、端口 `80`、权重 `100`（表格列：**IP/域名、端口、权重、操作**，无名称列）
4. **模型服务配置**：模型协议多选 `openai`；schema 默认 `https`，uri 默认 `/v1/models`，只读 addr:port 展示首个实例
5. 可选执行「获取」回填模型（见 PR-C-08），手动输入模型名回车添加（见 PR-C-10），或「批量添加」（见 PR-C-16）
6. **服务鉴权 Keys**：添加 1 行，Key 名称 `key-primary`、Key 值 `sk-xxx`
7. 点击「提交」

#### 预期结果

1. 提交调用 `POST /providers`，返回 200 与完整对象
2. 提交体：`instance_pool[].name` **不传**（服务端默认与 addr 相同）；`keys` 含 name+key；不含 `create_time` / `update_time`（见 PR-C-13）
3. 提交成功后关闭抽屉，列表刷新出现新服务商
4. 实例池表格无「名称」列，行首列标题为 IP/域名

---

### PR-C-02：创建服务商-成功（服务商域名模式）

**功能模块**: 创建与编辑  
**优先级**: P0  
**用例编号**: PR-C-02

#### 前置条件

- 不存在与待创建名称重复的服务商

#### 测试步骤

1. 打开创建抽屉
2. **实例池**：实例形态选择「服务商域名」；填写服务商域名 `api.deepseek.com`
3. 其余必填项填合法值（名称、模型协议），点击「提交」

#### 预期结果

1. 域名模式仅展示单个「服务商域名」输入框，无多行实例表
2. 提交体为**单实例**：`instance_pool=[{addr: "api.deepseek.com", port: 443, weight: 100}]`（服务端默认端口 443、权重 100）
3. 提交成功，列表刷新出现新服务商

---

### PR-C-03：编辑服务商-成功

**功能模块**: 创建与编辑  
**优先级**: P0  
**用例编号**: PR-C-03

#### 前置条件

- 存在可编辑的服务商

#### 测试步骤

1. 列表页点击某服务商「编辑」
2. 观察名称字段状态，修改描述、实例池、模型协议、Keys 等
3. 点击「提交」

#### 预期结果

1. 编辑 Drawer 各字段**回显正确**（与详情一致）
2. 名称字段**只读 / 禁用**（创建后不可改）
3. 提交调用 `PATCH /providers/{name}`；`keys`、`instance_pool` 按**全量替换**提交
4. 修改实例池后，被引用该 provider 的集群实例池同步更新（后端行为，可在集群详情复核）
5. 提交成功关闭抽屉并刷新列表

---

### PR-C-04：实例池-添加 / 删除行

**功能模块**: 创建与编辑  
**优先级**: P1  
**用例编号**: PR-C-04

#### 前置条件

- 打开创建 / 编辑抽屉（IP 模式）

#### 测试步骤

1. 点击「+ 创建」添加实例行，填写多行实例
2. 删除其中一行
3. 当仅剩一行时再次点击删除

#### 预期结果

1. 「+ 创建」可新增行，最多行数不设硬限制
2. 删除行后该行数据不再参与提交
3. **至少保留一行**：仅剩一行时删除按钮**禁用**（不可删空）
4. 提交时至少一个实例 `weight > 0`（见 PR-V-07）

---

### PR-C-05：实例 IP+端口 重复校验

**功能模块**: 创建与编辑  
**优先级**: P0  
**用例编号**: PR-C-05

#### 前置条件

- 打开创建 / 编辑抽屉（IP 模式）

#### 测试步骤

1. 添加两行实例，填入相同的地址与端口（如 `127.0.0.1:80`）
2. 触发校验（失焦 / 提交）

#### 预期结果

1. 底栏单行提示「实例 IP 和端口不能重复」；重复行标识错误
2. 提交被拦截；修改为不同地址或端口后提示消失可提交
3. 前端按 `addr` 去重（与接口 `(name, addr, port)` 组合不重复规则一致）

---

### PR-C-06：模型协议多选必填

**功能模块**: 创建与编辑  
**优先级**: P1  
**用例编号**: PR-C-06

#### 前置条件

- 打开创建 / 编辑抽屉

#### 测试步骤

1. 不选择模型协议直接提交
2. 选择多个协议（openai + anthropic）提交

#### 预期结果

1. 模型协议**必填**，未选择时提交被拦截并提示
2. 选项仅 `openai`、`anthropic`；可多选且元素不可重复
3. 提交体 `model_protocols` 与所选一致

---

### PR-C-07：模型列表接口展示

**功能模块**: 创建与编辑  
**优先级**: P1  
**用例编号**: PR-C-07

#### 前置条件

- 打开创建 / 编辑抽屉，已填写实例池

#### 测试步骤

1. 观察「模型列表接口」区域
2. 修改 schema、uri，切换实例

#### 预期结果

1. 区域包含：schema 下拉（http / https）、**只读 addr:port**（取**首个实例**的地址与端口）、uri 输入框（默认 `/v1/models`）
2. uri 以 `/` 开头校验（见 PR-V-10）
3. 修改首个实例后 addr:port 同步更新；不存在配置 `headers.Authorization` 的入口

---

### PR-C-08：「获取」模型-回填

**功能模块**: 创建与编辑  
**优先级**: P0  
**用例编号**: PR-C-08

#### 前置条件

- 已选择模型协议且已填写实例地址（「获取」可用）

#### 测试步骤

1. 点击「获取」按钮
2. 观察请求与模型列表回填
3. 不点「提交」，关闭抽屉后重新打开

#### 预期结果

1. 调用 `POST /providers/tools/discover-models`，Body 传：`{model_protocol, schema, addr, port, uri?, apikey?}`
2. 返回 `Data.models` 后回填到模型列表（多选形式，支持手动输入 / 删除）
3. 请求过程无 toast；失败时（如连接失败）给出错误提示且不回填
4. 回填结果**不自动保存**——须点「提交」才写入 Provider（见 PR-C-12）

---

### PR-C-09：「获取」置灰条件

**功能模块**: 创建与编辑  
**优先级**: P0  
**用例编号**: PR-C-09

#### 前置条件

- 打开创建抽屉，尚未配置任何内容

#### 测试步骤

1. 未选择模型协议、未填实例地址时观察「获取」按钮
2. 仅选择协议但未填实例地址
3. 仅填实例地址但未选协议

#### 预期结果

1. **未选协议或未填实例地址任一不满足时，「获取」置灰**
2. 置灰时无按钮 Tooltip、无输入框下方长说明
3. 条件满足（协议已选 + 实例地址已填）后按钮可用
4. 模型列表区域旁 `?` Tooltip 说明列表用途、获取流程、置灰条件

---

### PR-C-10：模型列表手动添加与删除

**功能模块**: 创建与编辑  
**优先级**: P0  
**用例编号**: PR-C-10

#### 前置条件

- 打开创建 / 编辑抽屉

#### 测试步骤

1. 在模型列表输入框手动输入模型名，回车 / 失焦添加
2. 点击「获取」回填模型后，删除某个模型 tag
3. 观察未回填时的占位展示

#### 预期结果

1. 输入模型名回车 / 失焦后加入列表（`filterable + allow-create`）；「获取」仍可用（覆盖回填）
2. 模型 tag 可手动删除（多选可移除）
3. 未回填 / 未添加时展示占位「暂无模型」

---

### PR-C-11：Keys 表-添加 / 删除行

**功能模块**: 创建与编辑  
**优先级**: P1  
**用例编号**: PR-C-11

#### 前置条件

- 打开创建 / 编辑抽屉

#### 测试步骤

1. 点击「+ 添加 Key」新增行
2. 填写 Key 名称与 Key 值
3. 删除某行

#### 预期结果

1. Keys 表格列为：**Key 名称、Key 值、操作**；**无权重列**
2. 可添加多行、可删除；Keys 为可选字段（可为空数组）
3. 提交体 `keys: [{name, key}]`，无 weight 字段

---

### PR-C-12：获取成功后须「提交」才保存

**功能模块**: 创建与编辑  
**优先级**: P0  
**用例编号**: PR-C-12

#### 前置条件

- 「获取」可用且能成功返回模型

#### 测试步骤

1. 执行「获取」成功，模型列表已回填
2. **不点「提交」**，直接关闭抽屉
3. 重新打开编辑该服务商

#### 预期结果

1. 关闭抽屉后模型回填结果**丢失**（未提交）
2. 重新打开后模型列表为空 / 展示原已保存数据
3. 仅当点击「提交」并成功后，模型列表才持久化到 Provider

---

### PR-C-13：提交体结构校验

**功能模块**: 创建与编辑  
**优先级**: P0  
**用例编号**: PR-C-13

#### 前置条件

- 打开创建抽屉并填写完整合法数据

#### 测试步骤

1. 填写完整数据后提交，抓取请求体
2. 对照接口定义（`POST /providers`）

#### 预期结果

1. 请求体**不含** `create_time` / `update_time`（系统生成）
2. `instance_pool[].name` 不传（服务端默认与 addr 相同）
3. `models` 仅当「获取」回填后才存在；未获取时不传或传空
4. 不再配置 `headers.Authorization`；模型发现接口认证头由 `model_protocols` 决定（openai → `Authorization: Bearer`，anthropic → `x-api-key`）
5. 未传 `time_zone` 时服务端默认 `Asia/Shanghai`

---

### PR-C-14：名称全局唯一校验

**功能模块**: 创建与编辑  
**优先级**: P0  
**用例编号**: PR-C-14

#### 前置条件

- 已存在服务商 `deepseek`

#### 测试步骤

1. 创建服务商，名称填写 `deepseek`（已存在）
2. 提交

#### 预期结果

1. 前端 / 后端校验拦截，提示名称已存在（后端返回 555 / 422，以前端表现为主）
2. 抽屉不关闭，可修改名称后重新提交

---

### PR-C-15：取消 / 关闭抽屉

**功能模块**: 创建与编辑  
**优先级**: P1  
**用例编号**: PR-C-15

#### 前置条件

- 打开创建 / 编辑抽屉并填写了部分内容

#### 测试步骤

1. 点击「取消」或右上角关闭按钮
2. 再次打开抽屉

#### 预期结果

1. 抽屉关闭，已填写内容不保存
2. 若存在未保存修改，提示确认（以设计为准）
3. 列表数据不变

---

### PR-C-16：模型列表批量添加

**功能模块**: 创建与编辑  
**优先级**: P1  
**用例编号**: PR-C-16

#### 前置条件

- 打开创建 / 编辑抽屉

#### 测试步骤

1. 点击「批量添加」，在弹窗中粘贴混合分隔文本（换行、逗号、中文逗号、分号，含空行与重复项），点击「确认」
2. 列表已有模型时再次批量添加（含已有名称 + 一个新名称）
3. 在模型列表下拉框粘贴 `m1,m2,m3`
4. 填写必要字段后提交

#### 预期结果

1. Tag 为去重后的模型列表；空行与重复项被忽略
2. 仅合并新增模型，不覆盖已有 Tag
3. 剪贴板可拆出 ≥2 个名称时，自动拆成多个 Tag
4. 提交体 `models` 与当前 Tag 一致（须再点「提交」才落库）

#### 测试数据

- 批量文本：`gpt-4o` + 空行 + `gpt-4o-mini, deepseek-chat，gpt-4o;claude-3`
- 二次合并：`gpt-4o` + `llama-3`
- 下拉粘贴：`m1,m2,m3`

---

*文档版本: v0.2*  
*最后更新: 2026-09-02（PR-C-16 模型列表批量添加）*
