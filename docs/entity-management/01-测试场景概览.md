# Entity管理模块 - 测试场景概览

## 模块概述

Entity管理模块是AI网关系统的配额控制与限流管理核心模块，负责管理API访问的身份凭证和资源实体。该模块包含三个核心功能区域：

| 功能区域 | 说明 |
| --------- | ------ |
| **Entity类型管理** | 定义Entity类型（如部门、团队、个人等），支持层级级别设置（1-5级） |
| **Entity组织管理** | 创建和管理Entity实体，支持层级结构，配置配额和限流策略 |
| **API-Key管理** | 创建、管理API访问凭证，配置配额计划和限流策略，挂载到Entity |

## 配置顺序

三个模块的配置存在依赖关系，必须按照以下顺序进行：

```
Entity组织管理（默认Tab） → Entity类型管理 → API-Key管理
```

---

## 场景测试设计

本模块采用**场景测试**方法，从真实用户的使用流程出发，模拟用户在特定情境下完整操作系统，验证系统是否能正常工作。

### 场景一：管理员配置Entity类型

**场景描述**：系统管理员首次配置系统，定义Entity类型（如部门、团队、个人），建立组织架构的基础。

**涉及测试场景**：

| 步骤 | 操作 | 对应场景编号 | 优先级 |
| ------ | ------ | ------------ | -------- |
| 1 | 进入Entity类型管理页面，查看现有类型列表 | EM-T-01 | P1 |
| 2 | 点击"创建类型"，填写类型名、描述和级别，点击确定 | EM-T-02 | P0 |
| 3 | 验证类型名必填、重复、格式、长度边界校验 | EM-T-03~EM-T-06 | P1/P2 |
| 4 | 创建过程中取消操作 | EM-T-07 | P1 |
| 5 | 创建多个不同级别的类型 | EM-T-02（多次） | P0 |
| 6 | 在搜索框输入类型名进行搜索、分页 | EM-T-08~EM-T-09 | P1/P2 |
| 7 | 点击"编辑"，验证类型名不可修改，修改描述和级别 | EM-T-10~EM-T-11 | P1 |
| 8 | 点击"删除"，确认/取消删除类型 | EM-T-12~EM-T-13 | P0/P1 |
| 9 | 验证删除有关联Entity的类型时的处理 | EM-T-14 | P1 |
| 10 | 修改Entity类型级别 | EM-T-15 | P1 |

**测试目标**：验证管理员能否顺利创建、编辑、删除Entity类型，系统各项校验机制是否正常工作。

---

### 场景二：管理员配置Entity组织架构

**场景描述**：管理员基于已定义的Entity类型，创建组织架构（如部门→团队→个人的层级关系），并配置配额和限流策略。

**涉及测试场景**：

| 步骤 | 操作 | 对应场景编号 | 优先级 |
| ------ | ------ | ------------ | -------- |
| 1 | 进入Entity组织管理页面，查看现有Entity列表 | EM-E-01 | P1 |
| 2 | 点击"创建Entity"，填写名称和类型，不选择父Entity（根节点） | EM-E-02 | P0 |
| 3 | 验证名称必填、唯一性校验 | EM-E-03~EM-E-04 | P1 |
| 4 | 创建带配额计划和限流策略的Entity | EM-E-05~EM-E-06 | P0 |
| 5 | 验证限流至少配置一项的校验 | EM-E-20 | P0 |
| 6 | 创建子Entity，验证父Entity下拉级别联动 | EM-E-07、EM-E-18 | P0 |
| 7 | 创建多层级Entity（部门→团队→个人） | EM-E-08 | P0 |
| 8 | 创建过程中取消操作 | EM-E-09 | P1 |
| 9 | 按ID、名称、类型、父Entity、配额、限流状态搜索/筛选Entity | EM-E-10~EM-E-12、EM-E-12b/12c | P1 |
| 10 | 编辑Entity，验证名称/类型不可改，修改配额和限流 | EM-E-13、EM-E-19 | P1/P0 |
| 11 | 点击行查看Entity详情、重置配额 | EM-E-14~EM-E-15 | P1 |
| 12 | 删除无下级的Entity | EM-E-16 | P0 |
| 13 | 删除有下级的Entity | EM-E-17 | P1 |
| 14 | 删除有API-Key挂载的Entity | EM-E-21 | P0 |
| 15 | E2E：类型→根/子Entity→删除约束验证（自动化见场景四） | EM-E-22 | P0 |
| 16 | 配额/并发边界校验（小数、上界、下界） | EM-E-23~EM-E-25、EM-E-38 | P0 |
| 17 | TPM/RPM 规则字段校验（必填、范围、上界） | EM-E-26~EM-E-42 | P0 |
| 18 | 名称格式校验（含 `@` / 首尾限制）、TPM/RPM 组合重复、规则名称长度 | EM-E-43~EM-E-46、EM-CV-01~02、EM-CV-09 | P0/P1 |
| 19 | RMB 单位配额小数校验、4 位小数边界、total_token 必须整数 | EM-E-47~EM-E-49 | P0 |
| 20 | RMB 单位配额详情展示、列表用量展示 | EM-E-50~EM-E-51 | P0/P1 |
| 21 | RMB 配额 9000 万上限与边界校验 | EM-E-52、EM-E-53、EM-E-55 | P0/P1 |
| 22 | 重置配额弹窗-total_token 上界校验 | EM-E-56 | P1 |

**测试目标**：验证管理员能否顺利创建、编辑、删除Entity，层级关系是否正确，配额和限流策略配置是否正常，支持 RMB 单位配额（小数精度、展示）。

---

### 场景三：管理员配置API-Key

**场景描述**：管理员为内部程序或外部用户创建API访问凭证，配置配额和限流策略，并挂载到特定的Entity上。

**涉及测试场景**：

| 步骤 | 操作 | 对应场景编号 | 优先级 |
| ------ | ------ | ------------ | -------- |
| 1 | 进入API-Key管理页面，查看列表及Key脱敏展示 | EM-K-01、EM-K-19 | P1/P0 |
| 2 | 打开创建表单，验证默认值 | EM-K-20 | P0 |
| 3 | 点击"添加"，填写描述，创建API-Key | EM-K-02 | P0 |
| 4 | 验证描述必填校验 | EM-K-03 | P1 |
| 5 | 配置过期时间、允许子网（含IPv6）、允许模型 | EM-K-21~EM-K-23 | P0/P1 |
| 6 | 创建带配额计划和限流策略的API-Key，验证边界与联动 | EM-K-04~EM-K-05、EM-K-24~EM-K-26 | P0 |
| 7 | 创建挂载Entity和不挂载Entity的API-Key | EM-K-06~EM-K-07 | P0/P1 |
| 8 | 创建无限配额和带过期时间的API-Key | EM-K-08 | P1 |
| 9 | 创建过程中取消操作 | EM-K-09 | P1 |
| 10 | 按Key ID、Key值、描述、状态、配额类型、配额、限流状态、挂载Entity筛选API-Key | EM-K-10~EM-K-12、EM-K-15b/15c | P1 |
| 11 | 编辑API-Key，验证数据回填，修改描述/配额/挂载 | EM-K-13~EM-K-14、EM-K-27 | P1/P0 |
| 12 | 点击行查看API-Key详情 | EM-K-28 | P1 |
| 13 | 删除API-Key（含确认/取消） | EM-K-17 | P0 |
| 14 | 重置API-Key配额余额及校验 | EM-K-18、EM-K-29 | P1 |
| 15 | E2E：创建→编辑→重置配额→删除（自动化见场景四 EM-J-01） | EM-K-30 | P0 |
| 16 | 配额/并发边界校验（小数、上界、下界） | EM-K-26~EM-K-28、EM-K-31、EM-K-44 | P0 |
| 17 | TPM/RPM 规则字段校验（必填、范围、上界） | EM-K-32~EM-K-48 | P0 |
| 18 | TPM/RPM 组合重复、规则名称长度 | EM-K-49~EM-K-51 | P0/P1 |
| 19 | RMB 单位配额小数校验、4 位小数边界、total_token 必须整数 | EM-K-52~EM-K-54 | P0 |
| 20 | RMB 单位配额详情展示、列表用量展示 | EM-K-55~EM-K-56 | P0/P1 |
| 21 | RMB 配额 9000 万上限与边界校验 | EM-K-57、EM-K-58、EM-K-60 | P0/P1 |
| 22 | 重置配额弹窗-total_token 上界校验 | EM-K-61 | P1 |

**测试目标**：验证管理员能否顺利创建、编辑、删除API-Key，配额和限流策略配置是否正常，挂载Entity功能是否正常，支持 RMB 单位配额（小数精度、展示）。

> **说明**：系统不提供列表内禁用/启用 API-Key 操作（无 EM-K-15/16）。

---

### 场景四：跨模块场景串联（@journey）

**场景描述**：在单条自动化用例中，按真实管理员配置顺序，**全程 UI 操作**串联三个功能区域，验证跨模块依赖与端到端流程。与场景一～三的功能用例（`tests/api-key/test_01~05`、`tests/entity-management/test_01~09`）**并存**，不互相替代。

| 步骤 | 操作 | 对应编号 | 自动化 | 优先级 |
| ------ | ------ | ---------- | -------- | -------- |
| 1 | UI 创建 Entity 类型 | EM-T-02（步骤复用） | `test_05_entity_full_journey` EM-J-01 | P0 |
| 2 | UI 创建 Entity 组织（根节点） | EM-E-02（步骤复用） | 同上 | P0 |
| 3 | UI 创建 API-Key 并挂载 Entity、配置配额 | EM-K-02/04/06（步骤复用） | 同上 | P0 |
| 4 | UI 编辑 API-Key → 重置配额 → 删除 Key → 删除 Entity → 删除类型 | EM-K-30 | 同上 | P0 |
| 5 | UI 创建父/子类型与父/子 Entity，验证删除约束后清理 | EM-E-22 | `test_05_entity_full_journey` EM-E-22 | P0 |

**测试目标**：验证「类型 → 组织 → API-Key」配置顺序下，全 UI 路径可走通；删除约束在跨步骤上下文中仍成立。

**布局合理性**：各步骤创建数据时，使用长名称/描述验证列表、详情、编辑回显不破坏布局（已整合到各功能用例的预期结果中）。

**代码入口**：`tests/entity-management/test_05_entity_full_journey.spec.js`（`serial` + `@journey`）

**运行**：

```bash
npx playwright test tests/entity-management/test_05_entity_full_journey.spec.js
```

> **编号说明**：`EM-J-xx` 为**场景串联专用编号**（Journey）；步骤与预期详见 [02-功能测试用例](./02-功能测试用例/00-索引与映射.md) §2.22、§3.30、§4.1。

---

### 场景五：PageTable 通用组件交互测试

**场景描述**：验证列表页面使用的 PageTable 公用组件在添加/编辑/分页等交互场景下的行为正确性。

**涉及测试场景**：

| 步骤 | 操作 | 对应场景编号 | 优先级 |
| ------ | ------ | ------------ | -------- |
| 1 | 输入查不到数据的搜索条件 → 添加数据 → 验证搜索条件被清空 | PT-01 | P0 |
| 2 | 输入搜索条件筛选出数据 → 编辑该数据 → 验证搜索条件被清空 | PT-02 | P0 |
| 3 | 选择状态下拉筛选 → 添加数据 → 验证筛选条件被重置 | PT-03 | P0 |
| 4 | 批量创建数据触发分页 → 翻到第 2 页 → 编辑第 2 页数据 → 验证页码回到第 1 页 | PT-04 | P0 |

**测试目标**：验证 PageTable 公用组件在添加/编辑后搜索条件清理、翻页编辑后页码重置等交互场景的正确性。

---

### 场景六：详情页全字段一致性验证

**场景描述**：验证详情页展示的每一个字段都与接口返回的数据一致，覆盖基本信息、配额信息、限流信息、时间信息等所有字段。

**涉及测试场景**：

| 步骤 | 操作 | 对应场景编号 | 优先级 |
| ------ | ------ | ------------ | -------- |
| 1 | 创建带完整配置的 API-Key（配额 + 限流+挂载 Entity）→ 编辑触发 update_time → 验证详情页所有字段（含 RMB 单位动态展示） | AKD-01 | P0 |
| 2 | 创建带完整配置的子 Entity（含配额 + 限流）→ 验证详情页所有字段（含 RMB 单位动态展示） | EOD-01 | P0 |

**测试目标**：验证详情页字段与接口数据的一致性，包括：

- 基本信息：描述/名称、Key ID/类型、状态、挂载 Entity/父 Entity
- 配额信息：无限配额、配额总量、已使用、剩余量、配额单位（支持 RMB 单位动态展示）、重置周期
- 限流信息：限流状态、TPM 规则、RPM 规则
- 时间信息：创建时间、更新时间

---

## 测试场景统计

### 按功能区域

| 功能区域 | 测试场景数量 | 优先级分布 |
| --------- | ------------ | ----------- |
| Entity 类型管理 | 15 个 | P0: 3, P1: 8, P2: 4 |
| Entity 组织管理 | 37 个 | P0: 20, P1: 17 |
| API-Key 管理 | 43 个 | P0: 26, P1: 17 |
| PageTable 通用组件 | 4 个 | P0: 4 |
| 详情页一致性验证 | 2 个 | P0: 2 |
| **总计** | **103 个** | **P0: 55, P1: 44, P2: 4** |

### 按使用场景

| 使用场景 | 涉及场景编号 | 场景数量 | 说明 |
| --------- | ------------ | --------- | ------ |
| 场景一：管理员配置 Entity 类型 | EM-T-01~EM-T-15 | 15 个 | 唯一用例数 |
| 场景二：管理员配置 Entity 组织架构 | EM-E-01~EM-E-56、EM-E-12b/12c | 38 个 | 唯一用例数，含 RMB 单位配额 |
| 场景三：管理员配置 API-Key | EM-K-01~EM-K-14、EM-K-15b/15c、EM-K-17~EM-K-30、EM-K-44~EM-K-61 | 45 个 | 唯一用例数，含 RMB 单位配额 |
| 场景四：跨模块场景串联 | EM-J-01、EM-E-22（journey） | 2 个 | 串联用例，步骤复用 T/E/K |
| 场景五：PageTable 通用组件交互 | PT-01~PT-04 | 4 个 | 列表组件交互测试 |
| 场景六：详情页全字段一致性 | AKD-01、EOD-01 | 2 个 | 详情页字段验证，含 RMB 单位动态展示 |

> **统计口径说明**：场景数量按"唯一用例数"统计，即同一用例在多个场景中复用时只计一次。场景四为自动化串联层，不计入 79 条功能用例总数。

---

## 测试用例编号规则

| 前缀 | 含义 | 示例 |
| ------ | ------ | ------ |
| EM-T-xx | Entity 类型管理（Type） | EM-T-01 ~ EM-T-15 |
| EM-E-xx | Entity 组织管理（Entity） | EM-E-01 ~ EM-E-56 |
| EM-K-xx | API-Key 管理（Key） | EM-K-01 ~ EM-K-14、EM-K-17 ~ EM-K-30、EM-K-44 ~ EM-K-61 |
| EM-J-xx | 跨模块场景串联（Journey） | EM-J-01（全 UI 配置链，覆盖 EM-K-30 目标） |
| PT-xx | PageTable 通用组件测试 | PT-01 ~ PT-04 |
| AKD-xx | API-Key 详情页一致性测试 | AKD-01 |
| EOD-xx | Entity Org 详情页一致性测试 | EOD-01 |

### 自动化文件映射

| 层级 | 文件 | 用例范围 | 说明 |
| ------ | ------ | ---------- | ------ |
| 功能回归 | `tests/entity-type/test_01` ~ `test_03` | EM-T-01 ~ EM-T-15 | Entity 类型 |
| 功能回归 | `tests/entity-management/test_01` ~ `test_04` | EM-E-01 ~ EM-E-17 等 | Entity 组织 |
| 功能回归 | `tests/api-key/test_01` ~ `test_02` | EM-K-01 ~ EM-K-19 等 | API-Key |
| 场景串联 | `tests/entity-management/test_05` | EM-J-01、EM-E-22 | @journey，全 UI 跨模块 |
| 组件交互 | `tests/entity-management/test_07_page_table_interaction` | PT-01 ~ PT-04 | PageTable 通用组件 |
| 详情页验证 | `tests/api-key/test_05_api_key_display_consistency` | AKD-01 | API-Key 详情页全字段 |
| 详情页验证 | `tests/entity-management/test_08_entity_org_display_consistency` | EOD-01 | Entity Org 详情页全字段 |

---

## 优先级定义

| 优先级 | 级别 | 说明 |
| -------- | ------ | ------ |
| P0 | Critical | 核心功能，影响系统正常使用，必须通过 |
| P1 | High | 重要功能，影响用户体验，应该通过 |
| P2 | Medium | 一般功能，边界场景，建议通过 |

---

## 测试前置条件

| 测试场景 | 前置条件 |
| --------- | --------- |
| EM-E-02 创建Entity | 需先创建至少一个Entity类型（EM-T-02） |
| EM-E-06 / EM-E-07 创建子Entity | 需先创建父Entity（EM-E-02） |
| EM-K-06 创建挂载Entity的API-Key | 需先创建至少一个Entity（EM-E-02） |
| EM-T-14 删除有关联Entity的类型 | 需先创建类型和关联的Entity |
| EM-E-17 删除有下级的Entity | 需先创建父子Entity关系 |
| EM-E-21 删除有API-Key挂载的Entity | 需先创建Entity并挂载API-Key |
| EM-K-30 E2E API-Key全流程 | 需先完成 Entity 类型与组织配置；自动化见 `test_05_entity_full_journey` EM-J-01 |
| EM-E-22 E2E 层级与删除约束 | 需 UI 创建类型与 Entity；自动化见 `test_05_entity_full_journey` EM-E-22 |

---

## 测试数据准备

| 数据类别 | 准备内容 | 用途 |
| ---------- | ---------- | ------ |
| Entity类型 | `dep`(level=1)、`team`(level=2)、`person`(level=3) | 类型与层级、父子关系校验 |
| Entity | `root-dep`（根）、`sub-team`（子，parent=root-dep） | 挂载、删除约束、父列展示 |
| API-Key | 有限配额+有 used；无限配额；已挂/未挂 Entity；长短 key 各一条 | 列展示、编辑回填、重置配额 |
| 依赖顺序 | Entity-Type → Entity → API-Key | 与配置顺序一致 |

| 数据项 | 说明 | 示例值 |
| -------- | ------ | -------- |
| Entity类型名 | 全局唯一，1-32字符，仅小写字母/数字/下划线/连字符 | dep、team、person |
| Entity名称 | 全局唯一，1-64 字符；小写字母/数字/`_`/`-`/`@`；不能以 `_`、`-`、`@` 开头或结尾 | op、bfe、user@project |
| Entity级别 | 1-5，数字越小级别越高 | level=1（部门）、level=2（团队）、level=3（个人） |
| API-Key描述 | 自定义描述 | BFE项目测试Key |
| 配额总量（total_token 单位） | 非负整数，取值范围 0 ~ 9,999,999,999 | 100000000 |
| 配额总量（RMB 单位） | 非负数，最多 4 位小数；取值范围 0 ~ 90,000,000.00 | 1299.9900 |
| 配额单位 | total_token / RMB | RMB |
| 重置周期 | never/weekly/monthly | monthly |
| 限流TPM | Token每分钟限制 | 10000 tokens/min |
| 限流RPM | 请求每分钟限制 | 100 requests/min |
| 并发限制 | 最大并发数 | 50 |

---

## 自动化实施建议

| 阶段 | 范围 | 说明 | 状态 |
| ------ | ------ | ------ | ------ |
| 第一期 | 全部 P0 功能用例 | 主路径：创建/编辑/删除、删除约束 | 大部分已落地（`tests/entity-management/test_01~09`、`tests/api-key/test_01~05`） |
| 第二期 | 全部 P1 用例 + P2 抽样 | 搜索分页、详情、表单边界校验 | 进行中 |
| 第三期 | E2E 回归集（EM-E-22、EM-K-30） | @journey 跨模块串联 | **已落地** `test_05_entity_full_journey` |

**双层结构**：

| 层级 | 目的 | 入口 |
|------|------|------|
| 功能层 | 单点回归、快速定位 | `npm run test:entity` |
| 场景层 | 真实用户路径、跨模块依赖 | `npm run test:journey` |

---

## 自动化实施注意事项

> 以下规范详见 [SKILL.md](../../.trae/skills/ai-gateway-test-generation/SKILL.md) 对应章节。

### 测试标签（Tag）

| 标签 | 用途 | 示例 |
| ------ | ------ | ------ |
| `@smoke` | 冒烟测试，核心功能快速验证 | `test('@smoke 验证列表页面加载', ...)` |
| `@regression` | 回归测试，完整 CRUD 流程 | `test('@regression 验证创建成功', ...)` |
| `@validation` | 表单校验 | `test('@validation 验证必填校验', ...)` |
| `@journey` | 端到端场景串联 | `test('@journey 全流程配置链验证', ...)` |

### Spec 文件结构

- 使用 `test.step()` 组织步骤，步骤描述清晰
- 前置条件中有数据清理（防残留）
- 结尾有 `清理测试数据` step
- 使用 Page Object 模式，spec 中无裸选择器

### 并发安全

- 多 worker 并行时，测试数据必须加时间戳/随机后缀隔离
- 命名规范：`test_{模块}_{时间戳}_{随机数}`
- 避免多用例同时操作同一资源（如同一父 Entity 下创建同名子节点）

### 异步等待

- 使用 Playwright 内置的 auto-waiting 机制
- 避免硬编码 `waitForTimeout`
- 长流程用例设置合理超时时间

### 敏感信息处理

- 密码、Token、Session Key 等不得硬编码在代码中
- 使用环境变量或 `config/config.ts` 中的配置管理
- 测试账号信息通过 `auth.setup.ts` 统一获取

### 环境依赖

- 不同环境（test/staging/prod）可能有差异，用例需兼容
- 避免依赖特定环境的预置数据
- 每个用例应能独立运行，不依赖其他用例的执行结果

### 用例追溯标记

- 用例标题中包含需求 ID 和 Bug ID，便于追溯和影响分析
- 格式：`EM-T-01 Entity类型列表展示 [REQ-xxx]`
- 精准回归时可根据标记快速定位受影响用例

---

## 相关文档

- [功能测试用例](./02-功能测试用例/00-索引与映射.md) - 按功能模块组织的详细测试用例，包含完整的测试步骤、预期结果和测试数据
- [SKILL.md](../../.trae/skills/ai-gateway-test-generation/SKILL.md) - 测试用例编写指南（完整规范）

---

---

## 变更说明

### v2.0 (2026-08-14)

**变更主题**：RMB 单位配额支持

**变更内容**：

- 场景二（配置Entity组织架构）：新增 RMB 单位配额小数校验（EM-E-47）、4 位小数边界（EM-E-48）、total_token 必须整数（EM-E-49）、详情展示（EM-E-50）、列表用量展示（EM-E-51）；补充名称格式/组合重复/规则名称长度（EM-E-43~46）；测试目标补充 RMB 单位支持
- 场景三（配置API-Key）：新增 RMB 单位配额小数校验（EM-K-52）、4 位小数边界（EM-K-53）、total_token 必须整数（EM-K-54）、详情展示（EM-K-55）、列表用量展示（EM-K-56）；补充 TPM/RPM 组合重复/规则名称长度（EM-K-49~51）；测试目标补充 RMB 单位支持
- 场景六（详情页一致性）：AKD-01、EOD-01 描述补充 RMB 单位动态展示
- 测试场景统计：85 → 95（+10），P0: 43→51, P1: 38→40, P2: 4
- 测试数据准备：补充 RMB 单位配额示例数据

---

### v2.1 (2026-08-16)

**变更主题**：RMB 配额 9000 万元上限支持

**变更内容**：

- 场景二（配置Entity组织架构）：新增 RMB 配额 9000 万上界校验（EM-E-52）、9000 万边界值合法（EM-E-53）、重置配额弹窗上界校验（EM-E-54）
- 场景三（配置API-Key）：新增 RMB 配额 9000 万上界校验（EM-K-57）、9000 万边界值合法（EM-K-58）、重置配额弹窗上界校验（EM-K-59）
- 测试场景统计：95 → 99（+4），P0: 51→55, P1: 40, P2: 4
- 测试数据准备：RMB 配额取值范围补充 0 ~ 90,000,000.00

---

### v2.2 (2026-08-16)

**变更主题**：RMB 配额 9000 万上限补充编辑场景校验

**变更内容**：

- 场景二（配置Entity组织架构）：新增编辑表单 RMB 配额 9000 万上界校验（EM-E-55）
- 场景三（配置API-Key）：新增编辑表单 RMB 配额 9000 万上界校验（EM-K-60）
- 测试场景统计：99 → 101（+2），P0: 55, P1: 40→42, P2: 4

---

### v2.3 (2026-09-02)

**变更主题**：total_token 配额 DECIMAL(18,8) 上限覆盖重置弹窗

**变更内容**：

- 场景二（配置Entity组织架构）：新增重置配额弹窗 total_token 上界校验（EM-E-56）；创建上界仍为 EM-E-24
- 场景三（配置API-Key）：新增重置配额弹窗 total_token 上界校验（EM-K-61）；创建上界仍为 EM-K-28
- 测试场景统计：101 → 103（+2），P0: 55, P1: 42→44, P2: 4

### v2.4 (2026-09-05)

**变更主题**：Entity 名称字符集放开 `@`

**变更内容**：

- 场景二：名称格式校验补充 `用户名@项目名`（EM-CV-09）；首尾限制扩展为 `_`、`-`、`@`（EM-CV-02）
- 测试数据准备：Entity 名称示例增加 `user@project`

---

*文档版本: v2.4*  
*最后更新: 2026-09-05（Entity 名称放开 @）*
