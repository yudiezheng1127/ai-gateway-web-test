[← 返回索引](./00-索引与映射.md)

### 2.23 创建Entity-配额总量小数校验

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-23

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 点击"创建Entity"按钮
2. 填写名称、选择类型
3. 在"配额信息"区域：无限配额选择"否"
4. 在"配额总量"尝试输入小数值（如 `5.3`）
5. （自动化）因 InputNumber `:precision="0"` **界面无法输入小数**，通过写模型触发校验后确认 tip

#### 预期结果

- **产品 UI**：配额总量 InputNumber 精度为 0，手工无法稳定输入小数（会被截断/拒绝）
- **校验文案**（字段校验路径）：**配额总量必须为非负整数**
- 不出现后端 JSON 解析类错误（如 `cannot unmarshal number 5.3 into … int64`）

#### 测试数据

- 配额总量：`5.3`（自动化经 Vue 模型注入；UI 无法直接键入）

---

### 2.24 创建Entity-配额总量上界校验

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-24

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 点击"创建Entity"按钮
2. 填写名称、选择类型
3. 在"配额信息"区域：无限配额选择"否"
4. 在"配额总量"写入超过 `9999999999` 的值（如 `10000000000`，InputNumber `:max` 会钳制界面输入，自动化经 Vue 模型注入）
5. 点击"提交"按钮

#### 预期结果

- 表单校验失败，抽屉不关闭
- 配额总量字段显示：**配额总量超出允许范围**
- 不出现后端 JSON 解析类错误（如 `cannot unmarshal number … into … int64`）

#### 测试数据

- 配额总量：`10000000000`（超过 `DECIMAL(18,8)` 整数上限 `9999999999`）

---

### 2.25 创建Entity-最大并发上界校验

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-25

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 点击"创建Entity"按钮
2. 填写名称、选择类型
3. 在"限流配置"区域：启用限流选择"是"
4. 最大并发下拉选择"限制并发数"，在输入框中输入超过 int32 上界的数值（如 `2147483648`）
5. 点击"提交"按钮

#### 预期结果

- 表单校验失败，抽屉不关闭
- 最大并发字段显示：**最大并发超出允许范围**
- 不出现后端 JSON 解析类错误

#### 测试数据

- 最大并发下拉：选择"限制并发数"，输入 `2147483648`（超过 int32 最大值 `2147483647`）

---

### 2.26 TPM/RPM 规则字段校验

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-26 ~ EM-E-37  
**自动化**: `test_06_entity_rate_limit_fields.spec.js`

#### 前置条件

- 已登录并进入 Entity 组织管理
- 已存在 Entity 类型
- 创建 Entity 表单中**启用限流 = 是**，并已添加对应 TPM/RPM 规则行

> **序号说明**：错误提示中的「第 **n** 条」= 该类型（TPM 或 RPM）规则列表中**从 1 计数**的序号，与出错的那一行一一对应（UI 校验 `{ index: i + 1 }`）。仅添加 1 条规则时 n=1；若第 2 条规则字段非法，则显示「第2条…」，以此类推。

#### 校验矩阵

1. **EM-E-26 · TPM 规则名称**：输入留空 → 预期「第{n}条TPM规则请输入规则名称」
2. **EM-E-27 · TPM 时间窗口(分)**：输入 `0` → 预期「第{n}条TPM规则时间窗口范围为1-360分钟」
3. **EM-E-28 · TPM 时间窗口(分)**：输入 `361` → 预期「第{n}条TPM规则时间窗口范围为1-360分钟」
4. **EM-E-29 · TPM 滑动步长(分)**：输入 `0` → 预期「第{n}条TPM规则滑动步长范围为1-360分钟」
5. **EM-E-30 · TPM 滑动步长(分)**：输入 `361` → 预期「第{n}条TPM规则滑动步长范围为1-360分钟」
6. **EM-E-31 · TPM 最大Token数**：输入 `0` → **允许为 0**（合法下界）
7. **EM-E-32 · TPM 最大Token数**：输入 `9223372036854775808` → 预期「第{n}条TPM规则最大Token数超出允许范围」
8. **EM-E-33 · RPM 规则名称**：输入留空 → 预期「第{n}条RPM规则请输入规则名称」
9. **EM-E-34 · RPM 时间窗口(分)**：输入 `0` → 预期「第{n}条RPM规则时间窗口范围为1-360分钟」
10. **EM-E-35 · RPM 时间窗口(分)**：输入 `361` → 预期「第{n}条RPM规则时间窗口范围为1-360分钟」
11. **EM-E-36 · RPM 最大请求数**：输入 `0` → 允许为 0（合法下界）
12. **EM-E-37 · RPM 最大请求数**：输入 `9223372036854775808` → 预期「第{n}条RPM规则最大请求数超出允许范围」

#### 字段取值约定

- **规则名称**：必填，非空字符串
- **时间窗口(分)**：1 ~ 360
- **滑动步长(分)（仅 TPM）**：1 ~ 360，且 ≤ 时间窗口
- **最大Token数（TPM）**：0 ~ 9223372036854775807
- **最大请求数（RPM）**：0 ~ 9223372036854775807

---

### 2.38 创建Entity-最大并发下拉选项校验

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-38  
**说明**: 最大并发已改为下拉选择（不限制 / 封禁 / 限制并发数），不再有数值下界校验

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 点击"创建Entity"按钮
2. 填写名称、选择类型
3. 在"限流配置"区域：启用限流选择"是"
4. 最大并发下拉选择"不限制"，不添加 TPM/RPM 规则
5. 点击"提交"按钮
6. 观察校验提示
7. 将最大并发下拉修改为"封禁"
8. 点击"提交"按钮
9. 观察创建结果

#### 预期结果

- 步骤5-6：选择"不限制"且不配置 TPM/RPM 时，表单校验失败，显示：**请添加 TPM/RPM 规则，或将最大并发设为「封禁」/「限制并发数」**
- 步骤8-9：选择"封禁"时，创建成功（封禁表示禁止所有请求）

#### 测试数据

- 最大并发下拉：「不限制」（不能单独满足限流要求）、「封禁」（合法，满足限流要求）

---

### 2.39 创建Entity-TPM滑动步长必填校验

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-39

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 点击"创建Entity"按钮
2. 填写名称、选择类型
3. 在"限流配置"区域：启用限流选择"是"
4. 点击"添加规则"按钮，添加一条 TPM 规则
5. 填写规则名称、适用模型、时间窗口(分)=1、最大Token数=10000
6. **不填写滑动步长(分)**，保持为空
7. 点击"提交"按钮
8. 观察校验提示

#### 预期结果

- 步骤7-8：表单校验失败，抽屉不关闭，滑动步长字段显示：**第{n}条TPM规则滑动步长不能为空**

#### 测试数据

- TPM 规则名称：tpm_rule_step_required
- 时间窗口(分)：1
- 最大Token数：10000
- 滑动步长(分)：留空

---

### 2.40 创建Entity-TPM时间窗口必填校验

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-40

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 点击"创建Entity"按钮
2. 填写名称、选择类型
3. 在"限流配置"区域：启用限流选择"是"
4. 点击"添加规则"按钮，添加一条 TPM 规则
5. 填写规则名称、适用模型、最大Token数=10000、滑动步长=1
6. **不填写时间窗口(分)**，保持为空
7. 点击"提交"按钮
8. 观察校验提示

#### 预期结果

- 步骤7-8：表单校验失败，抽屉不关闭，时间窗口字段显示：**第{n}条TPM规则时间窗口不能为空**

#### 测试数据

- TPM 规则名称：tpm_rule_window_required
- 时间窗口(分)：留空
- 最大Token数：10000
- 滑动步长(分)：1

---

### 2.41 创建Entity-TPM最大Token数必填校验

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-41

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 点击"创建Entity"按钮
2. 填写名称、选择类型
3. 在"限流配置"区域：启用限流选择"是"
4. 点击"添加规则"按钮，添加一条 TPM 规则
5. 填写规则名称、适用模型、时间窗口=1、滑动步长=1
6. **不填写最大Token数**，保持为空
7. 点击"提交"按钮
8. 观察校验提示

#### 预期结果

- 步骤7-8：表单校验失败，抽屉不关闭，最大Token数字段显示：**第{n}条TPM规则最大Token数不能为空**

#### 测试数据

- TPM 规则名称：tpm_rule_token_required
- 时间窗口(分)：1
- 最大Token数：留空
- 滑动步长(分)：1

---

### 2.42 创建Entity-RPM最大请求数必填校验

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-42

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 点击"创建Entity"按钮
2. 填写名称、选择类型
3. 在"限流配置"区域：启用限流选择"是"
4. 点击"添加规则"按钮，添加一条 RPM 规则
5. 填写规则名称、适用模型、时间窗口=1
6. **不填写最大请求数**，保持为空
7. 点击"提交"按钮
8. 观察校验提示

#### 预期结果

- 步骤7-8：表单校验失败，抽屉不关闭，最大请求数字段显示：**第{n}条RPM规则最大请求数不能为空**

#### 测试数据

- RPM 规则名称：rpm_rule_request_required
- 时间窗口(分)：1
- 最大请求数：留空

---

### 2.43 创建Entity-名称格式校验

**功能模块**: Entity组织管理  
**优先级**: P1  
**用例编号**: EM-E-43  
**自动化**: `test_09_entity_name_and_rate_limit_duplicate.spec.js`

#### 测试步骤

1. 点击「创建Entity」
2. 名称分别尝试：
   - 前后空白：` entity1 `
   - 65 字符
   - 含控制字符
   - 含 `@`：`user@project`（合法）
   - 首尾 `@`：`@user`、`user@`（非法）
   - 合法：`entity_{ts}`（1–64 字符，trim 后无前后空白）
3. 选择类型后提交

#### 预期结果

- 非法样本被拦截（名称格式/长度/空白/控制字符错误，以 i18n 为准）；`@user`、`user@` 拦截
- `user@project` 与 `entity_{ts}` 可提交

---

### 2.44 创建Entity-TPM组合重复校验

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-44  
**自动化**: `test_09_entity_name_and_rate_limit_duplicate.spec.js`

#### 测试步骤

1. 点击「创建Entity」，填写名称、选择类型
2. 启用限流，添加 **两条 TPM 规则**，配置完全相同的 `(model, window_minutes, max_tokens, step_minutes)`
3. 点击「提交」

#### 预期结果

- 表单校验失败，提示 **TPM 组合重复**（以 i18n `entity.tpmCombinationDuplicate` 为准）

---

### 2.45 创建Entity-TPM规则名称长度校验

**功能模块**: Entity组织管理  
**优先级**: P1  
**用例编号**: EM-E-45  
**自动化**: `test_09_entity_name_and_rate_limit_duplicate.spec.js`

#### 测试步骤

1. 启用限流，添加 TPM 规则
2. 规则名称分别尝试：**129 字符**、**1 字符合法名**
3. 点击「提交」

#### 预期结果

- 超过 128 字符被拦截（`entity.ruleNameLengthError`）
- 1–128 字符可通过名称长度校验

---

### 2.46 创建Entity-RPM组合重复校验

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-46  
**自动化**: `test_09_entity_name_and_rate_limit_duplicate.spec.js`

#### 测试步骤

1. 启用限流，添加 **两条 RPM 规则**，配置完全相同的 `(model, window_minutes, max_requests)`
2. 点击「提交」

#### 预期结果

- 表单校验失败，提示 **RPM 组合重复**（以 i18n `entity.rpmCombinationDuplicate` 为准）

#### 补充说明

- TPM/RPM 的 `max_tokens` / `max_requests` 允许 **0**（边界见 EM-E-31/32/36/37）

---

### 2.47 创建Entity-unit=RMB-配额小数校验

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-47

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 点击"创建Entity"按钮
2. 填写名称、选择类型
3. 在"配额信息"区域：无限配额选择"否"
4. 配额单位选择 **RMB**
5. 在"配额总量"输入框中输入小数值（如 `100.56`）
6. 点击"提交"按钮

#### 预期结果

- 表单校验通过，创建成功
- RMB 单位下配额总量允许为小数，**最多 4 位小数**（5 位及以上小数会被 UI 截断或拦截，见 EM-E-48）
- 不出现后端 JSON 解析类错误

#### 测试数据

- 配额单位：`RMB`
- 配额总量：`100.56`

---

### 2.48 创建Entity-unit=RMB-配额4位小数边界

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-48

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 点击"创建Entity"按钮
2. 填写名称、选择类型
3. 在"配额信息"区域：无限配额选择"否"
4. 配额单位选择 **RMB**
5. 先输入 4 位小数（如 `100.1234`），点击"提交"
6. 观察创建结果
7. 重新打开创建表单，输入 5 位小数（如 `100.12345`）
8. 观察输入框与校验提示

#### 预期结果

- 步骤 5-6：4 位小数合法，提交成功
- 步骤 7-8：输入第 5 位小数时被 UI 自动截断（InputNumber 精度限制）或触发校验提示「配额总量小数位数不能超过4位」
- 5 位小数无法成功提交

#### 测试数据

- 4 位小数：`100.1234`（合法）
- 5 位小数：`100.12345`（非法，应被拦截）

---

### 2.49 创建Entity-unit=total_token-配额必须整数

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-49  
**说明**: 本用例验证 unit=total_token 时配额必须为整数的规则在支持 RMB 后仍然成立，防止单位切换导致校验丢失。

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 点击"创建Entity"按钮
2. 填写名称、选择类型
3. 在"配额信息"区域：无限配额选择"否"
4. 配额单位选择 **total_token**
5. 在"配额总量"尝试输入小数值（如 `5.3`）
6. （自动化）因 InputNumber `:precision="0"` **界面无法输入小数**，通过写模型触发校验后确认 tip

#### 预期结果

- **产品 UI**：配额总量 InputNumber 精度为 0，手工无法稳定输入小数（会被截断/拒绝）
- **校验文案**（字段校验路径）：**配额总量必须为非负整数**
- 不出现后端 JSON 解析类错误

#### 测试数据

- 配额单位：`total_token`
- 配额总量：`5.3`（自动化经 Vue 模型注入；UI 无法直接键入）

---

### 2.50 Entity详情-RMB单位配额展示

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-50

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已创建至少一个 Entity 类型

#### 测试步骤

1. 通过接口创建一个 unit=RMB 的 Entity，配额总量 `100.12345678`（内部 8 位小数）
2. 在列表中点击该 Entity 名称，打开详情抽屉
3. 检查"配额信息"区域的展示格式：
   - 配额总量
   - 已使用
   - 剩余量
   - 单位

#### 预期结果

- 配额总量显示为 `¥100.1235`（4 位小数，按四舍五入）
- 已使用 / 剩余量同样按 4 位小数展示，前缀为 `¥`
- 单位显示为 `RMB`
- 进度条百分比与 used / quota 比例一致

#### 测试数据

- 配额单位：`RMB`
- 配额总量：`100.12345678`（内部 8 位，展示 4 位）
- 已使用：`50.12345678`
- 剩余量：`50.00000000`

---

### 2.51 Entity列表-RMB单位配额用量展示

**功能模块**: Entity组织管理  
**优先级**: P1  
**用例编号**: EM-E-51

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已创建至少一个 Entity 类型

#### 测试步骤

1. 通过接口创建两个 Entity：
   - Entity A：unit=`total_token`，配额 `100000`
   - Entity B：unit=`RMB`，配额 `100.12345678`
2. 刷新列表，找到这两个 Entity 行
3. 检查"配额计划"列或"已使用"列的展示

#### 预期结果

- Entity A 显示为 `{used} tokens / {quota} tokens` 格式（total_token 单位；列表数值 ≥1000 时缩写为 K/M，如 100000 → `100.0K tokens`，与 API Key 列表一致）
- Entity B 显示为 `¥xxx` 格式（RMB 单位，4 位小数，不做 K/M 缩写）
- 不同 unit 的 Entity 在列表中按各自单位动态展示，不混用

#### 测试数据

- Entity A：unit=total_token，quota=100000
- Entity B：unit=RMB，quota=100.12345678

---

### 2.52 创建Entity-unit=RMB-配额总量9000万上界校验

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-52

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 点击"创建Entity"按钮
2. 填写名称、选择类型
3. 在"配额信息"区域：无限配额选择"否"，配额单位选择 **RMB**
4. 在"配额总量"写入超过 9000 万元的值（如 `90000001`，InputNumber `:max` 会钳制界面输入，自动化经 Vue 模型注入）
5. 点击"提交"按钮

#### 预期结果

- 表单校验失败，抽屉不关闭
- 配额总量字段显示：**RMB 配额不能超过 9000 万元**
- 不出现后端 JSON 解析类错误

#### 测试数据

- 配额单位：`RMB`
- 配额总量：`90000001`（超过 RMB_QUOTA_MAX=90000000）

---

### 2.53 创建Entity-unit=RMB-配额总量9000万边界值合法

**功能模块**: Entity组织管理  
**优先级**: P0  
**用例编号**: EM-E-53

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 点击"创建Entity"按钮
2. 填写名称、选择类型
3. 在"配额信息"区域：无限配额选择"否"，配额单位选择 **RMB**
4. 在"配额总量"输入 `90000000.00`（9000 万元上限边界值）
5. 点击"提交"按钮

#### 预期结果

- 表单校验通过，创建成功
- 9000 万元边界值（90000000.00）为合法值，不触发上限校验

#### 测试数据

- 配额单位：`RMB`
- 配额总量：`90000000.00`

---

### 2.54 重置配额弹窗-unit=RMB-9000万上界校验

**功能模块**: Entity组织管理  
**优先级**: P1  
**用例编号**: EM-E-54

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已创建 unit=RMB 的 Entity（配额总量如 `100.00`）

#### 测试步骤

1. 打开该 Entity 详情，点击"重置配额"按钮
2. 确认弹窗单位显示为 RMB
3. 在"重置后配额总量"输入超过 9000 万元的值（如 `90000001`）
4. 点击"确认"按钮

#### 预期结果

- 重置失败，提示 **RMB 配额不能超过 9000 万元**
- 输入 `90000000.00` 时可正常提交（边界合法）

#### 测试数据

- 重置后配额：`90000001`（非法）、`90000000.00`（合法）

---

### 2.55 编辑Entity-unit=RMB-配额总量9000万上界校验

**功能模块**: Entity组织管理  
**优先级**: P1  
**用例编号**: EM-E-55  
**说明**: Upsert 组件创建/编辑共用，`unit=RMB` 时 `max=90000000` 的校验在编辑表单同样生效。

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已创建 unit=RMB 的 Entity（配额总量如 `100.00`）

#### 测试步骤

1. 点击该 Entity 行的"编辑"按钮，打开编辑表单
2. 确认配额单位显示为 RMB
3. 在"配额总量"输入超过 9000 万元的值（如 `90000001`，InputNumber `:max` 会钳制界面输入，自动化经 Vue 模型注入）
4. 点击"提交"按钮
5. 修改为边界值 `90000000.00` 后再次提交

#### 预期结果

- 步骤 4：表单校验失败，抽屉不关闭，配额总量字段显示：**RMB 配额不能超过 9000 万元**
- 步骤 5：边界值 `90000000.00` 校验通过，保存成功，编辑后 GET 读回 quota 与提交值一致
- 不出现后端 JSON 解析类错误

#### 测试数据

- 配额单位：`RMB`
- 配额总量：`90000001`（非法）、`90000000.00`（合法边界）

---

### 2.56 重置配额弹窗-total_token 上界校验

**功能模块**: Entity组织管理  
**优先级**: P1  
**用例编号**: EM-E-56  
**说明**: 重置配额弹窗走 View 组件，与创建 Upsert 不是同一路径；`unit=total_token` 时上限同样为 `DECIMAL(18,8)` 整数上限 `9999999999`。

#### 前置条件

- 已登录系统并进入 Entity 组织管理页面
- 已创建 unit=total_token 的 Entity（配额总量如 `1000`）

#### 测试步骤

1. 打开该 Entity 详情，点击"重置配额"按钮
2. 在"重置后配额总量"写入超过 `9999999999` 的值（如 `10000000000`，InputNumber `:max` 会钳制界面输入，自动化经 Vue 模型注入）
3. 点击"确认"按钮
4. 修改为边界值 `9999999999` 后再次提交

#### 预期结果

- 步骤 3：重置失败，Toast 提示 **配额总量超出允许范围**，弹窗不关闭
- 步骤 4：边界值 `9999999999` 可正常提交，弹窗关闭
- 不出现后端数据库异常

#### 测试数据

- 配额单位：`total_token`
- 重置后配额：`10000000000`（非法）、`9999999999`（合法边界）

---
