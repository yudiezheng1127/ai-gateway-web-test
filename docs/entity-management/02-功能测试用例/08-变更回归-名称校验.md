[← 返回索引](./00-索引与映射.md)

## 7. 变更回归：名称校验（2026-08-24 / 08-25 / 2026-09-05）

> 变更背景：
>
> 1. **Entity 名称（EntityName）**：长度 1–64；仅允许小写字母、数字、`_`、`-`、`@`（支持 `用户名@项目名`）；不能以 `-`、`_`、`@` 开头或结尾；不能包含空白字符；全局唯一。
> 2. **限流规则（TPM / RPM）`name` 校验变更**：长度 1–128；字符集限制为 `[a-zA-Z0-9_-]`；同一 `RateLimitPolicy` 内不能重复；**创建后不可修改**（编辑时名称禁用）。
>
> 接口依据：`00-common.md` §17 EntityName、§10 TPMConfig、§11 RPMConfig；`entities.md` §2.1 创建约束。  
> 2026-09-05：EntityName 字符集放开 `@`（web `modifications/2026-09-05-entity-name-allow-at`）。

### EM-CV-01：Entity 名称-仅允许小写字母

**功能模块**: 名称校验  
**优先级**: P0  
**用例编号**: EM-CV-01

#### 前置条件

- 打开创建 Entity 抽屉

#### 测试步骤

依次输入以下名称并触发校验：

1. `ops` → 预期通过
2. `ops_team`、`ops-team`、`ops2024` → 预期通过
3. `Ops`、`OpsTeam`、`OPS` → 预期**拦截**（不允许大写字母）
4. `Ops Team` → 预期拦截（含空白字符）

#### 预期结果

1. 名称仅允许小写字母、数字、`_`、`-`、`@`；输入含大写字母时校验拦截，提示「名称须为小写字母、数字、下划线、连字符或 @（如 user@project），且不能以 _、- 或 @ 开头/结尾」
2. 合法名称提交成功，`POST /entities` 返回 200

---

### EM-CV-02：Entity 名称-首尾字符限制

**功能模块**: 名称校验  
**优先级**: P0  
**用例编号**: EM-CV-02

#### 前置条件

- 打开创建 Entity 抽屉

#### 测试步骤

依次输入以下名称并触发校验：

1. `ops-team` → 预期通过
2. `-ops`、`_ops`、`@ops` → 预期拦截（不能以 `-` / `_` / `@` 开头）
3. `ops-`、`ops_`、`ops@` → 预期拦截（不能以 `-` / `_` / `@` 结尾）

#### 预期结果

- 不能以 `-`、`_`、`@` 开头或结尾；违反时拦截，提示「名称须为小写字母、数字、下划线、连字符或 @（如 user@project），且不能以 _、- 或 @ 开头/结尾」

---

### EM-CV-03：Entity 名称-长度边界

**功能模块**: 名称校验  
**优先级**: P1  
**用例编号**: EM-CV-03

#### 前置条件

- 打开创建 Entity 抽屉

#### 测试步骤

1. 输入 65 个字符的名称
2. 输入 64 个字符的名称
3. 留空

#### 预期结果

- 65 字符拦截（长度须 1–64）；64 字符通过
- 留空拦截（必填）

---

### EM-CV-04：Entity 名称-全局唯一

**功能模块**: 名称校验  
**优先级**: P0  
**用例编号**: EM-CV-04

#### 前置条件

- 已存在名为 `ops` 的 Entity

#### 测试步骤

1. 创建 Entity，名称填 `ops`
2. 提交

#### 预期结果

- 后端拦截（`555` 创建重复对象），提示名称已存在；抽屉不关闭，可修改名称后重试

---

### EM-CV-05：限流规则 name-字符集校验

**功能模块**: 名称校验  
**优先级**: P0  
**用例编号**: EM-CV-05

#### 前置条件

- 打开创建 / 编辑 Entity 或 API-Key，启用限流策略并添加 TPM / RPM 规则

#### 测试步骤

依次输入以下规则名称并触发校验：

1. `tpm_1min`、`Tpm-1min`、`rpm_2024` → 预期通过（`[a-zA-Z0-9_-]`）
2. `tpm 1min`、`tpm.1min`、`tpm@1`、`tpm中文` → 预期**拦截**（仅允许 `[a-zA-Z0-9_-]`）

#### 预期结果

- 规则名称字符集限制为 `[a-zA-Z0-9_-]`；含空格 / `.` / `@` / 中文等字符拦截并提示
- 与 `00-common.md` §10/§11 一致

---

### EM-CV-06：限流规则 name-长度边界（1–128）

**功能模块**: 名称校验  
**优先级**: P1  
**用例编号**: EM-CV-06

#### 前置条件

- 打开创建 / 编辑 Entity 或 API-Key，添加 TPM / RPM 规则

#### 测试步骤

1. 规则名称输入 129 字符
2. 输入 128 字符（合法字符集内）
3. 留空

#### 预期结果

- 129 字符拦截（长度须 1–128）；128 字符通过
- 留空拦截（必填、非空）

---

### EM-CV-07：限流规则 name-同策略内唯一

**功能模块**: 名称校验  
**优先级**: P0  
**用例编号**: EM-CV-07

#### 前置条件

- 打开创建 / 编辑 Entity 或 API-Key，已添加一条 TPM 规则 `tpm_1min`

#### 测试步骤

1. 再添加一条 TPM 规则，名称同样填 `tpm_1min`
2. 提交

#### 预期结果

- 同一 `RateLimitPolicy` 内规则名称不能重复；重复时拦截并提示（TPM 内与 RPM 内分别唯一）

---

### EM-CV-08：限流规则 name-创建后不可修改

**功能模块**: 名称校验  
**优先级**: P1  
**用例编号**: EM-CV-08

#### 前置条件

- 存在一条已创建、含限流规则（TPM / RPM）的 Entity 或 API-Key 记录

#### 测试步骤

1. 编辑该记录，进入限流策略配置
2. 尝试修改已有规则的名称字段

#### 预期结果

1. 已有规则的名称字段**只读 / 禁用**（创建后不可修改）
2. 可修改规则的其他字段（model、window_minutes、max_tokens/max_requests、step_minutes）
3. 新增规则的名称可输入

---

### EM-CV-09：Entity 名称-允许中间出现@

**功能模块**: 名称校验  
**优先级**: P0  
**用例编号**: EM-CV-09  
**设计依据**: `00-common.md` §17 EntityName；原型 `entity-upsert.js`  
**OpenAPI**: `POST /entities` `name`

#### 前置条件

- 打开创建 Entity 抽屉
- 已存在至少一个 Entity 类型

#### 测试步骤

1. 确认名称 placeholder 为 `user@project`，form-tip 含「支持 用户名@项目名」且「不能以 _、- 或 @ 开头/结尾」
2. 名称输入 `alice@default`，选择类型后提交
3. 再次打开创建抽屉，名称输入 `user@project`，选择类型后提交

#### 预期结果

1. tip / placeholder 与原型一致
2. `alice@default`、`user@project` 前端校验通过，创建成功，列表可见该名称
3. `POST /entities` 返回 200

---

*文档版本: v0.2*  
*最后更新: 2026-09-05（EntityName 放开 @）*
