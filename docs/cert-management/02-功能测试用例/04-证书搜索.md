[← 返回索引](./00-索引与映射.md)

## 4. 证书搜索

> **归属场景**: 场景四（管理员搜索证书）

### 4.1 按证书名称搜索

**用例编号**: CM-C-18

#### 测试步骤

1. 在「请输入证书名称查询」输入框输入关键词
2. 观察列表过滤结果

#### 预期结果

- 列表实时过滤，仅显示名称匹配的证书

---

### 4.2 按过期时间搜索

**用例编号**: CM-C-19

#### 测试步骤

1. 在「请输入过期时间查询」输入框输入日期片段（如 `2033-10`）

#### 预期结果

- 列表过滤显示过期时间匹配的结果
