# 模型定价（Model Prices）模块 - 测试场景概览

## 模块概述

模型定价管理模块负责维护 AI 网关中各个 Provider / 模型的定价信息，是计费、用量统计、成本分析的基础配置数据。对应 API 端点：`/model-prices`。

| 功能区域 | 说明 |
| --- | --- |
| 模型定价列表 | 分页展示所有模型定价记录，支持按 provider、mode 筛选，按 model 搜索，支持分页 |
| 单条 CRUD | 新增、编辑、删除单条模型定价记录，字段含 provider（可下拉选择 / 手写的自动补全框）/model/base_model/mode/prices 等 |
| 详情页 | 点击某条记录查看全部字段详情，由「基本信息 / 限制对象 / 价格 / 元数据」四个 Card 组成；创建/更新时间位于「基本信息」Card 底部 |
| YAML 导入 | 支持通过 YAML 文件批量导入模型定价，支持 replace（全量替换，默认）与 merge（增量合并）两种模式 |
| 分时段价格 | 价格 Card 双配置块：默认价格（prices，必填）+ 分时段价格（tier_prices.peak，可选）；未命中 tier 时 fallback 默认价格 |
| 字段校验 | 必填、长度、枚举、非负、唯一性等完整校验矩阵 |

记录唯一性约束：`(provider, model, mode)` 三元组必须唯一。

---

## 配置顺序 / 依赖关系

模型定价为**独立数据模块**，与模型服务商**不强关联**：不要求先创建服务商，也不阻塞集群配置。`provider` 字段通常填服务商名以便计费归集，但仅为软关联。真正的强依赖链路是：**先创建模型服务商 → 集群「大模型配置」才能引用该服务商**（模型定价不在这条必选链路上）。

```text
（主链路 · 强依赖）
① 配置模型服务商（Providers） → ② 集群大模型配置引用服务商（Clusters） → ③ 路由规则 / 全链路

（独立模块 · 软关联）
模型定价（Model Prices）    为 provider / 模型维护默认价格 + 分时段价格，供计费 / 报表 / 成本分析；
                            provider 通常填服务商名（可选，不强校验）；删除服务商不级联删除定价
```

**依赖关系说明**：

| 关系 | 说明 |
| --- | --- |
| Model Prices 与 Providers（软关联，非强依赖） | 定价 `provider` 字段通常填服务商名（非必填）；服务商列表「查询模型价格」直达本模块并按提供商筛选；删除服务商**不级联删除**定价记录 |
| Model Prices 与 Clusters（数据对应，非配置依赖） | 集群转发模型与服务商模型一致时，定价记录用于该模型的计费；定价缺失不影响集群配置 |
| 下游 | 计费 / 报表 / 成本分析依赖本模块价格数据准确性 |

- **可独立使用**：不强制要求服务商存在，可直接创建 / 编辑 / 删除定价记录（YAML 导入可独立初始化）
- **推荐流程**：先建服务商 → 再配置定价（保证 provider 名称一致，便于计费归集），但**不阻塞**集群配置

---

## 场景测试设计

本模块从管理员真实操作目标出发组织用例，共定义 7 个核心场景。单页 CRUD 与字段校验详见 [02-功能测试用例](./02-功能测试用例/00-索引与映射.md)。

### 场景一：管理员查看模型定价列表

**场景描述**：管理员进入模型定价列表页，查看所有模型定价记录，使用 provider 筛选、model 搜索、mode 筛选、组合筛选、分页浏览，并在空数据状态下查看空态展示。

**涉及测试场景**：

| 步骤 | 操作 | 对应编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | 进入模型定价列表页，查看表格加载与分页栏 | MP-L-01 | P1 |
| 2 | 按 provider 筛选 | MP-L-02 | P1 |
| 3 | 按 model 名称搜索 | MP-L-03 | P1 |
| 4 | 按 mode 筛选 | MP-L-04 | P1 |
| 5 | provider + mode 组合筛选 | MP-L-05 | P1 |
| 6 | 分页切换（上一页/下一页/页码跳转） | MP-L-06 | P1 |
| 7 | 空列表展示 | MP-L-07 | P2 |

**测试目标**：验证列表页数据加载、筛选、搜索、分页操作正确，空状态展示符合预期。

---

### 场景二：管理员创建 / 编辑 / 删除模型定价

**场景描述**：管理员在列表页点击「新增」创建模型定价记录，或对已有记录执行编辑、删除操作；删除时有二次确认，取消则不删除。

**涉及测试场景**：

| 步骤 | 操作 | 对应编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | 新增模型定价（所有必填项合法）→ 成功 | MP-C-01 | P0 |
| 2 | 编辑模型定价 → 成功 | MP-C-02 | P0 |
| 3 | 删除模型定价（确认）→ 成功 | MP-C-03 | P0 |
| 4 | 删除模型定价（取消）→ 不删除 | MP-C-04 | P1 |

**测试目标**：验证 CRUD 核心操作的完整流程，确认提交后列表数据与接口返回一致。

---

### 场景三：字段校验

**场景描述**：管理员在新增 / 编辑表单中填写各字段，验证前端与后端的校验规则，包括必填、长度、枚举、非负、唯一性等。

**涉及测试场景**：

| 步骤 | 操作 | 对应编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | 必填字段校验（provider / model / base_model / mode / prices） | MP-V-01 | P0 |
| 2 | 字符串长度边界（1–255 字符） | MP-V-02 | P1 |
| 3 | mode 枚举值校验（13 种合法值） | MP-V-03 | P0 |
| 4 | prices 至少包含一个价格字段 | MP-V-04 | P0 |
| 5 | prices 字段值非负校验 | MP-V-05 | P0 |
| 6 | limits 字段值为非负整数 | MP-V-06 | P1 |
| 7 | (provider, model, mode) 唯一性校验 | MP-V-07 | P0 |
| 8 | capabilities 多选标签（添加/删除/重复） | MP-V-08 | P1 |
| 9 | supported_parameters 多选标签 | MP-V-09 | P1 |
| 10 | metadata.source URL 格式校验 | MP-V-10 | P2 |

**测试目标**：验证所有字段的前端校验与后端校验行为正确，错误提示清晰明确，与接口定义一致。

---

### 场景四：详情页全字段一致性

**场景描述**：管理员从列表页点击某条记录进入详情页，查看所有字段展示，并与接口返回数据逐一比对，确保展示值与接口数据完全一致。

**涉及测试场景**：

| 步骤 | 操作 | 对应编号 | 优先级 |
|---|---|---|---|
| 1 | 详情页全字段与接口数据一致性 | MP-D-01 | P0 |

**测试目标**：验证详情页所有字段（id、provider、model、base_model、mode、capabilities、supported_parameters、limits、prices、price_currency、metadata、create_time、update_time）的展示值与 GET /model-prices/{id} 接口返回值完全一致；其中创建/更新时间位于「基本信息」Card 底部，不单独成 Card。

---

### 场景五：YAML 导入

**场景描述**：管理员通过 YAML 批量导入模型定价，支持粘贴 YAML 内容预览、校验失败提示、replace / merge 两种模式导入、导入取消等场景。

**涉及测试场景**：

| 步骤 | 操作 | 对应编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | YAML 解析预览（粘贴有效 YAML，预览正确） | MP-I-01 | P1 |
| 2 | YAML 校验失败提示（格式错误 / 必填缺失等） | MP-I-02 | P0 |
| 3 | YAML 批量导入成功（replace 模式） | MP-I-03 | P0 |
| 4 | YAML 导入取消 / 关闭 | MP-I-04 | P1 |
| 5 | merge 模式导入（增量合并） | MP-I-05 | P0 |
| 6 | 重复记录导入处理（replace / merge 模式差异） | MP-I-06 | P0 |

**测试目标**：验证 YAML 导入的完整流程，包括解析预览、校验失败、两种模式下的导入行为与结果，导入后列表数据与接口返回一致。

---

### 场景六：分时段价格配置

**场景描述**：管理员在新增 / 编辑模型定价时，在「价格」Card 中分别填写**默认价格**（`prices`，必填）与**分时段价格**（`tier_prices.peak`，可选）；提交后详情页按「默认价格 + 分时段价格」分组展示；`tier_prices` 覆盖新增的图像 / 音频 / 视频价格键。

**涉及测试场景**：

| 步骤 | 操作 | 对应编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | 价格 Card 双配置块（默认价格 + 分时段价格） | MP-T-01 | P0 |
| 2 | 分时段价格 `?` Tooltip（fallback 语义） | MP-T-02 | P1 |
| 3 | 时段对象只读「忙时」Tag | MP-T-03 | P1 |
| 4 | 分时段价格-动态列表添加 / 删除 | MP-T-04 | P1 |
| 5 | 分时段价格-可选（未命中 tier 时 fallback） | MP-T-05 | P0 |
| 6 | 分时段价格字段校验（非负 / 键名枚举） | MP-T-06 | P0 |
| 7 | 提交 tier_prices 结构 | MP-T-07 | P0 |
| 8 | 详情页分时段价格展示 | MP-T-08 | P0 |
| 9 | 编辑回显 tier_prices | MP-T-09 | P1 |
| 10 | prices 键名枚举-新增 4 键 | MP-T-10 | P1 |

**测试目标**：验证分时段价格（tier_prices）的完整配置流程、fallback 语义、提交体结构与详情回显，与 `model-prices.md` 接口定义一致。

---

### 场景七：为服务商模型配置定价（跨模块，软关联）

**场景描述**：管理员按真实操作习惯，**为已创建的模型服务商**的模型配置模型定价（provider 关联服务商名，含默认价格与分时段价格），再从服务商列表直达定价列表并按该服务商筛选（无记录时提示未找到提供商）；主链路上集群「大模型配置」引用服务商（先建服务商 → 再配集群），验证定价记录与集群转发模型的数据对应关系，以及服务商删除后定价记录的保留行为。定价为独立数据模块，**不强关联服务商**，本场景仅验证真实流程下的数据对应与软关联行为。

| 步骤 | 操作 | 对应编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | 前置：已创建模型服务商（含探测模型） | PR-C-01 | P0 |
| 2 | 新增定价：provider 填服务商名，model 填服务商模型，配置默认价格 | MP-LINK-01 | P0 |
| 3 | 为同一模型配置分时段价格（忙时 peak） | MP-T-01 / MP-T-07 | P0 |
| 4 | 从服务商列表「查询模型价格」直达并按该服务商筛选 | MP-LINK-02 / PR-L-11 | P0 |
| 5 | （主链路）集群「大模型配置」引用该服务商，转发模型选择该模型（数据对应） | MP-LINK-03 / RM-BC-71 | P1 |
| 6 | 删除服务商后，该模型定价记录保留（计费数据不丢失） | MP-LINK-04 / PR-LINK-08 | P1 |

**测试目标**：验证模型定价的 provider 软关联、直达跳转、与集群转发模型的数据对应，及删除服务商后的数据保留；确认定价**不阻塞**集群配置（主链路为 服务商 → 集群），与 `model-prices.md` / `providers.md` 接口一致。

---

## 测试场景统计

### 按功能区域

| 功能区域 | 测试场景数量 | 优先级分布 |
| --- | --- | --- |
| 列表页 | 7 个 | P0: 0, P1: 6, P2: 1 |
| CRUD | 4 个 | P0: 3, P1: 1 |
| 字段校验 | 10 个 | P0: 5, P1: 4, P2: 1 |
| 详情页 | 1 个 | P0: 1 |
| YAML 导入 | 6 个 | P0: 4, P1: 2 |
| 分时段价格 | 10 个 | P0: 5, P1: 5 |
| 跨模块联动（服务商→集群，定价软关联） | 4 个 | P0: 2, P1: 2 |
| **总计** | **42 个** | **P0: 20, P1: 20, P2: 2** |

### 按使用场景

| 使用场景 | 涉及场景编号 | 说明 |
| --- | --- | --- |
| 场景一：管理员查看模型定价列表 | MP-L-01 ~ MP-L-07 | 列表展示、筛选、搜索、分页、空列表 |
| 场景二：管理员创建 / 编辑 / 删除模型定价 | MP-C-01 ~ MP-C-04 | 新增、编辑、删除（确认 / 取消） |
| 场景三：字段校验 | MP-V-01 ~ MP-V-10 | 必填、长度、枚举、非负、唯一性等 |
| 场景四：详情页全字段一致性 | MP-D-01 | 详情页全字段与接口数据一致 |
| 场景五：YAML 导入 | MP-I-01 ~ MP-I-06 | 解析预览、校验、replace / merge、取消 |
| 场景六：分时段价格配置 | MP-T-01 ~ MP-T-10 | 价格双块、fallback、提交结构、详情回显、4 新键 |
| 场景七：为服务商模型配置定价（跨模块，软关联） | MP-LINK-01 ~ MP-LINK-04 | provider 软关联、直达跳转、与集群转发模型对应、删除保留 |

---

## 测试用例编号规则

| 前缀 | 含义 | 示例 |
| --- | --- | --- |
| MP-L-xx | 列表页（List） | MP-L-01 ~ MP-L-07 |
| MP-C-xx | 增删改（Create / Edit / Delete） | MP-C-01 ~ MP-C-04 |
| MP-V-xx | 字段校验（Validation） | MP-V-01 ~ MP-V-10 |
| MP-D-xx | 详情页（Detail） | MP-D-01 |
| MP-I-xx | YAML 导入（Import） | MP-I-01 ~ MP-I-06 |
| MP-T-xx | 分时段价格（Tier） | MP-T-01 ~ MP-T-10 |
| MP-LINK-xx | 跨模块联动（Linkage） | MP-LINK-01 ~ MP-LINK-04 |

> 说明：模型定价模块前缀使用 `MP`（Model Prices）。

---

## 优先级定义

| 优先级 | 级别 | 说明 |
| --- | --- | --- |
| P0 | Critical | 核心配置路径、CRUD 主流程、必填校验、唯一性、详情一致性、YAML 导入主流程 |
| P1 | High | 搜索筛选分页、编辑取消、长度边界、多选标签、导入预览与取消 |
| P2 | Medium | 空列表、URL 格式等边界 / 次要校验 |

---

## 测试前置条件

| 测试场景 | 前置条件 |
| --- | --- |
| MP-L-02 ~ MP-L-06 筛选 / 搜索 / 分页 | 列表中存在足够数量的模型定价记录（覆盖不同 provider / mode） |
| MP-L-07 空列表 | 列表中无任何模型定价记录（需先清空） |
| MP-C-01 创建成功 | 不存在待创建的 (provider, model, mode) 组合 |
| MP-C-02 编辑成功 | 存在可编辑的模型定价记录 |
| MP-C-03 / 04 删除 | 存在可删除的模型定价记录 |
| MP-V-07 唯一性校验 | 已存在一条 (provider=A, model=B, mode=chat) 记录 |
| MP-D-01 详情页一致性 | 存在一条字段完整的模型定价记录（含 capabilities / limits / metadata 等） |
| MP-I-03 / 05 / 06 YAML 导入 | 已准备好合法的 YAML 内容（含多条记录） |
| MP-T-01 ~ MP-T-10 分时段价格 | 至少存在 1 个 provider；含 `tier_prices.peak` 的定价记录用于回显 / 详情验证 |
| MP-LINK-01 ~ MP-LINK-04 跨模块联动 | 已创建模型服务商（含探测模型）；存在引用该服务商的集群 |

---

## 测试数据准备

| 数据类别 | 准备内容 | 用途 |
| --- | --- | --- |
| 基础记录 | provider=openai, model=gpt-4, base_model=gpt-4, mode=chat, prices={input_cost_per_token: 0.00003, output_cost_per_token: 0.00006} | 列表展示、详情查看、编辑、删除 |
| 多 provider 记录 | provider=anthropic / azure / minimax 等各 1–2 条 | provider 筛选测试 |
| 多 mode 记录 | mode=chat / embedding / image_generation 等各 1–2 条 | mode 筛选测试 |
| 完整字段记录 | 含 capabilities、supported_parameters、limits、metadata 全部字段 | 详情页全字段一致性验证 |
| YAML 数据 | 含 version、default_currency、models 数组，至少 3 条记录 | YAML 导入测试 |
| YAML 错误数据 | 格式错误 / 必填缺失 / 重复三元组 | YAML 校验失败测试 |
| 分时段价格数据 | provider=deepseek, model=deepseek-v3, prices 若干 + tier_prices.peak 若干 | 分时段价格提交 / 回显 / 详情验证 |
| 联动数据 | 已创建服务商 deepseek（含模型 deepseek-v3）+ 引用 deepseek 的集群 | 跨模块联动：provider 关联 / 集群转发模型对应 / 删除保留 |

| 数据项 | 说明 | 示例值 |
| --- | --- | --- |
| provider | Provider / Cluster 标识，1–255 字符；新增/编辑表单中为可下拉选择 / 手写的自动补全框（候选来自 get-provider-names） | openai |
| model | 模型名，1–255 字符 | gpt-4 |
| base_model | 归一化模型名，1–255 字符 | gpt-4 |
| mode | 枚举值（13 种） | chat |
| capabilities | string 数组，21 种枚举 | ["chat", "vision", "tools"] |
| supported_parameters | string 数组，15 种枚举 | ["temperature", "max_tokens"] |
| limits | object，键为 context_window 等 | {context_window: 8192, max_output_tokens: 4096} |
| prices | object，至少一个价格字段，非负数 | {input_cost_per_token: 0.00003} |
| price_currency | 固定 RMB | RMB |
| metadata | object，键为 source / notes | {source: "<https://openai.com/pricing>", notes: "官方定价"} |

---

## 自动化实施建议

| 阶段 | 范围 | 说明 |
| --- | --- | --- |
| 第一期 | P0 主路径 | 列表加载、CRUD 主流程、必填校验、唯一性校验、详情页一致性、YAML 导入主流程 |
| 第二期 | P1 用例 | 筛选搜索分页、删除取消、长度边界、limits 校验、多选标签、导入预览与取消 |
| 第三期 | P2 用例 | 空列表、URL 格式校验等边界场景 |

自动化文件映射规划：

| 层级 | 文件（规划） | 用例范围 | 说明 |
| --- | --- | --- | --- |
| 功能回归 | test_01_model_price_list.spec.js | MP-L-01 ~ MP-L-07 | 列表页筛选 / 搜索 / 分页 / 空态 |
| 功能回归 | test_02_model_price_crud.spec.js | MP-C-01 ~ MP-C-04 | 新增 / 编辑 / 删除 |
| 功能回归 | test_03_model_price_validation.spec.js | MP-V-01 ~ MP-V-10 | 字段校验矩阵 |
| 功能回归 | test_04_model_price_detail.spec.js | MP-D-01 | 详情页全字段一致性 |
| 功能回归 | test_05_model_price_import.spec.js | MP-I-01 ~ MP-I-06 | YAML 导入 |
| 功能回归 | test_06_model_price_tier.spec.js | MP-T-01 ~ MP-T-10 | 分时段价格双块 / fallback / 提交 / 详情 / 4 新键 |
| 功能回归 | test_07_model_price_linkage.spec.js | MP-LINK-01 ~ MP-LINK-04 | 跨模块联动：provider 关联服务商、直达跳转、与集群转发模型对应、删除保留 |

---

## 相关文档

- [功能测试用例](./02-功能测试用例/00-索引与映射.md) — 详细步骤与预期
- [Open API 定义](./model-prices.md) — 模型定价接口定义（与本文档同目录或上级目录）
- `src/modules/ModelPrices/index.vue` — 列表页源码（规划路径）
- `src/modules/ModelPrices/components/ModelPriceForm.vue` — 新增 / 编辑表单源码（规划路径）
- `src/modules/ModelPrices/components/ModelPriceDetail.vue` — 详情页源码（规划路径）
- `src/modules/ModelPrices/components/YamlImportDialog.vue` — YAML 导入弹窗源码（规划路径）

---

*文档版本: v0.1*  
*最后更新: 2026-08-27（提供商字段改为可下拉选择 / 手写的自动补全框；详情页创建/更新时间并入「基本信息」Card 底部）*
