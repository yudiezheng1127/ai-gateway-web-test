[← 返回索引](./00-索引与映射.md)

## 2d. 大模型与校验

> 本章按**功能**组织：第 1~3 步的基础配置 / 超时 / 健康检查校验，与第 4 步「大模型配置」的字段校验。大模型配置已改为**引用模型服务商（Provider）**，`llm_config.provider` 必填、`keys[]` 仅含 `{name, weight}`（无 key 明文），并新增 `key_affinity`（Key 亲和性）。
> 校验规则以 `design-docs/api-define/OpenAPI接口定义/clusters.md` 为准。

## 基础配置与校验（第 1~3 步）

### 2.21 业务集群名称重复

**用例编号**: RM-BC-21 | **优先级**: P1  
**设计依据**: `design/business-cluster/02-step-basic-required.jpg`

#### 前置条件

- 列表中已存在业务集群 `bc_existing`

#### 测试步骤

1. 点击「添加集群」进入向导第 1 步
2. 集群名称填写 `bc_existing`
3. 其余必填项按 design 填写合法值后尝试「下一步」或最终「提交」

#### 预期结果

- 创建被拦截，提示集群名称已存在/**全局不可重复**（文案以 design 为准）
- 列表无新增重复集群

---

### 2.23 业务集群名称格式校验

**用例编号**: RM-BC-23 | **优先级**: P1  
**设计依据**: `design/business-cluster/02-step-basic-required.jpg`、`02-step-basic-no-hash-header.jpg` 等

#### 测试步骤

1. 点击「添加集群」进入向导第 1 步
2. 集群名称分别尝试：
   - 含非法字符：`bc@1`、`bc name`（空格）
   - 首尾非法：`-bc1`、`bc1.`
   - 长度：65 字符
   - 合法对照：单字符 `a`、`bc_{ts}`（1–64 字符）
3. 填写其余必填项后尝试「下一步」

#### 预期结果

- 非法名称被拦截，提示名称格式/长度错误（以 i18n `cluster.tipClusterNameRule` 为准）
- 合法名称（含单字符 `a`）可进入第 2 步

#### 格式规范（ClusterName · 对齐 `00-common.md`）

- 长度 **1–64**；字符集：字母、数字、`-`、`_`、`.`  
- 不得以 `.`、`-`、`_` 开头或结尾；不含空白；全局唯一（见 RM-BC-21）

---

### 2.26 向导第1步必填与联动校验

**用例编号**: RM-BC-26 | **优先级**: P1  
**设计依据**: `design/business-cluster/02-step-basic-required.jpg`、`02-step-basic-protocol.jpg`、`02-step-basic-no-hash-header.jpg`  
**OpenAPI**: `clusters` → `name`、`basic.protocol`、`sticky_sessions`

#### 测试步骤

1. 点击「添加集群」进入第 1 步「基础配置」
2. 分别尝试「下一步」，预期**被拦截**：
   - **集群名称留空**
   - **协议**（http/https）未选择
3. 对照**合法路径**（应可进入第 2 步）：
   - 填写合法集群名称 + 协议（默认 https）
   - **会话保持** `enabled=false`（默认）时，无需配置哈希策略/Header
   - **会话保持** `enabled=true` + 哈希策略选 **CLIENT_ID_ONLY** 或 **CLIENT_ID_PREFERED** 时，hash_header **必填**（留空被拦截）
   - 哈希策略选 **CLIENT_IP_ONLY** 时，hash_header 不要求

#### 预期结果

- 步骤 2：名称留空、协议未选 → 拦截（文案以 design 为准），停留在第 1 步
- 步骤 3：上述合法组合均可进入第 2 步；CLIENT_ID_ONLY/PREFERED 需填 hash_header
- 名称格式/重复另见 RM-BC-21、RM-BC-23

> **2026-08-05 恢复必填**：启用+CLIENT_ID_ONLY/PREFERED 时 hash_header 必填。

#### 补充说明

- OpenAPI：`max_idle_conn_per_rs` **下界 0**（可为 0）；产品上界 **99999999**
- `req_write_buffer_size` **必填且须 > 0**；默认 **512**（见 RM-BC-31）
- 健康检查 **Host** 可选；非空时须为合法 Hostname（**允许 IP**，见 RM-BC-32）

---

### 2.27 向导第2步超时重传数值校验

**用例编号**: RM-BC-27 | **优先级**: P1  
**设计依据**: `design/business-cluster/03-step-timeout-required.jpg`、`03-step-timeout-required-toast.jpg`  
**OpenAPI**: `clusters` → `basic.timeouts`、`basic.retries`

#### 测试步骤

1. 完成第 1 步合法配置后进入第 2 步
2. 分别尝试：
   - **清空全部超时字段**后点「下一步」→ 应通过（字段可选）
   - 超时字段填入 **`0`**、**负数**、非数字 → 应拦截
   - **集群内重试** `max_retry_in_cluster` 填入 `-1` 或非数字
3. 对照：各超时留空或使用 **> 0** 的整数（ms）、重试次数填 `0` 或正整数，可进入第 3 步

#### 预期结果

- 非法值被拦截，Toast 或字段级提示（以 design 为准）
- 停留在第 2 步

---

### 2.28 向导第3步健康检查字段校验

**用例编号**: RM-BC-28 | **优先级**: P1  
**设计依据**: `design/business-cluster/04-step-health-required.jpg`、`04-step-health-format.jpg`  
**OpenAPI**: `clusters` → `passive_health_check`

#### 测试步骤

1. 进入第 3 步「被动健康检查」
2. 分别尝试「下一步」：
   - **全部字段留空**（含 Host、Uri、故障阈值、间隔、期望状态码）→ 应通过
   - **Uri** 填 `interface`（不以 `/` 开头）→ 应拦截
   - **Host** 填非法域名（见 **RM-BC-32**）
   - **故障阈值/间隔** 填 `-1` 或非整数（有值时）
   - **期望状态码** 填 `99`、`600`、`-1`（见 **RM-BC-41**）
3. 对照合法值：Host 留空或 `127.0.0.1` / `www.test1.com`，Uri `/interface`，故障阈值 `3`，间隔 `1000`，期望状态码 `0` 或 `200`

#### 预期结果

- 全部留空可进入第 4 步（提交时空值补 API 默认）
- 有值但不合法时被拦截（文案以 design 为准）

---

### 2.30 向导数值字段边界值校验

**用例编号**: RM-BC-31 | **优先级**: P1  
**设计依据**: `design/business-cluster/06-step-model-validation.jpg`、`07-step-schedule-sum-error.jpg`；其余边界态 design 待补  
**OpenAPI**: `clusters` basic/sticky/health；`llm_config.keys` 权重 0~100

#### 测试步骤

按向导步骤分别验证**下界、上界、越界**（可分步 Parametrize）：

1. **步骤 1 · 单个后端最大空闲连接数 `max_idle_conn_per_rs`**：非法 `-1`、`100000000`（>99999999）；合法 `0`（下界）、`2`（用户指南默认）、`99999999`（上界）
2. **步骤 1 · 请求写缓存大小 `req_write_buffer_size`**：非法 留空/null、**`0`**；合法 `512`（默认）、`1`
3. **步骤 2 · 各超时(ms)**：非法 **`0`**、`-1`、非数字；合法 默认值或 `1`
4. **步骤 2 · 集群内重试 `max_retry_in_cluster`**：非法 `-1`、非数字；合法 `0`（下界）、`2`（默认）
5. **步骤 3 · 故障阈值 failnum**：非法 `0`、`-1`、非整数；合法 `1`（下界）、`3`（默认）
6. **步骤 3 · 健康检查间隔(ms)**：非法 `0`、`-1`；合法 `1`（下界）、`1000`
7. **步骤 3 · 期望状态码**：非法 `99`、`600`、`-1`；合法 `0`（忽略返回码）、`200`（见 RM-BC-41）

#### 预期结果

- 非法样本提交/下一步被拦截（文案以 design 为准）
- 合法边界样本通过该校验项

#### 补充说明

- 数值字段产品上界统一为 **99999999**（超时/阈值/间隔/连接数等）
- 超时与写缓存须 **> 0**（对齐新版 OpenAPI）

---

### 2.32 健康检查 Host 域名格式校验

**用例编号**: RM-BC-32 | **优先级**: P1  
**设计依据**: `design/business-cluster/04-step-health-format.jpg`  
**OpenAPI**: `passive_health_check.host` **可选**；非空时为 Hostname（允许 IPv4/IPv6）；留空时使用所属 provider 的 `instance_pool` 中第一个实例的 `addr`

#### 格式规范（RFC 1123 · 已确认）

- **总长**：**2–255** 字符（trim 后）
- **标签**：每段 1–63 字符；不得以 `-` 开头或结尾
- **IPv4/IPv6**：**允许**作为 Host 值（如 `127.0.0.1`）

#### 测试步骤

1. 进入第 3 步，其余健康检查项填合法值
2. **Host 留空**，点「下一步」→ 应通过（使用所属 provider 首个实例 addr）
3. Host 分别尝试：
   - `-bad.com`、`bad-.com`（标签首尾 `-`）
   - `###invalid.com`
4. 对照合法：`127.0.0.1`、`www.test1.com`

#### 预期结果

- Host 留空或通过合法 IP/FQDN 均可进入下一步
- 非法 FQDN 样本被拦截

---

### 2.39 集群描述长度与控制字符校验

**用例编号**: RM-BC-39 | **优先级**: P1  
**自动化**: `tests/cluster/test_05_business_cluster_field_validation.spec.js`  
**OpenAPI**: `clusters.description` 可选；长度 0–256；禁止控制字符

#### 测试步骤

1. 向导第 1 步「描述」分别尝试：257 字符、含控制字符、合法留空/256 字符
2. 点「下一步」

#### 预期结果

- 超长或含控制字符被拦截；合法文本通过

---

### 2.41 健康检查期望状态码范围

**用例编号**: RM-BC-41 | **优先级**: P1  
**自动化**: `tests/cluster/test_05_business_cluster_field_validation.spec.js`  
**OpenAPI**: `statuscode` 须为 **0** 或 **100–599**

#### 测试步骤

1. 分别填入 `0`、`200`、`99`、`600`、`-1`

#### 预期结果

- `0`、`200` 通过；`99`、`600`、`-1` 拦截

---

## 大模型配置（第 4 步）

### 2.29 向导第4步大模型必填与格式校验

**用例编号**: RM-BC-29 | **优先级**: P1  
**自动化**: 组合用例，子要求拆分覆盖：所属服务商必填 → test_06 RM-BC-71；转发模型必填 → test_06 RM-BC-73；重定向 `source_model` 必填 → test_02 RM-BC-08  
**设计依据**: `design/business-cluster/06-step-model-required.jpg`、`06-step-model-validation.jpg`  
**OpenAPI**: `clusters` `llm_config`（`provider`、`models`、`keys`、`key_policy`、`key_affinity`、`match_prefix`、`strip_prefix`）

#### 测试步骤

1. 完成前 3 步后进入第 4 步「大模型配置」
2. 分别尝试「下一步」：
   - **不选所属服务商** → 应拦截
   - **选择服务商但不选转发模型** → 应拦截
   - 已添加模型重定向行但 `source_model` 留空 → 应拦截（见 RM-BC-08）
3. 对照：选择服务商、选择转发模型、Key 合法或留空后可下一步

#### 预期结果

- **所属服务商必填**、**转发模型必填**
- 服务鉴权 Keys、Key 路由策略、Key 亲和性、模型重定向、match_prefix/strip_prefix 均为可选或有默认值
- 模型重定向字段为 `source_model`/`target_model`（见 RM-BC-08、RM-BC-40）

---

### 2.71 所属服务商-必填下拉与分步拉取

**用例编号**: RM-BC-71 | **优先级**: P0  
**OpenAPI**: `llm_config.provider`（必填，非空，必须引用 `/providers` 中已存在的 provider）

#### 前置条件

- 系统中已存在 ≥1 个服务商；进入向导第 4 步「大模型配置」

#### 测试步骤

1. 观察「所属服务商」控件
2. 点击下拉展开，观察请求
3. 不选所属服务商直接进入下一步 / 提交

#### 预期结果

1. 控件为**下拉（必填）**，标签「所属服务商」，不可手输任意值
2. 展开时调用 `GET /providers/actions/get-provider-names`，选项为全部服务商名（**分步拉取**：先拉名称列表，选中后再拉详情）
3. 未选择时提交 / 下一步被拦截，提示所属服务商必填
4. 无服务商时下拉为空并提示（引导先创建服务商）

---

### 2.72 所属服务商-切换后转发模型 / Keys 联动

**用例编号**: RM-BC-72 | **优先级**: P0  
**OpenAPI**: `llm_config.provider`、`llm_config.models`、`llm_config.keys`（均依赖 provider 详情）

#### 前置条件

- 存在 2 个服务商（models / keys 不同），进入大模型配置

#### 测试步骤

1. 选择服务商 A，待数据加载完成
2. 切换为服务商 B
3. 再切回服务商 A

#### 预期结果

1. 选中服务商后调用 `GET /providers/{name}` 拉取详情，加载其 `models`（models 列表接口配置）与 `keys`
2. 切换服务商后，**转发模型**选项与 **Keys 配置下拉**更新为新的服务商数据
3. 已选的转发模型中不属于新服务商的模型被**剔除**；Keys 表按新服务商 keys 重建（原有已选 key 名若仍存在于新服务商则保留）
4. 切换过程 loading 正确，无残留旧数据

---

### 2.73 转发模型-多选必填与「全选」

**用例编号**: RM-BC-73 | **优先级**: P0  
**OpenAPI**: `llm_config.models`（必填，至少 1 个非空元素，元素不能重复，必须是 provider `models` 的子集）

#### 前置条件

- 选择了一个含多个模型的服务商，进入大模型配置

#### 测试步骤

1. 观察「转发模型」控件
2. 点击「全选」
3. 不选任何模型提交

#### 预期结果

1. 转发模型为**多选**控件，选项为该服务商探测到的模型（**转发模型为该服务商模型的子集**）
2. 提供「**全选**」快捷操作，点击后选中该服务商全部模型
3. 转发模型**必填**，为空时提交 / 下一步被拦截

---

### 2.74 转发模型-服务商模型为空

**用例编号**: RM-BC-74 | **优先级**: P1  
**OpenAPI**: `llm_config.models`（须为 provider `models` 子集）

#### 前置条件

- 选择一个 models 为空的（未配置模型发现 / 未获取）服务商

#### 测试步骤

1. 选择 models 为空的服务商
2. 尝试选择转发模型

#### 预期结果

1. 转发模型下拉为空（展示「暂无模型」占位），无法选择
2. 提交被拦截提示「该服务商暂无可用模型」（引导服务商侧配置模型列表后返回重试）

---

### 2.75 Keys 配置-非必填与空行过滤

**用例编号**: RM-BC-75 | **优先级**: P0  
**OpenAPI**: `llm_config.keys`（非必填，默认 `[]`；元素 `{name, weight}`，name 引用 provider key 名称）

#### 前置条件

- 选择的服务商含 keys；进入大模型配置

#### 测试步骤

1. 不添加任何 Key，提交
2. 添加一行但 Key 名称留空，提交
3. 添加两行，其中一行填名称、一行留空，提交

#### 预期结果

1. Keys **非必填**：无 Key 时提交成功，请求体 `keys` 为空
2. **空行不参与校验**（不触发 name / 权重校验）；提交时**过滤空 name 行**
3. 新增空行不报错；仅存在非空行时才参与唯一性与权重和校验
4. 服务商自身无 keys 时，**隐藏 Keys 表格且不校验**（见 RM-BC-78）

---

### 2.76 Keys 配置-服务商 Key 下拉与权重

**用例编号**: RM-BC-76 | **优先级**: P1  
**OpenAPI**: `llm_config.keys[]`（`name` 必填、非空、组内唯一、必须对应 provider `keys` 中存在的 name；`weight` 0–100 整数）

#### 前置条件

- 选择的服务商含 ≥2 个 keys

#### 测试步骤

1. 添加 Key 行，展开「服务商 Key」下拉
2. 填写权重，再添加一行

#### 预期结果

1. Keys 表格列为：**服务商 Key（下拉）、权重、操作**；下拉选项为该服务商的 key **名称**（不含 key 值明文）
2. 同一 Key 名称在表格中**不可重复选择**：某行已选的 Key 不会出现在其他行下拉中（本行仍显示当前选中项）
3. **新行权重默认 0**（不自动分配 100/N）；可手动修改
4. 可添加 / 删除多行；提交时仍校验名称组内唯一

---

### 2.77 Keys 配置-权重和 = 100（非空时）

**用例编号**: RM-BC-77 | **优先级**: P0  
**OpenAPI**: `llm_config.keys[]`（weight 0–100 整数，权重和 = 100）

#### 前置条件

- 添加了 ≥2 行非空 Key 行

#### 测试步骤

1. 两行权重均填 `50`，提交
2. 修改为 `40` + `40`，提交
3. 只有一行非空，权重填 `100`，提交

#### 预期结果

1. **仅当存在非空 Key 行时**，校验权重和必须等于 100；`50+50` 通过
2. `40+40=80` 拦截，提示权重和须为 100
3. 单行非空权重为 100 通过；权重范围 0–100 整数，越界拦截
4. 空行权重不参与求和

---

### 2.78 服务商无 keys 时隐藏 Keys 表格

**用例编号**: RM-BC-78 | **优先级**: P1  
**OpenAPI**: `llm_config.keys`（默认 `[]`）

#### 前置条件

- 选择了一个 `keys` 为空的服务商

#### 测试步骤

1. 选择无 keys 的服务商，观察大模型配置表单

#### 预期结果

1. Keys 配置表格**隐藏**（不展示空表）
2. 提交不校验 keys，请求体 `keys` 为空
3. 切换到有 keys 的服务商后表格恢复展示

---

### 2.52 key_policy - strategy 选项

**用例编号**: RM-BC-52 | **优先级**: P1  
**OpenAPI**: `llm_config.key_policy.strategy`（默认 `weighted_random`，本版仅支持 `weighted_random`）

#### 前置条件

- 进入第 4 步「大模型配置」

#### 测试步骤

1. 找到「Key 路由策略」Card 中的 strategy 下拉/展示
2. 查看可选值
3. 不做任何修改，使用默认值

#### 预期结果

- strategy 选项仅包含 `weighted_random`
- 默认值为 `weighted_random`
- 无需手动选择（禁用或仅展示）

---

### 2.53 key_policy - max_retries 校验

**用例编号**: RM-BC-53 | **优先级**: P1  
**OpenAPI**: `llm_config.key_policy.max_retries`（默认 0，须为 >=0 的整数）

#### 前置条件

- 进入第 4 步「大模型配置」

#### 测试步骤

1. `max_retries` 填 `-1` → 尝试下一步
2. `max_retries` 填 `0` → 通过
3. `max_retries` 填 `3` → 通过
4. `max_retries` 填 `1.5` → 被拦截

#### 预期结果

- 负数、小数、非数字被拦截
- `0`、正整数通过
- 默认值为 `0`

---

### 2.54 key_policy - retry_backoff_initial 校验

**用例编号**: RM-BC-54 | **优先级**: P1  
**OpenAPI**: `llm_config.key_policy.retry_backoff_initial`（默认 500ms，须为 >=0 的整数）

#### 前置条件

- 进入第 4 步「大模型配置」

#### 测试步骤

1. `retry_backoff_initial` 填 `-1` → 被拦截
2. `retry_backoff_initial` 填 `0` → 通过
3. `retry_backoff_initial` 填 `500` → 通过

#### 预期结果

- 负数、小数、非数字被拦截
- 非负整数通过
- 默认值为 `500`（单位 ms）

---

### 2.55 key_policy - retry_backoff_max 校验

**用例编号**: RM-BC-55 | **优先级**: P1  
**OpenAPI**: `llm_config.key_policy.retry_backoff_max`（默认 5000ms，须为 >=0 的整数，且须 >= retry_backoff_initial）

#### 前置条件

- 进入第 4 步「大模型配置」

#### 测试步骤

1. `retry_backoff_initial=1000`，`retry_backoff_max=500` → 被拦截
2. `retry_backoff_initial=1000`，`retry_backoff_max=1000` → 通过
3. `retry_backoff_initial=500`，`retry_backoff_max=5000` → 通过
4. `retry_backoff_max=-1` → 被拦截

#### 预期结果

- `retry_backoff_max < retry_backoff_initial` 时被拦截，提示不能小于初始退避时间（文案以 design 为准）
- 相等或更大时通过
- 负数、小数、非数字被拦截
- 默认值为 `5000`（单位 ms）

---

### 2.79 Key 路由策略

**用例编号**: RM-BC-79 | **优先级**: P1  
**OpenAPI**: `llm_config.key_policy`（`strategy` / `max_retries` / `retry_backoff_initial` / `retry_backoff_max`）

#### 前置条件

- 进入大模型配置

#### 测试步骤

1. 查看「Key 路由策略」区域
2. 修改 strategy 与重试参数

#### 预期结果

1. `strategy` 下拉仅 `weighted_random`（选项文案「加权随机」）
2. `max_retries` ≥0 整数校验；`retry_backoff_initial` ≥0；`retry_backoff_max` ≥ `retry_backoff_initial`
3. 提交体 `key_policy` 结构：`{strategy, max_retries, retry_backoff_initial, retry_backoff_max}`
4. Keys 为空时 key_policy 不提交或为空（以接口为准）

---

### 2.87 Key 亲和性 Card 展示与默认值

**用例编号**: RM-BC-87 | **优先级**: P1  
**设计依据**: prototype-design `cluster-upsert.js`（`keyAffinityHtml`）  
**OpenAPI**: `llm_config.key_affinity`（enabled 默认 false / ttl 默认 600 / redis_prefix 默认 `bfe:ai:key_affinity` / penalty_enable 默认 true）

#### 前置条件

- 进入创建向导「大模型配置」步骤（或编辑向导大模型配置）

#### 测试步骤

1. 在「Key 路由策略」Card 之后查找「Key 亲和性」Card
2. 查看默认状态与各字段默认值

#### 预期结果

1. 大模型配置页出现「Key 亲和性」Card，位于「Key 路由策略」之后
2. 默认 **启用：关闭**
3. 关闭状态下**不显示** 绑定空闲超时(秒)、Key 惩罚、Redis Key 前缀（条件字段）
4. 提交时默认 `key_affinity = {enabled: false, ttl: 600, redis_prefix: "bfe:ai:key_affinity", penalty_enable: true}`

---

### 2.88 Key 亲和性启用开关与条件字段显隐

**用例编号**: RM-BC-88 | **优先级**: P1  
**OpenAPI**: `llm_config.key_affinity`（enabled / ttl / penalty_enable / redis_prefix）

#### 前置条件

- 进入大模型配置

#### 测试步骤

1. 将「启用」切为「开启」
2. 观察条件字段
3. 再将「启用」切回「关闭」

#### 预期结果

1. 「启用」为下拉（关闭/开启），label 带 `?` 提示「开启后，同一会话的请求将绑定到同一 Key，避免会话内切换时 Key 漂移」
2. 开启后显示：**绑定空闲超时(秒)**（默认 600）、**Key 惩罚**（默认开启，label 带 `?` 提示「开启后，失败的 Key 会被临时惩罚，降低再次被选中概率」）、**Redis Key 前缀**（默认 `bfe:ai:key_affinity`）
3. 关闭后条件字段隐藏

---

### 2.89 Key 亲和性 ttl 校验

**用例编号**: RM-BC-89 | **优先级**: P1  
**OpenAPI**: `llm_config.key_affinity.ttl`（默认 600，须为 >0 的整数）

#### 前置条件

- 进入大模型配置，启用 Key 亲和性

#### 测试步骤

1. 绑定空闲超时(秒) 填 `0` → 尝试下一步
2. 填 `-1` → 尝试下一步
3. 填 `1.5` → 尝试下一步
4. 填 `1` → 通过
5. 填 `600` → 通过

#### 预期结果

- 未启用 Key 亲和性时 ttl 不校验（可留空 / 任意）
- 启用后：`0`、负数、小数被拦截，提示须为大于 0 的整数
- `1`、`600` 等正整数通过
- 默认值为 `600`

---

### 2.90 Key 亲和性 redis_prefix 校验

**用例编号**: RM-BC-90 | **优先级**: P1  
**OpenAPI**: `llm_config.key_affinity.redis_prefix`（默认 `bfe:ai:key_affinity`，若传入须非空）

#### 前置条件

- 进入大模型配置，启用 Key 亲和性

#### 测试步骤

1. Redis Key 前缀 清空 → 尝试下一步
2. 填写自定义前缀（如 `my:prefix`）→ 通过
3. 恢复默认值 `bfe:ai:key_affinity` → 通过

#### 预期结果

- 未启用 Key 亲和性时 redis_prefix 不校验
- 启用后：前缀为空被拦截，提示不能为空
- 非空字符串（含默认值）通过
- 默认值为 `bfe:ai:key_affinity`

---

### 2.91 Key 亲和性提交体结构

**用例编号**: RM-BC-91 | **优先级**: P1  
**OpenAPI**: `clusters.md` `llm_config.key_affinity`

#### 前置条件

- 完成全部步骤，提交创建 / 编辑

#### 测试步骤

1. 启用 Key 亲和性，修改 ttl / Key 惩罚 / Redis 前缀，提交
2. 关闭 Key 亲和性，提交
3. 抓取 `POST /clusters` / `PUT /clusters/{name}` 请求体

#### 预期结果

1. 提交体含 `key_affinity` 对象
2. `enabled` / `penalty_enable` 为布尔（`true`/`false`），`ttl` 为数值，`redis_prefix` 为字符串
3. 启用时字段值 = UI 设置；关闭时提交默认结构（`enabled: false`）

---

### 2.56 LLM 配置界面 Card 分组展示

**用例编号**: RM-BC-56 | **优先级**: P1  
**设计依据**: 多 Key、key_policy 重构后布局 + Key 亲和性（2026-08-26）

#### 前置条件

- 进入第 4 步「大模型配置」

#### 测试步骤

1. 自上而下观察页面布局
2. 核对 Card 标题、数量、分组逻辑

#### 预期结果

- 页面共展示 **5 张 Card**：
  1. **模型服务配置**：所属服务商（下拉）、转发模型（多选）、Header 等
  2. **模型重定向**：source_model / target_model 表格
  3. **服务鉴权 Keys**：多 Key 动态表格（服务商 Key 下拉 / 权重）
  4. **Key 路由策略**：strategy、max_retries、retry_backoff_initial、retry_backoff_max
  5. **Key 亲和性**：启用（关闭/开启）；开启后展示绑定空闲超时(秒)、Key 惩罚、Redis Key 前缀（详见 RM-BC-87 / RM-BC-88）
- Card 间有间距，布局不重叠
- **已移除**：provider_type 输入框、模型列表协议/URI 手动配置、单 key 字段

---

### 2.65 match_prefix / strip_prefix 显隐与开关联动

**用例编号**: RM-BC-65 | **优先级**: P1  
**OpenAPI**: `llm_config.match_prefix` / `llm_config.strip_prefix`

#### 前置条件

- 已登录系统，新建业务集群向导进入第 4 步「大模型配置」
- 默认表单：strip_prefix=false，match_prefix 为空

#### 测试步骤

1. 在「模型服务配置」Card 中观察「裁剪前缀」（strip_prefix）开关状态
2. 观察「匹配前缀」（match_prefix）输入框是否可见
3. 打开「裁剪前缀」开关
4. 观察「匹配前缀」输入框是否出现
5. 在输入框填写 `openrouter/`，再关闭「裁剪前缀」开关
6. 再次打开「裁剪前缀」开关，观察 match_prefix 值

#### 预期结果

- 步骤 1：开关默认关闭（false）
- 步骤 2：match_prefix 输入框**默认隐藏**
- 步骤 3-4：开启开关后 match_prefix 输入框显示（`v-if="strip_prefix"`）
- 步骤 5：关闭开关后 match_prefix 输入框消失
- 步骤 6：重新开启后 match_prefix 已被自动清空（watch 清空逻辑）

#### 测试数据

- match_prefix：`openrouter/`
- strip_prefix：false → true → false → true

---

### 2.66 match_prefix 必填校验（strip_prefix=true）

**用例编号**: RM-BC-66 | **优先级**: P1  
**OpenAPI**: `llm_config.match_prefix`（strip_prefix=true 时必填）

#### 前置条件

- 已登录系统，向导进入第 4 步「大模型配置」

#### 测试步骤

1. 打开「裁剪前缀」开关
2. 保持「匹配前缀」为空
3. 点击「下一步」触发校验

#### 预期结果

- 校验失败，提示「**开启裁剪前缀时，匹配前缀必填**」（i18n `matchPrefixRequiredWhenStrip`）
- 关闭「裁剪前缀」开关后，match_prefix 为空时校验通过

#### 测试数据

- strip_prefix=true，match_prefix 留空

---

### 2.67 match_prefix 格式校验（须以 / 结尾）

**用例编号**: RM-BC-67 | **优先级**: P1  
**OpenAPI**: `llm_config.match_prefix`（传入时必须以 `/` 结尾）

#### 前置条件

- 已登录系统，向导进入第 4 步「大模型配置」
- 已打开「裁剪前缀」开关

#### 测试步骤

1. 在「匹配前缀」输入 `openrouter`（无 `/`）
2. 触发 blur 校验
3. 修改为 `openrouter/`
4. 触发 blur 校验

#### 预期结果

- 步骤 1-2：校验失败，提示「**匹配前缀必须以 / 结尾**」（i18n `matchPrefixMustEndWithSlash`）
- 步骤 3-4：校验通过

#### 测试数据

- 非法：`openrouter`；合法：`openrouter/`

---

### 2.68 match_prefix / strip_prefix 提交与编辑回显

**用例编号**: RM-BC-68 | **优先级**: P1  
**OpenAPI**: `llm_config.match_prefix` / `llm_config.strip_prefix`

#### 前置条件

- 已登录系统，进入业务集群列表页

#### 测试步骤

1. 新建业务集群，第 4 步打开「裁剪前缀」开关并填写 match_prefix=`openrouter/`
2. 完成向导并提交
3. 捕获创建请求体，检查 `llm_config` 中是否包含 match_prefix / strip_prefix
4. 通过 GET /clusters 读回，确认字段保存
5. 编辑该集群进入第 4 步，检查开关与输入框回显
6. 新建另一集群：关闭「裁剪前缀」开关（不填 match_prefix），提交后检查请求体
7. 再次编辑步骤 1 的集群，进入第 4 步，关闭「裁剪前缀」开关，观察 match_prefix 输入框与值
8. 提交编辑，捕获请求体检查 match_prefix 是否被清空/删除；GET /clusters 读回确认

#### 预期结果

- 步骤 3：请求体 `llm_config` 含 `match_prefix: "openrouter/"`、`strip_prefix: true`
- 步骤 4：GET 读回两字段一致
- 步骤 5：编辑回显 strip_prefix=true、match_prefix=`openrouter/`（输入框可见且值正确）
- 步骤 6：请求体**不包含** match_prefix / strip_prefix（为空时删除字段）
- 步骤 7：关闭开关后 match_prefix 输入框消失且值被自动清空（watch 清空逻辑）
- 步骤 8：编辑提交请求体 `llm_config` **不包含** match_prefix（值已清空，字段被删除）、`strip_prefix: false`；GET 读回一致
- 复查页（Review）不展示这两个字段，一致性以请求体 + GET 读回为准

#### 测试数据

- 集群 1：strip_prefix=true，match_prefix=`openrouter/`
- 集群 2：strip_prefix=false，match_prefix 为空

---

### 2.40 模型映射字段回显与提交

**用例编号**: RM-BC-40 | **优先级**: P1  
**自动化**: `tests/cluster/test_05_business_cluster_field_validation.spec.js`  
**OpenAPI**: `llm_config.model_mappings[].source_model` / `target_model`

#### 测试步骤

1. 第 4 步配置重定向（`source_model=A`，`target_model=B`）
2. 查看复查页、提交后 GET、编辑回显

#### 预期结果

- UI 使用 `source_model`/`target_model`（非旧 `key`/`value`）；三处一致

---

### 2.81 模型重定向

**用例编号**: RM-BC-81 | **优先级**: P1  
**OpenAPI**: `llm_config.model_mappings`（模型名称映射数组；`source_model` 不能重复）

#### 前置条件

- 进入大模型配置，已选转发模型

#### 测试步骤

1. 在「模型重定向」表格添加映射（源模型 → 目标模型）
2. 提交并回显

#### 预期结果

1. 支持添加 / 删除重定向行；源模型选项来自转发模型
2. 提交体 `model_mappings` 为映射数组；编辑回显一致
3. 无重定向时传空数组

---

### 2.92 模型重定向联动（选目标后自动填原请求名称）

**用例编号**: RM-BC-92 | **优先级**: P1  
**OpenAPI**: `llm_config.model_mappings`（`source_model` / `target_model`）  
**说明**: 选择「转发的后端模型名称」后，若左侧「原请求的模型名称」为空，则自动填入同名；左侧已填写时不覆盖，仍可手动改。

#### 前置条件

- 进入大模型配置，已选所属服务商与至少两个转发模型

#### 测试步骤

1. 添加一行重定向，只选择右侧目标模型（不填左侧）
2. 确认左侧「原请求的模型名称」已自动填入与目标模型同名
3. 将左侧改为自定义别名后，再更换右侧目标模型
4. 提交并抓取 `POST /clusters` 请求体

#### 预期结果

1. 步骤 2：左侧自动为所选目标模型名称
2. 步骤 3：左侧别名不被覆盖
3. 提交体 `model_mappings` 为 `{ source_model: 别名, target_model: 新目标模型 }`

---

### 2.84 提交体结构（无 instance_pool / provider_type / model_endpoint）

**用例编号**: RM-BC-84 | **优先级**: P0  
**OpenAPI**: `clusters.md` `llm_config` 字段定义

#### 前置条件

- 完成全部 5 步并提交创建

#### 测试步骤

1. 提交并抓取 `POST /clusters` 请求体
2. 对照 clusters.md `llm_config` 字段定义

#### 预期结果

1. 请求体 `llm_config` 包含：`provider`（必填）、`models`（转发模型）、`match_prefix`、`strip_prefix`、`model_mappings`、`keys`（可空）、`key_policy`、`key_affinity`
2. **不再包含** `instance_pool`、`provider_type`、`model_endpoint`、`model_list`、`keys[].key`（key 明文）
3. `keys` 空时提交为 `[]`；`key_policy` 与 `key_affinity` 按默认值提交
4. 创建成功后详情 / 列表正常展示

---

*文档版本: v0.4*  
*最后更新: 2026-09-02（新增 RM-BC-92：模型重定向选目标后自动填原请求名称，已填写则不覆盖）*
