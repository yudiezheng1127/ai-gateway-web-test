[← 返回索引](./00-索引与映射.md)

## 3. 字段校验矩阵

> **验收文案以 design + 本表为准**；无 design 的项在联调后回填。  
> **字段校验自动化**：`test_02`（RM-BC-03~05/07~08/26~28/31~32）、`test_05`（RM-BC-39~41/65~68）、`test_06`（RM-BC-71~78/81/84/92）、`test_07`（RM-BC-52~56/79）、`test_08`（RM-BC-87~91）；其余见下表「自动化」列。「自动化」列为目标映射（`tests/cluster/`），标注「待重构/待新增」的在阶段 4 处理。

---

### 3.1 名称格式规范（横切）

| 模块 | 用户输入字段 | 允许字符 | 禁止/约束 | 长度 | 用例 |
|------|-------------|----------|-----------|------|------|
| 业务集群 | 集群名称 | 字母、数字、`-`、`_`、`.`；首尾限制 | 非法字符、65 字符、首尾 `-`/`_`/`.` | 1–64 | RM-BC-23 |

---

### 3.2 字段校验索引

| 模块 | 字段/场景 | 规则 | 错误提示（02 约定） | 用例编号 | 自动化 |
| ------ | ----------- | ------ | --------------------- | ---------- | -------- |
| 业务集群 | 集群名称 | 格式合法（1–64，首尾限制） | 名称格式/长度错误 | RM-BC-23 | test_03 |
| 业务集群 | 集群描述 | 可选；≤256；禁止控制字符 | 描述长度/控制字符错误 | RM-BC-39 | test_05 |
| 业务集群 | 会话保持 | `sticky_sessions.enabled` 开关；启用时才配置 hash | — | RM-BC-03 | test_02 |
| 业务集群 | 超时/重试 | 超时**可选**（有值时 **> 0**）；写缓存有值时 **> 0**；重试非负整数 | 超时/重试参数不合法 | RM-BC-27、RM-BC-31 | test_02 |
| 业务集群 | 健康检查 host | **可选**；非空时 Hostname（含 IP） | 格式错误 | RM-BC-28、RM-BC-32 | test_02 |
| 业务集群 | 健康检查 statuscode | **0** 或 **100–599** | 状态码范围错误 | RM-BC-41 | test_05 |
| 业务集群 | 集群名称 | **全局唯一** | 名称已存在 | RM-BC-21 | test_03 |
| 业务集群 | 集群名称 | 必填 | 须填写集群名称 | RM-BC-26 | test_02 |
| 业务集群 | 协议 | `http`/`https` 必选 | 请选择协议 | RM-BC-26 | test_02 |
| 业务集群 | 哈希 Header | 启用+CLIENT_ID_ONLY/PREFERED 时**必填**；CLIENT_IP_ONLY 不发送 | — | RM-BC-26 | test_02 |
| 业务集群 | 健康检查 Uri | **可选**；非空时须以 `/` 开头 | Uri 须以 `/` 开头 | RM-BC-28 | test_02 |
| 业务集群 | 空闲连接数 | 0~99999999 | 须为非负整数 | RM-BC-31 | test_02 |
| 业务集群 | 写缓存大小 | **可选**；有值时 **> 0**；默认 512 | 写缓存须大于 0 | RM-BC-31 | test_02 |
| 业务集群 | 所属服务商 | 必填 | 请选择所属服务商 | RM-BC-29、RM-BC-71 | test_06 |
| 业务集群 | 转发模型 | 必填；多选；为服务商 `models` 子集 | 请选择转发模型 | RM-BC-29、RM-BC-73 | test_06 |
| 业务集群 | 模型映射 | `source_model`/`target_model`；选目标后左侧为空则自动填同名，已填写不覆盖 | 原/目标模型名不能为空 | RM-BC-08、RM-BC-40、RM-BC-92 | test_02 / test_05 / test_06 |
| 业务集群 | 重定向原模型名 | 必填 | 原请求的模型名称不能为空 | RM-BC-08 | test_02 |
| 业务集群 | 服务鉴权 Keys | 服务商 Key 下拉（name）+ weight；name 组内唯一（其他行下拉过滤已选项）；weight 0–100 整数；非空时总和=100；空行不计入 | 权重和提示 + name 唯一提示 | RM-BC-75~78 | test_06 |
| 业务集群 | key_policy.strategy | 仅 `weighted_random`；默认 `weighted_random` | — | RM-BC-52 | test_07（待重构） |
| 业务集群 | key_policy.max_retries | >=0 整数；默认 0 | 须为非负整数 | RM-BC-53 | test_07（待重构） |
| 业务集群 | key_policy.retry_backoff_initial | >=0 整数；默认 500ms | 须为非负整数 | RM-BC-54 | test_07（待重构） |
| 业务集群 | key_policy.retry_backoff_max | >=0 整数且 >= initial；默认 5000ms | 须 >= 初始退避时间 | RM-BC-55 | test_07（待重构） |
| 业务集群 | 匹配前缀 match_prefix | **可选**；strip_prefix=true 时**必填**；非空须以 `/` 结尾 | 开启裁剪前缀时，匹配前缀必填 / 匹配前缀必须以 / 结尾 | RM-BC-66、RM-BC-67 | test_05 |
| 业务集群 | 裁剪前缀 strip_prefix | 布尔开关，默认 false；false 时不携带字段 | — | RM-BC-65、RM-BC-68 | test_05 / test_06 |
| 业务集群 | Key 亲和性 key_affinity | enabled 布尔默认 false；开启后 ttl >0 整数默认 600、redis_prefix 非空默认 `bfe:ai:key_affinity`、penalty_enable 布尔默认 true | 空闲超时须为大于 0 的整数 / Redis Key 前缀不能为空 | RM-BC-87~91 | test_08（待新增） |

> **已移除**：集群「实例配置」步骤及其地址/端口/机器名/IP/域名/weight 校验（集群实例改由 Providers 的 `instance_pool` 提供；注意独立的「AI网关实例池」模块（全局 `/alb-pool`）与此无关，见 [gateway-pool](../../gateway-pool/01-测试场景概览.md)）。

---

### 3.3 OpenAPI 对照索引

> 对照 `design-docs/api-define/OpenAPI接口定义/clusters.md`；**GUI 验收仍以 design 为准**，本节标注覆盖情况与差异。

| OpenAPI 文档 | 字段/规则 | API 要求 | 02 覆盖 | 说明 |
| -------------- | ----------- | ---------- | --------- | ------ |
| `clusters` | `name` | 必填，**全局唯一** | RM-BC-21、BC-23 | — |
| `clusters` | `basic.protocol` | `http`/`https` | RM-BC-03 | 向导第 1 步必选 |
| `clusters` | `basic.connection.max_idle_conn_per_rs` | int ≥0 | **RM-BC-31** | 上界 99999999 |
| `clusters` | `basic.buffers.req_write_buffer_size` | 可选；有值时 **> 0** | **RM-BC-31** | 默认 512 |
| `clusters` | `passive_health_check.host` | **可选**；Hostname（含 IP）；留空时使用所属 provider 首个实例 addr | RM-BC-28、**BC-32** | — |
| `clusters` | `passive_health_check.statuscode` | 0 或 100–599 | **RM-BC-41** | — |
| `clusters` | `sticky_sessions.enabled` | 布尔开关 | **RM-BC-03** | 已删除 session_sticky_type |
| `clusters` | `passive_health_check.uri` | **可选**；非空时以 `/` 开头 | RM-BC-28 | 空值提交补默认 `/` |
| `clusters` | `basic.retries.max_retry_in_cluster` | 非负整数 | **RM-BC-27** | 已重命名 |
| `clusters` | `llm_config.provider` | 必填；引用已存在的 provider | RM-BC-71、BC-29 | 所属服务商 |
| `clusters` | `llm_config.models` | 必填；元素非空不重复；为 provider `models` 子集 | RM-BC-73、BC-74 | 转发模型 |
| `clusters` | `llm_config.model_mappings` | source_model/target_model | **RM-BC-40** | 已重命名 |
| `clusters` | `llm_config.keys[]` | name 引用 provider key 名且组内唯一；weight 0–100 整数；总和=100；默认 `[]`；无 key 明文 | RM-BC-75~78 | 服务鉴权 Keys（name/weight） |
| `clusters` | `llm_config.key_policy.strategy` | 默认 `weighted_random`；本版仅 `weighted_random` | RM-BC-52 | Key 路由策略 |
| `clusters` | `llm_config.key_policy.max_retries` | >=0 整数；默认 0 | RM-BC-53 | — |
| `clusters` | `llm_config.key_policy.retry_backoff_initial` | >=0 整数；默认 500 | RM-BC-54 | 单位 ms |
| `clusters` | `llm_config.key_policy.retry_backoff_max` | >=0 整数且 >= initial；默认 5000 | RM-BC-55 | 单位 ms |
| `clusters` | `llm_config.match_prefix` | string；strip_prefix=true 时必填；必须以 / 结尾 | RM-BC-66、BC-67 | — |
| `clusters` | `llm_config.strip_prefix` | bool；默认 false；为空删除 | RM-BC-65、BC-68 | — |
| `clusters` | `llm_config.key_affinity` | enabled bool 默认 false；ttl int >0 默认 600；redis_prefix string 非空默认 `bfe:ai:key_affinity`；penalty_enable bool 默认 true | RM-BC-87~91 | 2026-08-26 Key 亲和性 |

**未纳入 GUI 用例的 API 层校验**（联调/API 测试另测）：子集群/实例池未就绪（`norms` 510）等。

---

### 3.4 业务集群向导字段校验矩阵

> 5 步创建向导逐步对照；**design 已有各步校验截图**（`02-step-basic-*`、`03-step-timeout-*`、`04-step-health-*`、`06-step-model-*`）。编辑向导为 5 步（与创建一致），第 1~4 步规则同下。  
> **「覆盖状态」= 02 用例正文是否已编写**，不等于 Playwright 已自动化；自动化见「自动化」列与 [§3.8](#38-自动化覆盖状态)。

| 步骤 | 字段/场景 | 规则 | 用例 | design | 覆盖状态 | 自动化 |
| ------ | ----------- | ------ | ------ | -------- | ---------- | -------- |
| 1 基础配置 | 集群名称 | 必填、格式、全局唯一 | BC-26、BC-21、BC-23 | `02-step-basic-required.jpg` 等 | ✅ | test_02 / test_03 |
| 1 | 协议 http/https | 必选 | BC-26 | `02-step-basic-protocol.jpg` | ✅ | test_02 |
| 1 | 哈希策略 + Header | 启用+CLIENT_ID_ONLY/PREFERED 时 `hash_header` **必填** | BC-26 | `02-step-basic-no-hash-header.jpg` | ✅ | test_02 |
| 1 | 会话保持 enabled | true/false 开关 | BC-03 | 待补 | ✅ | test_02 |
| 1 | 描述 | ≤256，无控制字符 | BC-39 | 待补 | ✅ | test_05 |
| 2 超时重传 | 各超时(ms) | **可选**；有值时 **> 0** | BC-27 | `03-step-timeout-required.jpg` | ✅ | test_02 |
| 2 | 集群内重试 max_retry_in_cluster | 非负整数 | BC-27 | 同上 | ✅ | test_02 |
| 3 健康检查 | host | **可选**；Hostname/IP | BC-28、BC-32 | 待补 | ✅ | test_02 |
| 3 | uri/failnum/interval/statuscode | **均可选**；有值时按格式/范围校验 | BC-28 | `04-step-health-required.jpg` | ✅ | test_02 |
| 3 | statuscode | 0 或 100–599 | BC-41 | 待补 | ✅ | test_04 |
| 4 大模型 | 所属服务商 | 必填下拉 | BC-71、BC-29 | 待补 | ✅ | test_04 / test_06 |
| 4 大模型 | 转发模型 | 必填多选 | BC-73、BC-29 | 待补 | ✅ | test_04 / test_06 |
| 4 大模型 | 服务鉴权 Keys（多 Key 表格） | 服务商 Key 下拉（name）组内唯一；weight 0–100 整数；非空时总和=100；空行不校验；无 key 明文 | BC-75~78 | 待补 | ✅ | 待自动化 |
| 4 大模型 | key_policy（strategy/max_retries/backoff） | strategy 仅 weighted_random；max_retries>=0；backoff_initial>=0；backoff_max>=initial | BC-52~55 | 待补 | ✅ | 待自动化 |
| 4 大模型 | Key 亲和性 | enabled 默认 false；启用后 ttl>0、redis_prefix 非空、penalty_enable 默认 true | BC-87~91 | 待补 | ✅ | 待自动化 |
| 4 大模型 | LLM 配置 Card 分组 | 5 张 Card：模型服务/模型重定向/服务鉴权Keys/Key路由策略/**Key 亲和性** | BC-56、BC-87 | 待补 | ✅ | 待自动化 |
| 4 | 模型映射 | source_model/target_model；选目标后左侧为空则自动填同名，已填写不覆盖 | BC-40、BC-92 | 待补 | ✅ | test_06 |
| 4 | 模型重定向 | source_model 非空 | BC-08 | `06-step-model-redirect-error.jpg` | ✅ | test_02 |
| 4 | 裁剪/匹配前缀 | strip_prefix 默认 false；开启后 match_prefix 必填且以 `/` 结尾 | BC-65~68 | 待补 | ✅ | test_05 / test_06 |
| 1 | 空闲连接数 | 0~99999999 | BC-31 | 待补 | ✅ | test_02 |
| 1 | 写缓存大小 | **可选**；有值时 **> 0**，默认 512 | BC-31 | 待补 | ✅ | test_02 |
| 5 复查 | — | 只读核对 | BC-11 | `08-step-review.jpg` | 成功路径 | test_03 |
| 5 | 创建成功主路径 | 5 步提交后列表可见 | BC-02 | 同上 | ✅ | test_03 |
| — | 删除被路由引用 | 引用约束 | BC-14 | 待补 | ✅ | test_03 |

> **已移除**：原「4 实例配置」步骤及对应地址/端口/域名/weight/形态切换校验（实例池由 Providers 维护）。

---

### 3.5 边界值校验索引

> **下界/上界/越界**三类样本；验收以 design 为准。与必填/格式用例配合使用。

| 模块 | 字段 | 下界 | 上界 | 非法样本 | 合法边界样本 | 用例 | 自动化 |
| ------ | ------ | ------ | ------ | ---------- | -------------- | ------ | -------- |
| 业务集群 | 集群名称长度 | 1 | 64 | 65 字符、`-x` | `a`、`bc_{ts}` | RM-BC-23 | test_03 |
| 业务集群 | 集群描述 | 0 | 256 | 257 字符、控制字符 | 留空、256 字符 | RM-BC-39 | test_04 |
| 业务集群 | 健康检查 Host | 可选 | 255 | `-bad.com` | `127.0.0.1`、`www.test1.com` | RM-BC-32 | test_02 |
| 业务集群 | 空闲连接数 | 0 | 99999999 | `-1`、`100000000` | `0`、`99999999` | RM-BC-31 | test_02 |
| 业务集群 | 写缓存大小 | **> 0** | — | null/空、**`0`** | `512`、`1` | RM-BC-31 | test_02 |
| 业务集群 | 各超时(ms) | **> 0** | 99999999 | **`0`**、`-1` | 默认、`1` | RM-BC-27、BC-31 | test_02 |
| 业务集群 | 集群内重试 | 0 | — | `-1` | `0`、`2` | RM-BC-27 | test_02 |
| 业务集群 | 期望状态码 | 0 或 100 | 599 | `99`、`600`、`-1` | `0`、`200` | RM-BC-41 | test_04 |
| 业务集群 | 服务鉴权 Key weight | 0 | 100 | `-1`、`101`、多行和 ≠100 | `0`、`100`；双行 50+50 | RM-BC-76、BC-77 | 待自动化 |
| 业务集群 | key_policy.max_retries | 0 | — | `-1`、小数 | `0`、`3` | RM-BC-53 | 待自动化 |
| 业务集群 | key_policy.retry_backoff_initial | 0 | — | `-1`、小数 | `0`、`500` | RM-BC-54 | 待自动化 |
| 业务集群 | key_policy.retry_backoff_max | 等于 initial | — | < initial、`-1` | 等于 initial、`5000` | RM-BC-55 | 待自动化 |
| 业务集群 | match_prefix | 无下界（为空合法，但 strip_prefix=true 时必填） | — | 不以 / 结尾（openrouter） | openrouter/ | RM-BC-66、BC-67 | test_04 |
| 业务集群 | key_affinity.ttl | 1 | — | `0`、`-1`、小数（启用时） | `1`、`600` | RM-BC-89 | 待自动化 |
| 业务集群 | key_affinity.redis_prefix | 非空（启用时） | — | 空串（启用时） | `bfe:ai:key_affinity`、`my:prefix` | RM-BC-90 | 待自动化 |

**业务集群向导**：BC-26~41 必填/格式/联动；**RM-BC-31** 数值边界；**RM-BC-32/40** Host 与模型映射。

---

### 3.7 UI↔OpenAPI 读回一致性

> **目的**：发现「查看/列表/编辑回显/复查」与接口返回不一致。自动化：`Promise.all` 拉取 OpenAPI + 打开 UI，按映射表断言。  
> **优先级**：P1，纳入回归；造数可用 UI 创建后以 GET 为基准（或 API 造数后 UI 查看）。

| 用例 | UI 场景 | OpenAPI 读接口 | 比对范围 | 自动化 |
| ------ | --------- | ---------------- | ---------- | -------- |
| RM-BC-34 | 详情页 | `GET .../clusters/{name}` | 见下表 | test_03 |
| RM-BC-35 | 复查页 | 第 1~5 步输入快照 + 提交后 GET | 逐步摘要一致 | test_03 |
| RM-BC-36 | 编辑向导各步 | `GET .../clusters/{name}` | 各步预填 | test_03 |
| RM-BC-37 | 业务集群列表行 | `GET /clusters` | 集群名称集合 | test_01 |
| RM-BC-38 | 编辑复查页 | 编辑向导第 1~4 步输入 | 各段摘要与输入逐项一致 | test_03 |

#### 业务集群详情字段映射（RM-BC-34 / RM-BC-36）

| UI 展示 | OpenAPI 路径 | 备注 |
| --------- | -------------- | ------ |
| 协议 | `basic.protocol` | |
| 最大空闲连接数 | `basic.connection.max_idle_conn_per_rs` | |
| 写缓存 | `basic.buffers.req_write_buffer_size` | |
| 哈希策略 / Header / 会话保持 enabled | `sticky_sessions.*` | 含 `enabled` 布尔值 |
| 各超时(ms) | `basic.timeouts.*` | 可选；有值时须 > 0 |
| 重试次数 | `basic.retries.max_retry_in_cluster` | 已重命名 |
| 健康检查各字段 | `passive_health_check.*` | 均可选；空值提交补默认 |
| 大模型配置 | `llm_config.*` | 含 `provider`（所属服务商）、`models`、`model_mappings`（source_model/target_model）、`keys[]`（name/weight，无 key 明文）、`key_policy`（strategy/max_retries/retry_backoff_initial/retry_backoff_max）、`key_affinity`（enabled/ttl/redis_prefix/penalty_enable）、match_prefix、strip_prefix |

> **不再展示「实例配置」**：实例池由 Provider 维护，集群详情/复查不再展示实例池明细。

#### 自动化实现要点

- 列表类：API 集合与 UI 行集合比对（忽略顺序或按 name 排序后 zip）
- 详情/编辑：逐字段 `expect(ui).toEqual(api)`
- 名称：UI 可能隐藏产品线/BFE 前缀，比对时统一规范化（见各章命名规则）
- 失败定位：记录 UI 值、API 值、字段路径三方 diff

---

### 3.8 自动化覆盖状态

| 优先级 | 用例 | 内容摘要 | 自动化（目标映射，`tests/cluster/`） |
| -------- | ------ | ---------- | -------- |
| ✅ | RM-BC-01、37 | 业务集群列表与 API 一致 | `test_01` |
| ✅ | RM-BC-03~05、07~08、26~28、31~32 | 向导各步 | `test_02`（需按 5 步重写） |
| P0 | RM-BC-02 | 5 步向导创建成功 | `test_03`（需按 5 步重写） |
| P0 | RM-BC-14 | 被路由引用时删除失败 | `test_03` |
| P1 | RM-BC-34~36、38 | UI↔OpenAPI 读回一致性 | `test_03` |
| P1 | RM-BC-39~41、65~68 | 字段校验补充 | `test_05`（已清理；RM-BC-29 拆解至 test_06） |
| P1 | RM-BC-52~56、79 | key_policy / Card 分组 / Key 路由策略 | `test_07`（待重构） |
| ✅ | RM-BC-71~78、81、84、92 | 大模型配置（provider / 转发模型 / Keys / 重定向 / 左右联动） | `test_06`（待重构） |
| ✅ | RM-BC-87~91 | Key 亲和性 | `test_08`（待新增） |

---

### 3.6 变更说明

> 以下项已在 UI 实现中闭合，**不再作为「待定」探针**：

| 编号 | 字段 | 结论 |
| ------ | ------ | ------ |
| 原待定-01 | `req_write_buffer_size` | **须 > 0**；`0` 拦截 |
| 原待定-02 | 健康检查 `host` | **可选**；允许 IP（`127.0.0.1` 通过）；留空时使用所属 provider 首个实例 addr |
| 2026-07-31 | 被动健康检查各字段 | **均可选**；提交时空值补默认（failnum=3、interval=1000、uri=`/`、statuscode=0） |
| 2026-07-31 | 超时字段 | **均可选**；有值时须 > 0 |
| 2026-08-05 | R1：hash_header 必填校验 | 启用+CLIENT_ID_ONLY/PREFERED 时 hash_header **必填**（留空被拦截）；CLIENT_IP_ONLY 不要求 |
| 2026-08-05 | R3：集群删除引用信息展示 | 删除被引用集群时弹窗展示完整引用信息（路由表类型/属主+规则名），含「前往处理」跳转 |
| 2026-08-14 | R7：多 Key 加权轮询 + provider 字段 | `llm_config` 单 key → `keys[]`（name/weight）多 Key 数组 + `key_policy` 对象；新增 `provider` 字段（所属服务商，必填下拉）；LLM 配置界面按 Card 分组 |
| 2026-08-16 | R9：match_prefix + strip_prefix 字段 | llm_config 新增 match_prefix（string，须以 / 结尾）与 strip_prefix（bool，默认 false）；输入框默认隐藏；开启后 match_prefix 必填；关闭时自动清空；为空则删除字段；编辑回显 |
| 2026-08-24/25 | R8：Provider 分离与 5 步向导 | 实例池 / 模型协议 / 模型发现端点 / Keys 明文从 Cluster 迁入 Provider；Cluster 通过 `llm_config.provider` 引用；向导由 6 步精简为 5 步（移除「实例配置」）；复查页不再展示实例池 |
| 2026-08-26 | R10：Key 亲和性 | llm_config 新增 `key_affinity`（enabled bool 默认 false / ttl int >0 默认 600 / redis_prefix string 非空默认 `bfe:ai:key_affinity` / penalty_enable bool 默认 true）；大模型配置新增「Key 亲和性」Card（5 张 Card）；启用后条件展示 ttl/Key 惩罚/Redis 前缀；复查页与编辑回显一致（RM-BC-87~91） |

---

## 文档版本

v0.26  
最后更新：2026-08-27（req_write_buffer_size 改为「可选，有值时 >0」，对齐 clusters.md）
