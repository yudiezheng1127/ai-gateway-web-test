# AI 业务集群（Business Cluster）模块 - 测试场景概览

## 模块概述

AI 业务集群模块负责配置路由转发的目标集群，是资源管理的核心模块。集群通过 `llm_config.provider` 引用模型服务商（Providers），由 Provider 提供实例池、模型列表、Keys 等数据。

对应接口文档：`ai-gateway-web/design-docs/api-define/OpenAPI接口定义/clusters.md`；对应原型：`ai-gateway-web/design-docs/prototype-design/pages/cluster-wizard.html`、`cluster-list.html`。

> **关联模块**：
>
> - **模型服务商（Providers）**：集群强依赖 Provider；Provider 的 `instance_pool`、`models`、`keys` 会同步影响引用它的集群。
> - **路由管理（Route Management）**：路由规则通过 `cluster_name` 指向业务集群。
> - **AI 网关实例池（Gateway Pool）**：全局单例资源（`/alb-pool`，网关转发引擎实例集合），与 Provider 的 `instance_pool`（服务商后端实例）是两个独立对象；集群的实例配置已改由 Provider 提供，与本网关实例池无关。详见 [gateway-pool 模块说明](../gateway-pool/01-测试场景概览.md)。

---

## 配置顺序

```text
① 配置模型服务商（Providers）          基础数据：实例池 / 模型 / Keys
        │
        ▼
② 配置 AI 业务集群（Clusters）          llm_config.provider 指向服务商 → 转发模型 / Keys
        │
        ▼
③ 路由规则 / 全链路                     将请求转发至集群
```

**依赖说明**：

| 关系 | 说明 |
| --- | --- |
| Providers → Clusters（强依赖） | 必须先创建 Provider，集群大模型配置才能选择服务商、转发模型和 Keys |
| Clusters → Route（被引用） | 集群可被默认规则或路由规则引用；被引用时不可删除 |

---

## 场景测试设计

### 场景一：管理员搭建 AI 业务集群

**场景描述**：管理员创建 AI 业务集群并配置相关参数，形成路由可指向的目标集群。

| 步骤 | 操作 | 对应场景编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | 进入 AI 业务集群，添加集群（**5 步**向导） | RM-BC-02 | P0 |
| 2 | 配置基础信息（名称、协议、连接数/缓存边界、哈希 Header 联动） | RM-BC-03、RM-BC-21、RM-BC-23、RM-BC-26、RM-BC-31 | P0/P1 |
| 3 | 配置超时与重传、健康检查（含 Host FQDN、逐步校验） | RM-BC-04~RM-BC-05、RM-BC-27~RM-BC-28、RM-BC-32 | P1 |
| 4 | 配置大模型：**所属服务商（必填下拉，引用 Provider）**、转发模型（全选）、裁剪/匹配前缀、模型重定向、Keys 配置（服务商 Key + 权重，可选）、Key 路由策略、hash_strategy、**Key 亲和性** | RM-BC-07、RM-BC-40、RM-BC-52~56、RM-BC-65~68、RM-BC-71~79、RM-BC-81、RM-BC-87~92 | P0/P1 |
| 5 | 大模型必填/格式、重定向 | RM-BC-08、RM-BC-29、RM-BC-73~RM-BC-75、RM-BC-92 | P1 |
| 6 | 第 5 步复查确认并提交（**复查页完整展示大模型配置，不含实例池**） | RM-BC-11、RM-BC-35 | P0 |
| 7 | 编辑集群（**5 步**，与创建步骤一致）；名称不可改 | RM-BC-12、RM-BC-16、RM-BC-36 | P0 |
| 8 | 点击「详情」查看集群全配置只读 | RM-BC-15、RM-BC-34 | P1 |
| 9 | 删除无引用的集群 | RM-BC-13 | P0 |
| 10 | 删除被默认规则/路由规则引用的集群（约束） | RM-BC-14 | P0 |

**测试目标**：验证业务集群创建与编辑流程；删除约束正确。

> **向导步骤（2026-08-24/25 变更后）**：**创建 5 步**（基础配置 → 超时和重传 → 被动健康检查 → **大模型配置** → 复查&检查）；**编辑 5 步**（与创建一致）。已移除「实例配置」步骤——实例池改由**模型服务商（Providers）**维护，集群通过 `llm_config.provider` 引用；复查页完整展示大模型配置字段（不含实例池）；`hash_strategy` 默认 `CLIENT_IP_ONLY`；Keys **非必填**（空行不参与校验、提交时过滤空行）。
>
> **Key 亲和性（2026-08-26）**：大模型配置新增「Key 亲和性」Card（`llm_config.key_affinity`，默认 `enabled: false`）；启用后展示绑定空闲超时(秒)（默认 600，>0 整数）、Key 惩罚（默认开启）、Redis Key 前缀（默认 `bfe:ai:key_affinity`，非空）；复查页展示启用状态与明细，编辑回显一致。验收以 [02d 大模型与校验](./02-功能测试用例/02d-大模型与校验.md)（RM-BC-87~91）与 [02e 一致性验证](./02-功能测试用例/02e-一致性验证.md)（复查/回显）为准。

---

## 测试场景统计

| 功能区域 | 测试场景数量（规划） | 优先级分布（规划） |
| --- | --- | --- |
| AI业务集群 | 56个 | P0: 15, P1: 41 |
| **总计** | **56个** | **P0: 15, P1: 41** |

---

## 测试用例编号规则

| 前缀 | 含义 | 示例 |
| --- | --- | --- |
| RM-BC-xx | AI业务集群（Business Cluster） | RM-BC-01 ~ RM-BC-16、RM-BC-21、RM-BC-23、RM-BC-26 ~ RM-BC-32、RM-BC-34 ~ RM-BC-41、RM-BC-52 ~ RM-BC-56、RM-BC-65 ~ RM-BC-68、RM-BC-71 ~ RM-BC-79、RM-BC-81、RM-BC-84、RM-BC-87 ~ RM-BC-92 |

### 自动化文件映射

| 层级 | 文件（`tests/cluster/`） | 用例范围 | 说明 |
| --- | --- | --- | --- |
| 业务集群列表 | `test_01_business_cluster_list` | RM-BC-01、37 | 列表与 API 一致 |
| 业务集群向导 | `test_02_business_cluster_wizard` | RM-BC-03~05、07~08、26~28、31~32 | 各步展示与校验（需按 5 步重写） |
| 业务集群 CRUD | `test_03_business_cluster_crud` | RM-BC-02、11~16、21、23、34~36、38 | 创建/编辑/删除/一致性（需按 5 步重写） |
| 实例配置校验 | `test_04_business_cluster_instance_validation` | — | **待删除**：实例配置已迁入 Providers |
| 字段校验补充 | `test_05_business_cluster_field_validation` | RM-BC-39~41、65~68 | P1 边界与格式（实例校验已迁入 Providers；RM-BC-09 废弃、RM-BC-29 拆解至 test_06） |
| 大模型配置与 Provider 联动 | `test_06_business_cluster_model` | RM-BC-71~78、81、84、92 | 所属服务商联动、转发模型全选、Keys 可选与权重、模型重定向（含左右联动）、提交体（需重写） |
| Key 路由策略与 Card 分组 | `test_07_business_cluster_key_policy` | RM-BC-52~56、79 | key_policy、Card 分组、Key 路由策略（需清理已删除的多 Key key 明文用例） |
| Key 亲和性 | `test_08_business_cluster_key_affinity` | RM-BC-87~91 | **本次新增**：Key 亲和性 Card、校验、提交体（复查/回显归入 test_03） |

> 网关实例池（RM-GP-xx）为独立模块（全局 `/alb-pool` 单例，非 Provider 的 `instance_pool`），用例与自动化（`tests/gateway-pool/test_01_gateway_pool_create.spec.js`）见 [gateway-pool 文档](../gateway-pool/01-测试场景概览.md)。

---

## 优先级定义

| 优先级 | 级别 | 说明 |
| --- | --- | --- |
| P0 | Critical | 核心配置路径、删除约束，必须通过 |
| P1 | High | 搜索、编辑、表单校验、取消操作，应该通过 |
| P2 | Medium | 分页、边界、可选步骤，建议通过 |

---

## 测试前置条件

| 测试场景 | 前置条件 |
| --- | --- |
| RM-BC-02 创建 AI 业务集群 | 需已存在 ≥1 个模型服务商（Provider），供「所属服务商」下拉选择 |
| RM-BC-16 编辑集群 | 需已创建集群（RM-BC-02） |
| RM-BC-15 查看详情 | 需已创建集群（RM-BC-02） |
| RM-BC-14 删除被路由引用集群 | 需集群已被默认规则或路由规则引用（见 RT 模块） |

---

## 测试数据准备

| 数据类别 | 准备内容 | 用途 |
| --- | --- | --- |
| 业务集群 | 完成大模型最小配置（含多 Key、key_policy、provider） | 默认规则/路由目标 |

| 数据项 | 说明 | 示例值 |
| --- | --- | --- |
| 动态命名 | 时间戳后缀避免冲突 | `bc_{ts}` |
| 删除顺序 | 路由 → 业务集群 | — |
| 服务鉴权 Keys | 服务商 Key 下拉（name）+ 权重（weight）两列，支持多行动态增删；非空时 weight 总和=100；空行不校验 | `[{name: key-1, weight: 70}, {name: key-2, weight: 30}]` |
| key_policy | strategy=weighted_random；max_retries=0；retry_backoff_initial=500；retry_backoff_max=5000 | Key 路由策略与重试配置 |
| 所属服务商（provider） | 必填下拉，引用已存在的 Provider；选择后拉取其 models/keys | `deepseek`、`qwen` |
| match_prefix / strip_prefix | match_prefix 字符串必须以 `/` 结尾；strip_prefix 布尔默认 false | `openrouter/` + `true` |
| key_affinity | enabled 布尔默认 false；ttl 整数默认 600（>0）；redis_prefix 字符串默认 `bfe:ai:key_affinity`（非空）；penalty_enable 布尔默认 true | `{enabled: true, ttl: 600, redis_prefix: "bfe:ai:key_affinity", penalty_enable: true}` |

---

## 自动化实施建议

| 阶段 | 范围 | 说明 |
| --- | --- | --- |
| 第一期 | P0 主路径 | 各链路的「创建成功 + 关键校验 + 删除约束」标杆 |
| 第二期 | P1 用例 | 搜索、编辑、多步表单子步骤 |

**造数策略**：深层依赖可用 OpenAPI 预置；当前页 UI 行为与 `DOC_*` 验收仍以 design/02 为准。

---

## 相关文档

- [功能测试用例](./02-功能测试用例/00-索引与映射.md) — 详细步骤与预期
- [设计原型](../../../ai-gateway-web/design-docs/prototype-design/pages/clusters.html) — 验收原型
- [模型服务商](../providers/01-测试场景概览.md) — Provider 为集群大模型配置的数据源
- [路由管理](../route-management/01-测试场景概览.md) — 默认规则与路由规则（依赖本模块）
- [AI 网关实例池](../gateway-pool/01-测试场景概览.md) — 全局网关转发引擎实例池（独立于 Provider 的 `instance_pool`）
- `.trae/skills/ai-gateway-test-generation/SKILL.md` — 生成与验收规范

---

---

## 变更说明

### v0.4 (2026-09-02)

**变更主题**：模型重定向左右联动（选目标后自动填原请求名称）

**变更内容**：

- 新增 RM-BC-92：只选右侧目标模型时，左侧为空则自动填入同名；左侧已填写则不覆盖
- 场景统计：55 → 56（+1），P1: 40→41
- 自动化：`test_06_business_cluster_model` 覆盖 RM-BC-92

### v0.3 (2026-08-27)

**变更主题**：移除「变更回归」分层，将变更回归用例归并至功能章节，消除新旧口径冲突

**变更内容**：

- 删除 02f（5 步向导与大模型配置变更回归）、02g（Key 亲和性变更回归）两个独立文件
- 02d「大模型与校验」归并所属服务商/转发模型/Keys（name/weight）/Key 路由策略/Key 亲和性字段用例；删除旧「价格关联提供商」（RM-BC-60~64）与「多 Key 含 key 明文」（RM-BC-43~50/57）冲突用例
- 02b「向导步骤」归并 5 步向导说明与 hash_strategy 默认 `CLIENT_IP_ONLY`
- 02e「一致性验证」归并复查页/编辑回显/详情的大模型与 Key 亲和性展示；清理「价格关联提供商」旧表述
- 统一字段名 `model_mappings`（source_model/target_model）

### v0.2 (2026-08-26)

**变更主题**：集群大模型配置新增 Key 亲和性（key_affinity）

**变更内容**：

- 大模型配置步骤新增第 5 张 Card「Key 亲和性」：enabled 开关、ttl（默认 600）、redis_prefix（默认 `bfe:ai:key_affinity`）、penalty_enable（默认 true）
- 字段校验矩阵（03）新增 key_affinity 边界行（ttl >0 整数、redis_prefix 非空）
- 一致性验证（02e）新增 key_affinity 字段映射

---

*文档版本: v0.4*  
*最后更新: 2026-09-02*
